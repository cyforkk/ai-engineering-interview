name: docs-check

on:
  push:
    paths:
      - "docs/**"
      - "tools/check_md_links.py"
      - "tools/thin_knowledge_whitelist.txt"
      - ".github/workflows/docs-check.yml"
  pull_request:
    paths:
      - "docs/**"
      - "tools/check_md_links.py"
      - "tools/thin_knowledge_whitelist.txt"

jobs:
  md-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Check links + thin knowledge whitelist
        run: python tools/check_md_links.py
