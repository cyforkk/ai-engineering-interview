# -*- coding: utf-8 -*-
"""加厚偏薄知识点：白话 + 可照着说 + 追问 + 链面渣"""
from pathlib import Path

DOCS = Path(__file__).resolve().parents[1] / "docs"


def w(name: str, text: str) -> None:
    p = DOCS / name
    p.write_text(text.strip() + "\n", encoding="utf-8")
    print(f"wrote {name} ({p.stat().st_size} bytes)")


# ---------------------------------------------------------------------------
OS = r'''
# 操作系统 · 知识点（精讲·可照着说）

<!-- NAV:START -->
> **导航** → [OS·面渣（主教材，长口述）](./操作系统面渣级口述.md)
>
> ⚠️ 若只背下面提纲仍不会说，**直接打开面渣全文通读 + 录音**。
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md)
>
<!-- NAV:END -->

> **读法：** 每个考点先看 **白话**，再 **照着「面试怎么答」念 1～2 遍**，最后练追问。  
> 完整故事版、结合 Java/Redis/容器的展开，以 [操作系统面渣级口述.md](./操作系统面渣级口述.md) 为准。

---

## 一、本专题要搞定什么

面试官问 OS，不是考你背内核源码，而是：

1. 进程/线程分得清，能否接到 **Java 并发**
2. 用户态/内核态、系统调用 **有成本** 你知不知道
3. 网络服务器为什么提 **epoll**（连到 Nginx/Redis/Netty）
4. 死锁、虚拟内存能否用 **一句话 + 例子** 说完

---

## 二、进程 vs 线程（P0）

### 白话

- **进程**：操作系统分配资源的基本单位。每个进程有独立地址空间（自己的「内存地盘」），互相默认隔离，A 崩了一般不直接写坏 B 的内存。
- **线程**：调度的基本单位（CPU 时间片切给谁）。同一进程里的线程 **共享堆、全局变量**，通信便宜，但共享数据就要同步（锁），否则竞态。
- **Java 视角**：`new Thread` / 线程池里的线程，共享同一 JVM 堆 → 所以 HashMap 多线程会出事，要 ConcurrentHashMap 或同步。

### 面试怎么答（可直接照着说，约 1 分钟）

> 进程是资源分配单位，有独立地址空间，隔离性强；线程是 CPU 调度单位，同进程线程共享堆和全局数据，切换开销通常比进程小。  
> 优点是通信快、创建轻；代价是一个线程写坏共享数据会影响全进程，必须做同步。  
> 在 Java 里所有业务线程共享堆，所以集合、单例、缓存都要考虑线程安全；Redis 单线程执行命令也是用「少共享可变状态」换简单正确。

### 追问

| 问 | 答要点 |
|----|--------|
| 进程通信方式？ | 管道、消息队列、共享内存、Socket 等 |
| 协程和线程？ | 用户态调度，更轻；Python asyncio / Go goroutine 思路相近 |
| 为何线程池？ | 复用，避免反复创建销毁；控制并发度 |

**完整口语稿：** [OS面渣 · 进程线程](./操作系统面渣级口述.md)

---

## 三、用户态 / 内核态 / 系统调用（P0）

### 白话

普通程序跑在 **用户态**，不能随便碰硬件、改页表。要读文件、建网络连接，必须走 **系统调用** 进入 **内核态**，由内核代劳。  
每次切换有成本（保存寄存器、权限检查）。所以高性能 IO 会：批量读写、缓冲、mmap、零拷贝，减少「用户↔内核」来回次数。

### 面试怎么答

> 用户程序不能直接访问硬件，需要通过系统调用陷入内核。  
> 用户态到内核态的切换有开销，所以高 QPS 服务会减少无意义的 syscall，用缓冲、批量、epoll 多路复用、必要时零拷贝。  
> 比如频繁小 write 不如缓冲后批量；Redis/Nginx 用事件驱动少阻塞在 syscall 上。

### 追问

| 问 | 答要点 |
|----|--------|
| 常见 syscall？ | read/write/open/socket/epoll_wait… |
| 零拷贝？ | 减少内核与用户空间数据拷贝次数（sendfile 等） |

---

## 四、上下文切换（P0）

### 白话

CPU 从一个线程切到另一个：要保存当前寄存器、程序计数器，再恢复目标线程状态。  
切换本身不是免费的；**切换太频繁**会出现：CPU 占用高、业务吞吐却上不去（都在「换人干活」）。

### 面试怎么答

> 上下文切换是调度时保存/恢复线程运行现场。  
> 过多切换会浪费 CPU。工程上用线程池限制线程数、用异步/事件驱动减少阻塞线程数量，避免「线程爆炸」。

---

## 五、epoll 与 IO 多路复用（P1，后端高频）

### 白话

一台机器要服务上万连接。若 **一个线程阻塞读一个连接**，线程会爆。  
**IO 多路复用**：一个线程监听很多 fd，谁就绪（有数据可读/可写）再处理谁。

- **select/poll**：每次把关注列表交给内核，内核线性扫，连接多了慢。  
- **epoll**（Linux）：用就绪队列，活跃连接多时更合适。  
Nginx、Redis、Netty 都是这类思路。

### 面试怎么答（约 1 分钟）

> 高并发网络服务不能一连接一线程阻塞读。  
> 用 IO 多路复用：一个线程通过 epoll 监听大量连接，只处理就绪的那些。  
> select/poll 每次要扫大量 fd；epoll 维护就绪列表，适合 C10K 以上场景。  
> 我理解 Redis 单线程处理命令、Nginx worker 事件循环，底层都受益于这种模型。

### 追问

| 问 | 答要点 |
|----|--------|
| 水平触发 vs 边缘触发？ | LT 只要还有数据就通知；ET 状态变化才通知，要一次读干净 |
| 和 Java NIO 关系？ | Selector 多路复用思想一致 |

**完整稿：** [OS面渣 · epoll](./操作系统面渣级口述.md)

---

## 六、死锁（P0）

### 白话

多个线程互相拿着对方需要的锁，都在等，谁也走不了。

**四个必要条件（缺一不可同时成立才可能死锁）：**

1. 互斥  
2. 占有且等待  
3. 不可抢占  
4. 循环等待  

**破：** 统一加锁顺序、超时获取、减少锁粒度、死锁检测后回滚一方（DB 常见）。

### 面试怎么答

> 死锁是并发方循环等待资源。四条件：互斥、持有等待、不可抢占、环路。  
> 预防常用固定加锁顺序和超时；线上可用 jstack 看 Java 线程是否互相 BLOCKED；数据库 InnoDB 会检测死锁并回滚一方。

---

## 七、虚拟内存与缺页（P1）

### 白话

进程以为自己有连续大内存，其实是 **虚拟地址**；内核用页表映射到物理页。  
访问到没加载的页 → **缺页异常** → 内核从磁盘/交换区装入，慢。  
JVM 对象在进程虚拟地址空间里；容器还受 cgroup 内存限制，RSS 超了可能被 OOM Kill。

### 面试怎么答

> 虚拟内存通过页表把虚拟地址映射到物理内存，实现隔离和超卖。  
> 缺页会走磁盘，延迟高。  
> 调 JVM 堆时要同时看宿主机/容器 limit，堆外和 page cache 也占物理内存。

---

## 八、自测（不会就回面渣）

- [ ] 进程/线程 1 分钟 + 接到 Java 堆共享  
- [ ] 系统调用为何有成本  
- [ ] epoll 一句话 + 为何比 select 合适  
- [ ] 死锁四条件 + 一种预防  

**不会说 = 去 [OS面渣](./操作系统面渣级口述.md) 整章录音，不要只盯本页提纲。**

**使用说明：** [如何使用本仓库.md](./如何使用本仓库.md)

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 加厚：白话+可照着说+追问 |
'''

NET = r'''
# 计算机网络 · 知识点（精讲·可照着说）

<!-- NAV:START -->
> **导航** → [网络·面渣（主教材，已加长）](./计算机网络面渣级口述.md)
>
> ⚠️ 表看不懂就跳面渣；本页提供 **可直接念的答法**。
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md)
>
<!-- NAV:END -->

---

## 一、本专题要搞定什么

1. TCP/UDP 怎么选型  
2. **三次握手 / 四次挥手 / TIME_WAIT** 能讲清「为什么」  
3. HTTPS 一句话链路  
4. 常见状态码、TCP 粘包（写过 socket/RPC 会被问）

**主教材：** [计算机网络面渣级口述.md](./计算机网络面渣级口述.md)

---

## 二、TCP vs UDP（P0）

### 白话

- **TCP**：先建连，保证可靠、有序、有流量/拥塞控制。代价是延迟和状态开销。HTTP、MySQL、RPC 大多基于 TCP。  
- **UDP**：不建连、不保证送达与顺序，开销小、延迟低。DNS、很多音视频/实时游戏用 UDP（上层自己做可靠性或容忍丢包）。

**不要说「UDP 没用」**——场景不同。

### 面试怎么答（约 45 秒）

> TCP 面向连接、可靠有序，适合不能丢的业务数据；UDP 无连接、快，适合能容忍丢包或自己做可靠性的场景。  
> Web 业务、数据库、大多数 RPC 用 TCP；DNS 查询、直播推流等常见 UDP。选型看是否要可靠、延迟预算和实现复杂度。

---

## 三、三次握手（P0）

### 白话

双方都要确认：「我能发、你能收；你能发、我能收」。

```text
客户端 → SYN
服务端 → SYN + ACK
客户端 → ACK
然后进入 ESTABLISHED
```

**为何不是两次：** 若只有两次，旧的延迟 SYN 可能被当成新连接，浪费服务端资源；三次让客户端再确认一次，避免历史连接误建立。也确认了双方初始序列号。

### 面试怎么答（约 1 分钟）

> 三次握手是 TCP 建立连接：客户端发 SYN，服务端回 SYN+ACK，客户端再 ACK。  
> 目的是同步双方初始序列号，并确认双向通信能力。  
> 用三次而不是两次，可以降低历史重复 SYN 导致错误连接的风险，服务端也不会在未确认前就投入过多资源。  
> 面试若追问状态：客户端 SYN_SENT，服务端 LISTEN→SYN_RCVD→ESTABLISHED。

**完整故事版：** [网络面渣 · 握手](./计算机网络面渣级口述.md)

---

## 四、四次挥手与 TIME_WAIT（P0）

### 白话

TCP 全双工：每个方向都要单独关闭。

```text
主动方 FIN → 被动方 ACK（被动方可能还有数据要发）
被动方发完再 FIN → 主动方 ACK
```

所以常是 **四次**（中间两次 ACK/FIN 有时可合并成三次，但原理按四次讲更清楚）。

**TIME_WAIT：** 主动关闭方最后等 2MSL。  
作用：1）保证最后的 ACK 若丢了还能重传场景下对端能正确结束；2）让旧连接的包在网络里「过期」，不污染新连接。

**CLOSE_WAIT 很多：** 通常是 **应用没调用 close**，被动方已收到 FIN 但进程没关连接。

### 面试怎么答

> 四次挥手因为全双工要分别关闭两个方向。  
> TIME_WAIT 在主动关闭方，持续约 2MSL，保护最后 ACK 并清空旧报文。  
> 若服务器出现大量 CLOSE_WAIT，优先查应用是否泄漏连接、没正确关闭。  
> 大量 TIME_WAIT 在高并发短连接场景常见，可从连接复用、参数与架构上优化，不要盲改内核参数。

---

## 五、HTTPS（P0）

### 白话

HTTP 明文；HTTPS ≈ HTTP over TLS。  
证书（PKI）证明「你连的是真的目标站」；握手阶段用非对称密码学换出对称密钥，之后大数据用对称加密传（更快）。

### 面试怎么答

> HTTPS 在 HTTP 下加 TLS：证书校验服务器身份，握手协商出对称密钥，后续加密通信，防窃听和篡改。  
> 还能说明公钥加密换钥、对称加密传数据，避免全程非对称的性能问题。

---

## 六、状态码（P0 速答）

| 码 | 怎么说 |
|----|--------|
| 200 | 成功 |
| 301/302 | 永久/临时重定向 |
| 400 | 请求本身有问题 |
| **401** | **没认证**（未登录/Token 无效） |
| **403** | **认证了但没权限** |
| 404 | 资源不存在 |
| 500 | 服务端未处理异常 |
| **502** | 网关/反向代理上游异常 |
| **504** | 网关等上游超时 |

### 面试怎么答

> 401 和 403 最容易混：401 是身份没过，403 是身份有了但授权不够。  
> 502/504 常出在 Nginx 反代：502 上游挂了或协议错，504 是上游太慢超时。

---

## 七、TCP 粘包 / 拆包（P1）

### 白话

TCP 是 **字节流**，没有「消息边界」。你 write 两次，对端可能一次 read 到两段拼在一起（粘包），或一次只读到半段（拆包）。

**解法：** 应用层协议定边界——长度前缀、分隔符、定长消息。

### 面试怎么答

> TCP 保证字节可靠有序，但不保留消息边界，所以有粘包拆包。  
> 协议层用长度头或分隔符解决；HTTP 有 Content-Length/chunked，自己写 RPC 也要定帧。

---

## 八、自测

- [ ] TCP/UDP 选型 45 秒  
- [ ] 握手为何三次  
- [ ] TIME_WAIT 两点作用 + CLOSE_WAIT 原因  
- [ ] HTTPS 一句话  
- [ ] 401 vs 403，502 vs 504  

**卡壳就通读：** [计算机网络面渣级口述.md](./计算机网络面渣级口述.md)

**使用说明：** [如何使用本仓库.md](./如何使用本仓库.md)

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 加厚为可照着说的答法 |
'''

AGENT = r'''
# Agent · 知识点（精讲·可照着说）

<!-- NAV:START -->
> **导航** → [Agent·面渣（主教材）](./Agent面渣级口述.md)
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md) · [AI路径](./路径-AI工程.md)
>
<!-- NAV:END -->

---

## 一、本专题要搞定什么

面试官问 Agent，是在考：

1. 你是否分得清 **Chat / RAG / 工作流 / Agent**  
2. 能否讲清 **ReAct 循环** 与 **死循环怎么防**  
3. 工具调用的工程边界（权限、校验、HITL）  
4. 何时 **不该** 上 Agent（成本、可控性）

**主教材：** [Agent面渣级口述.md](./Agent面渣级口述.md)

---

## 二、Agent 是什么（P0）

### 白话

普通聊天：模型根据上下文 **生成一段回复**。  
**Agent**：有一个目标，模型会 **多步推理**，中间可以 **调用工具**（搜库、查 API、写文件、跑代码），观察结果后再决定下一步，直到完成或触发停止条件。

公式（面试够用）：

```text
Agent ≈ LLM（大脑）+ 规划 + 记忆 + 工具（手）+ 停止/护栏
```

### 面试怎么答（约 1 分钟，可照着说）

> 我认为 Agent 不是多聊几句，而是目标驱动的闭环：模型根据当前状态决定下一步，可以调用工具获取信息或执行动作，把观察结果写回上下文，再继续推理，直到达成目标或命中停止条件。  
> 工程上关键不只是 prompt，而是工具权限、最大步数、超时、审计和失败降级。  
> 和纯 Chat 比它能行动；和固定工作流比它更灵活，但更贵、更难控，所以要护栏。

---

## 三、Chat / RAG / 工作流 / Agent 对比（P0）

| 概念 | 白话 | 可控性 | 成本 |
|------|------|--------|------|
| Chat | 一问一答生成 | 高 | 低 |
| RAG | 先检索再生成，知识可更新 | 中高 | 中 |
| 工作流 | 步骤人写死（DAG） | **最高** | 中 |
| Agent | 步骤模型动态选 | 较低 | **高** |

### 面试怎么答

> RAG 解决的是「知识从哪来、怎么 grounding」；工作流是确定性编排；Agent 是把「下一步干什么」交给模型。  
> 生产里常混合：外层工作流控风险，内层局部允许 Agent 调工具；检索本身也可以是 Agent 的一个 tool。

### 追问：什么时候不用 Agent？

> 步骤固定、合规要求强、延迟/成本敏感、工具副作用大——优先工作流 + RAG，而不是开放式 Agent。

---

## 四、ReAct（P0）

### 白话

经典模式：

```text
Thought（推理下一步）
→ Action（选工具 + 参数）
→ Observation（工具返回）
→ 再 Thought …
→ Final Answer
```

问题：模型可能 **重复调用同一工具**、步数爆炸、工具报错后胡乱重试 → **死循环或账单爆炸**。

### 面试怎么答（约 1.5 分钟）

> ReAct 把推理和行动交错：先 Thought，再 Action，拿到 Observation 再继续。  
> 好处是可解释、能接入真实工具。风险是循环与成本。  
> 我会做：max_steps 硬限制、相同 Action 指纹去重、全局超时、工具错误次数熔断、危险写操作必须 Human-in-the-loop、每步 trace 落日志方便复盘。  
> 达不成目标就降级：返回已收集证据、或转人工，而不是无限转。

### 防死循环清单（背这 5 条）

1. `max_steps`  
2. 重复 Action 检测  
3. 超时  
4. 工具错误熔断  
5. 危险操作审批（HITL）

**完整稿：** [Agent面渣 · ReAct](./Agent面渣级口述.md)

---

## 五、工具调用（Function Calling）（P0）

### 白话

1. 你用 JSON Schema 描述工具（名字、参数、含义）  
2. 模型 **输出结构化调用意图**（不是它真的碰到了你的数据库）  
3. **你的后端** 校验参数、检查权限、真正执行  
4. 把结果作为 Observation 塞回模型  

**安全原则：** 永不信任模型填的「我有权限」；写操作默认要审批；参数白名单；超时与重试策略由你定。

### 面试怎么答

> Function Calling 是模型生成调用计划，执行权在服务端。  
> 我会强调 schema 清晰、参数校验、鉴权在服务端、副作用工具分级、全程审计日志。  
> 工具描述含糊会导致乱调；返回结果过长要摘要，避免撑爆上下文。

---

## 六、记忆（P1）

| 类型 | 白话 | 注意 |
|------|------|------|
| 短时 | 当前对话窗口 | 有 token 上限 |
| 长时 | 向量库/画像/DB | 隐私与过期 |
| 工作记忆 | 本轮 Agent 中间状态 | 步数多要压缩 |

过长历史：**摘要** 后再塞，不要无限 append。

### 面试怎么答

> 短时靠上下文，长时靠外部存储检索。  
> 工程上要做摘要、检索过滤和权限隔离，防止把 A 用户记忆检索给 B。

---

## 七、评测与可观测（P1，加分）

- 任务成功率、平均步数、工具失败率、人工接管率、单价/次  
- trace：每步 Thought/Action/Observation 可回放  
- 回归集：固定任务集防「改 prompt 越改越坏」

---

## 八、自测

- [ ] Agent 定义 30～60 秒  
- [ ] 四者对比表能口述  
- [ ] ReAct + 五条防死循环  
- [ ] 工具安全三原则  
- [ ] 举一个「不该用 Agent」的例子  

**主教材通读：** [Agent面渣级口述.md](./Agent面渣级口述.md)

**使用说明：** [如何使用本仓库.md](./如何使用本仓库.md)

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 加厚为可照着说 |
'''

REDIS = r'''
# Redis · 知识点（精讲·可照着说）

<!-- NAV:START -->
> **导航** → [Redis·面渣（主教材）](./Redis面渣级口述.md)
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md) · [Java路径](./路径-Java后端.md)
>
<!-- NAV:END -->

---

## 一、本专题要搞定什么

1. 为什么快 + 五类型场景  
2. **穿透 / 击穿 / 雪崩** 三者能分开说  
3. 分布式锁 **SET NX EX + Lua 删**  
4. 缓存与 DB 一致性常见说法（Cache Aside）

**主教材：** [Redis面渣级口述.md](./Redis面渣级口述.md)

---

## 二、为什么快 + 数据类型（P0）

### 白话

1. **内存**计算，比磁盘 DB 快几个数量级  
2. **命令执行单线程**（逻辑简单，无锁竞争；Redis 6+ 多线程主要加速网络读写，别说成「多线程随便并行执行命令」）  
3. 高效编码与数据结构  
4. **IO 多路复用** 处理大量连接  

| 类型 | 典型场景 |
|------|----------|
| String | 缓存对象 JSON、计数器、Session、分布式锁 |
| Hash | 对象多字段（用户资料局部更新） |
| List | 最新消息列表、简单队列 |
| Set | 去重、共同关注 |
| ZSet | **排行榜**、延时任务（score 当时间） |

### 面试怎么答（约 1 分钟）

> Redis 快主要因为内存、单线程执行命令减少锁、高效数据结构和 IO 多路复用。  
> 五类型我会各举场景：String 做缓存和锁，Hash 做字段级对象，List 做动态列表，Set 做关系去重，ZSet 做排行榜。  
> 选型先看访问模式，不要所有东西都塞 String。

---

## 三、缓存三连（P0，最易混）

必须先 **定义差异**，再讲方案——混成一套话会被追问穿。

| 问题 | 一句话定义 | 核心方案 |
|------|------------|----------|
| **穿透** | 查 **根本不存在** 的数据，缓存不中，每次打到 DB | 缓存空值、布隆过滤器、参数校验 |
| **击穿** | **热点 key** 过期瞬间，大量并发重建 | 互斥锁重建、逻辑过期、永不过期+异步刷新 |
| **雪崩** | **大量 key 同时过期** 或 Redis 整体不可用 | TTL 加随机抖动、集群高可用、限流降级、多级缓存 |

### 面试怎么答（约 2 分钟，建议背结构）

> 我先区分三个概念。  
> 穿透是恶意或乱查不存在的 key，缓存永远帮不上忙，用布隆或空值缓存挡住。  
> 击穿是单个热点突然失效，用互斥只让一个线程回源，或逻辑过期避免真的删掉。  
> 雪崩是大面积一起失效或 Redis 挂了，TTL 打散，加上集群和本地缓存、接口限流。  
> 三者方案不能混用一套词，面试官会故意追问差异。

**完整长答：** [Redis面渣 · 三连](./Redis面渣级口述.md)

### 追问

| 问 | 答 |
|----|-----|
| 空值缓存会占内存？ | 短 TTL；布隆更省但有误判 |
| 逻辑过期是什么？ | value 里带过期时间，过期仍返回旧值并异步重建 |

---

## 四、分布式锁（P0）

### 白话

多机部署时，JVM 的 `synchronized` 只锁本机。要用 Redis：

```text
SET lock_key unique_value NX EX 30
```

- **NX**：不存在才成功 → 互斥  
- **EX**：超时自动释放 → 防死锁（持锁进程挂了）  
- **unique_value**：删除时确认是自己的锁，避免误删别人的  
- 删除用 **Lua**：`if get==me then del` 保证原子  

业务执行时间可能超过 EX → 需要 **续期**（Redisson 看门狗）。  
Redis 主从异步复制时，主挂可能丢锁 → 讲清业务能否接受；强需求可看 RedLock 争议或 ZooKeeper/etcd。

### 面试怎么答（约 1.5 分钟）

> 分布式锁我用 SET key value NX EX，保证互斥和超时。  
> value 用唯一 ID，释放时用 Lua 校验再删，防止超时后误删别人的锁。  
> 长任务要续期。  
> 同时说明：锁超时要大于业务耗时或可续期；Redis 故障转移在极端情况下可能有安全隐患，金融级要评估。

**完整稿：** [Redis面渣 · 锁](./Redis面渣级口述.md)

---

## 五、缓存与 DB 一致性（P1）

### 白话

常见 **Cache Aside**：

1. 读：先缓存，没有再 DB，再回填缓存  
2. 写：**先更新 DB，再删除缓存**（不是先改缓存）

删缓存失败 → 重试、或基于 binlog 的异步删除补偿。  
绝对强一致很难，互联网业务多接受 **最终一致**，用 TTL 兜底。

### 面试怎么答

> 读用旁路缓存；写先 DB 再删缓存，避免先改缓存导致脏数据更难修。  
> 删失败要补偿。强一致要上分布式事务或换架构，成本和复杂度高。

### 追问：为何不先更新缓存？

> 并发下更容易出现「缓存新、DB 旧」或写覆盖顺序问题；删缓存让下次读回源更稳妥（仍非绝对完美，但是主流实践）。

---

## 六、自测

- [ ] 为什么快四点  
- [ ] 三连定义不混  
- [ ] 锁：NX EX + 唯一 value + Lua  
- [ ] Cache Aside 写路径  

**主教材：** [Redis面渣级口述.md](./Redis面渣级口述.md)

**使用说明：** [如何使用本仓库.md](./如何使用本仓库.md)

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 加厚可照着说 |
'''

MYSQL = r'''
# MySQL · 知识点（精讲·可照着说）

<!-- NAV:START -->
> **导航** → [MySQL·面渣（主教材）](./MySQL面渣级口述.md)
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md) · [Java路径](./路径-Java后端.md)
>
<!-- NAV:END -->

---

## 一、本专题要搞定什么

1. 索引为什么快、聚簇/回表/最左/失效  
2. 事务隔离 + MVCC 人话版  
3. 慢 SQL 怎么定位怎么改  

**主教材：** [MySQL面渣级口述.md](./MySQL面渣级口述.md)

---

## 二、索引（P0）

### 白话

没索引 = 一本书没有目录，找人名只能从头翻到尾（**全表扫描**）。  
InnoDB 索引是 **B+ 树**：多层目录，叶子层串联，范围查询友好。

- **主键索引（聚簇）**：叶子节点就是 **整行数据**。  
- **二级索引**：叶子存 **主键值**；查完二级往往还要拿主键再回聚簇查一次 → **回表**。  
- **覆盖索引**：查询列都在二级索引里，不必回表，更快。  
- **联合索引 (a,b,c)**：从左往右用，叫 **最左前缀**；只写 `b=?` 通常用不上这个索引。

**常见失效：** 对索引列包函数、`LIKE '%xx'`、隐式类型转换、优化器判断全表更便宜。最终以 `EXPLAIN` 为准。

### 面试怎么答（约 2 分钟）

> InnoDB 用 B+ 树做索引。主键是聚簇索引，叶子就是行数据；二级索引叶子存主键，需要时回表。  
> 联合索引遵守最左前缀；能覆盖查询就少回表。  
> 建索引看区分度和查询条件，不是越多越好，写放大和空间都要代价。  
> 我会用 EXPLAIN 看 type、key、rows、Extra，确认有没有用上索引、是否 Using filesort/temporary。

### 追问

| 问 | 答 |
|----|-----|
| 为何不用二叉树？ | 太高，磁盘 IO 次数多；B+ 更矮更胖 |
| 索引越多越好？ | 否，占空间拖慢写 |
| 主键为何推荐自增？ | 顺序插入减少页分裂（也有业务用 UUID 的权衡） |

**完整稿：** [MySQL面渣 · 索引](./MySQL面渣级口述.md)

---

## 三、事务、隔离级别、MVCC（P0）

### 白话

**事务**：一组操作要么全成功要么全回滚。ACID：原子、一致、隔离、持久。

三种读现象：

| 现象 | 白话 |
|------|------|
| 脏读 | 读到别人 **未提交** 的修改 |
| 不可重复读 | 同一事务两次读同一行，中间被别人 **改了** |
| 幻读 | 范围查询行数变了（别人插入/删除） |

隔离级别从低到高大致：读未提交 → 读已提交（RC）→ 可重复读（RR）→ 串行化。  
InnoDB **默认 RR**。

**MVCC（人话）：** 一行有多个版本（undo 链）。普通 SELECT 是 **快照读**，按事务的 ReadView 决定能看见哪个版本，一般 **不加锁**。  
`SELECT ... FOR UPDATE` 是 **当前读**，读最新并加锁。

### 面试怎么答（约 2 分钟）

> 事务保证一组写操作的原子和持久。隔离级别权衡并发和异常读。  
> MySQL InnoDB 默认可重复读。  
> 实现上靠 MVCC：行版本链加 ReadView，快照读不加锁提高并发；当前读才加锁。  
> 脏读/不可重复读/幻读我会各举一句例子说明差异。RR 下间隙锁等机制用来缓解幻读，细节可再展开。

**完整稿：** [MySQL面渣 · 事务](./MySQL面渣级口述.md)

---

## 四、锁与日志（P1）

### 白话要点

- InnoDB **行锁** 本质加在索引上；条件没走索引可能锁更多行甚至退化。  
- 死锁：互相等锁，InnoDB 检测后 **回滚成本小的一方**。  
- **redo log**：崩溃恢复，保证已提交不丢（WAL 思想）。  
- **undo log**：回滚 + MVCC 版本。  
- **binlog**：主从复制、数据恢复。  
- 事务提交涉及 redo 与 binlog 的一致（两阶段提交思想）。

### 面试怎么答

> 行锁提高并发，但 SQL 要走对索引。  
> redo 保持久，undo 保回滚和多版本，binlog 保复制。  
> 面试若问崩溃恢复，我会从 WAL 和 redo 讲起。

---

## 五、慢 SQL 排查（P0 工程向）

### 可照着说的四步

```text
1. 慢查询日志 / 监控找到慢 SQL
2. EXPLAIN 看是否全表、用的哪个索引、扫描行数
3. 改：加合适索引、改写 SQL、避免选*、处理深分页
4. 在预发验证耗时与执行计划，再上线
```

深分页：`LIMIT 1000000,10` 很痛 → 用 `id > last_id` 或延迟关联。

### 面试怎么答

> 先量化慢在哪，再 EXPLAIN，再改索引或 SQL，最后用数据验证。  
> 不会一上来就「加个索引」而不看执行计划。

---

## 六、自测

- [ ] B+、聚簇、回表、最左、覆盖  
- [ ] 三种读现象 + 默认隔离级别  
- [ ] MVCC 一分钟  
- [ ] 慢 SQL 四步  

**主教材：** [MySQL面渣级口述.md](./MySQL面渣级口述.md)

**使用说明：** [如何使用本仓库.md](./如何使用本仓库.md)

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 加厚可照着说 |
'''

LLM = r'''
# LLM 基础常识 · 知识点（精讲·可照着说）

<!-- NAV:START -->
> **导航** → [LLM基础·面渣（主教材）](./LLM基础常识面渣级口述.md)
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md) · [AI路径](./路径-AI工程.md)
>
<!-- NAV:END -->

---

## 一、本专题要搞定什么

AI 工程岗开场题，不考你推 Transformer 公式，考：

1. **Token / 上下文窗口** 是否懂计费与长度  
2. **幻觉** 是否当成工程风险而不是玄学  
3. 温度等参数什么时候调  
4. 预训练 / SFT / 偏好对齐 各干什么  
5. 云 API vs 私有化怎么选型  

**主教材：** [LLM基础常识面渣级口述.md](./LLM基础常识面渣级口述.md)

---

## 二、Token 与上下文窗口（P0）

### 白话

模型不直接「按字」思考，而是把文本切成 **token**（常见是子词）。  
中文往往比英文 **更吃 token**（同样一段话 token 数更多）。  
API 通常按 **输入 token + 输出 token** 计费。  

**上下文窗口**：一次请求里模型最多能「看见」的长度，包括系统提示、历史对话、RAG 检索片段、用户问题，还要 **预留输出空间**。  
满了不是越拼越好，而是要：摘要历史、少而精检索、截断旧轮。

### 面试怎么答（约 1 分钟）

> Token 是模型的基本文本单位，也是计费和长度限制单位。中文通常更耗 token。  
> 上下文窗口是一次能塞下的上限，系统提示、历史、检索结果、问题都占窗口。  
> 超窗时我会做历史摘要、控制检索条数和单段长度，而不是无限拼接。  
> 工程上还会监控每请求 token，做成本与延迟预算。

### 追问

| 问 | 答 |
|----|-----|
| 和字符数关系？ | 不是 1:1，和分词器有关 |
| TTFT？ | Time To First Token，首 token 延迟，流式体验关键 |

---

## 三、温度与采样（P0）

### 白话

生成下一个 token 时带随机性。  

- **温度低（如 0～0.3）**：更确定，适合抽字段、分类、客服标准答、要稳定的工具参数。  
- **温度高**：更发散，适合 brainstorm，但更不稳定、更易跑飞。  

事实性、结构化输出：**偏低**。创意文案：可以略高。

### 面试怎么答

> 温度控制随机性。生产里事实与结构化任务我倾向低温；需要多样性再升高。  
> 还会配合 top_p 等，但面试先把温度场景说清即可。

---

## 四、幻觉（P0，必会）

### 白话

**幻觉**：说得流畅、语气很像真的，但内容是错的或捏造的。  
根因：模型本质是 **根据概率续写**，不是实时查权威数据库。它「会说话」≠「知道真相」。

**工程应对（不要只说换模型）：**

1. **RAG grounding**：让回答基于检索到的材料并尽量引用  
2. 低相关 **拒答** 或「材料不足」  
3. 结构化校验、规则、二次检查  
4. **评测集** 回归，而不是手测两句  

### 面试怎么答（约 1 分钟）

> 幻觉是大模型生成式特性，不是偶发 bug。  
> 业务上我会用检索增强把答案锚定到可引用材料，相关度低就拒答，关键字段做校验，并用金标集持续评测。  
> 不会承诺「某个模型永不胡说」，而是把风险压到业务可接受。

**完整稿：** [LLM面渣](./LLM基础常识面渣级口述.md)

---

## 五、预训练 / SFT / RLHF（P0）

| 阶段 | 白话 | 解决什么 |
|------|------|----------|
| 预训练 | 海量文本学语言与知识 | 「会说话、有知识」 |
| SFT | 指令-回答数据微调 | 「会按人的要求答」 |
| RLHF/偏好对齐 | 人类/AI 偏好优化 | 更有用、更安全、更听话 |

业务落地：

- 知识常变、要可引用 → **优先 RAG**  
- 语气/格式/领域任务形态 → Prompt 或 SFT  
- 安全与偏好 → 对齐 + 策略层  

### 面试怎么答

> 预训练打底，SFT 教会遵循指令，偏好对齐让表现更符合人类期望与安全。  
> 业务知识更新快时我优先 RAG；只有稳定风格或特殊格式成本合适才考虑微调。

---

## 六、云 API vs 私有化（P1）

| | 云 API | 私有化/本地 |
|--|--------|-------------|
| 优点 | 快上线、省运维、模型新 | 数据可控、可定制 |
| 代价 | 数据出域、单价、依赖供应商 | GPU、运维、效果迭代 |

常见：**网关统一**，敏感走私有，一般走云；都要超时、限流、降级、审计。

### 面试怎么答

> 看数据敏感度、调用量、延迟与团队运维能力。  
> 很多团队混合部署，用统一 LLM 网关屏蔽差异。

---

## 七、自测

- [ ] token/窗口 1 分钟  
- [ ] 幻觉定义 + 3 条工程手段  
- [ ] 温度场景  
- [ ] 三阶段训练各一句  
- [ ] 云 vs 私有选型因素  

**主教材：** [LLM基础常识面渣级口述.md](./LLM基础常识面渣级口述.md)  
若口述仍偏短，以本页「可照着说」+ 面渣一起练。

**使用说明：** [如何使用本仓库.md](./如何使用本仓库.md)

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 加厚可照着说 |
'''

PYTHON = r'''
# Python · 知识点（精讲·可照着说）

<!-- NAV:START -->
> **导航** → [Python·面渣](./Python面渣级口述.md) · [异步FastAPI](./Python异步与FastAPI面渣级口述.md)
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md) · [Python路径](./路径-Python.md)
>
<!-- NAV:END -->

---

## 一、本专题要搞定什么

1. 可变/不可变、**默认参数陷阱**  
2. 装饰器 / 生成器 / with 能讲用途  
3. **GIL** 下线程/进程/asyncio 怎么选（AI 服务必问）  
4. 能衔接到 FastAPI 异步注意点  

**主教材：** [Python面渣级口述.md](./Python面渣级口述.md)  
**服务深挖：** [Python异步与FastAPI面渣级口述.md](./Python异步与FastAPI面渣级口述.md)

---

## 二、可变与默认参数（P0）

### 白话

- **不可变**：int、str、tuple、frozenset… 改值通常产生新对象。  
- **可变**：list、dict、set… 可以原地改。  

**坑：** `def f(a=[])` 里的 `[]` 在 **函数定义时创建一次**，所有调用默认共享同一个 list，会串数据。

**正确写法：**

```python
def f(a=None):
    if a is None:
        a = []
```

浅拷贝：`copy.copy` / 切片，只复制外层；深拷贝：`copy.deepcopy` 递归复制。

### 面试怎么答（约 45 秒）

> Python 里 list、dict 可变，str、tuple 不可变。  
> 默认参数如果是可变对象，定义时只创建一次，多次调用会共享，属于常见坑，应该用 None 再在函数体内初始化。  
> 拷贝要区分浅拷贝和深拷贝，嵌套结构用深拷贝才独立。

---

## 三、装饰器 / 生成器 / with（P0）

### 白话

**装饰器：** 接收函数、返回函数的高阶用法，`@dec` 等价于 `f = dec(f)`。用途：日志、鉴权、计时、重试、缓存。用 `functools.wraps` 保留原函数元数据，避免调试时名字全变成 wrapper。

**生成器：** 函数里 `yield`，调用得到迭代器，**惰性**产出。读大文件逐行、流水线处理时省内存。

**with：** 上下文管理器，保证 `__enter__`/`__exit__` 成对，异常也会清理（关文件、放锁、还连接）。

### 面试怎么答

> 装饰器用来无侵入地增强函数；生成器用 yield 做惰性序列省内存；with 用上下文协议保证资源释放。  
> 手写装饰器我会提到 wraps；手写 with 可以提 contextmanager 或类实现。

**手写练习：** [Python手写题.md](./Python手写题.md)

---

## 四、GIL 与并发选型（P0，最重要）

### 白话

CPython 有 **全球解释器锁 GIL**：同一个进程里，**同一时刻通常只有一个线程在执行 Python 字节码**。  

因此：

| 负载类型 | 更合适 | 原因 |
|----------|--------|------|
| CPU 密集（重计算） | **多进程** / 推到 C 扩展 / 外部服务 | 多线程跑不满多核 |
| IO 密集（等网络、磁盘） | **多线程** 或 **asyncio** | 等待时会释放 GIL 或让出事件循环 |
| 高并发等 LLM/HTTP | **asyncio** + 异步客户端 | 单线程撑大量等待连接 |

**asyncio 大忌：** 在协程里写 `time.sleep`、同步 `requests`、重 CPU 循环 → **堵住事件循环**，所有请求一起卡。应使用 `asyncio.sleep`、httpx/aiohttp 异步库，CPU 重活丢进程池。

### 面试怎么答（约 1.5～2 分钟，可照着说）

> CPython 的 GIL 导致多线程做纯 Python CPU 计算很难吃满多核，CPU 密集我会用多进程或把计算卸载出去。  
> IO 密集时线程仍有价值，因为等待 IO 会释放 GIL。  
> 做 AI 网关、高并发调模型 API 时我优先 asyncio：用协程在单线程里交错等待大量网络 IO。  
> 但必须保证协程路径上没有阻塞调用，否则事件循环被堵，延迟会雪崩。  
> FastAPI 很适合这类服务：类型注解、async 路由、自动 OpenAPI；还要自己做超时、限流、连接池和观测。

**完整稿：** [Python面渣 · GIL](./Python面渣级口述.md) · [FastAPI 面渣](./Python异步与FastAPI面渣级口述.md)

### 追问

| 问 | 答 |
|----|-----|
| 为什么 Redis 也说单线程还快？ | 命令执行模型不同；这里强调的是 Python 字节码 GIL |
| 多进程代价？ | 内存复制/通信贵，要控制进程数 |
| FastAPI 同步 def？ | 会丢线程池跑，不适合想当然高并发阻塞逻辑 |

---

## 五、自测

- [ ] 默认参数陷阱能举反例  
- [ ] 装饰器用途 + wraps  
- [ ] GIL 下三类负载选型  
- [ ] async 里不能阻塞的原因 + 替代  

**使用说明：** [如何使用本仓库.md](./如何使用本仓库.md)

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 加厚可照着说 |
'''

HOW = r'''
# 如何使用本仓库（必读）

很多同学反馈：

> **知识点太简单 / 太干 / 看不懂 / 不知道面试怎么说。**

这不是你「笨」，是两种文档分工不同。按下面用，就不会晕。

---

## 1. 两种文档分别干什么

| 类型 | 文件名 | 作用 | 单独看够不够 |
|------|--------|------|--------------|
| **面渣口述** | `…面渣级口述.md` | **主教材**：完整 2～4 分钟口语稿 | **够** |
| **知识点** | `…知识点.md` | 地图 + 白话 + **可照着说的短答** + 追问 | 可练结构，**仍建议配面渣** |
| **路径表** | `路径-….md` | 先学什么后学什么 | 导航 |

**结论：**

- 想学会 **怎么开口** → **先面渣**（长文通读 + 录音）  
- 想快速过结构、查漏 → 再看知识点  
- 知识点里已尽量写了「面试怎么答（可照着说）」；若仍觉得短，**跳面渣不要硬啃表**

---

## 2. 推荐读法（每天一套）

```text
① 打开路径表，找到今天一步
② 打开「面渣」全文通读（可先听自己读）
③ 对着稿说 2 分钟 → 遮稿再说一遍
④ 打开「知识点」：只看自己卡壳的节
⑤ 做追问三连 / 自测清单
```

**错误用法（最常见）：**

```text
只刷知识点表格关键词 → 面试一开口只能甩名词 → 被追问就沉默
```

**正确心理预期：**

| 你的感觉 | 实际含义 | 动作 |
|----------|----------|------|
| 知识点太简单 | 它本来是提纲 + 短答 | 去读同名 **面渣** |
| 看不懂某张表 | 缺语境 | 面渣对应章节建立感觉 |
| 不知道怎么说 | 还没练口语 | 照「面试怎么答」念，再遮稿 |
| 面渣太长 | 正常 | 按路径 P0 先拿下，别一次通宵 |

---

## 3. 知识点正文怎么读

每个核心考点尽量四段：

1. **白话** — 用人话建立直觉  
2. **面试怎么答** — **可直接照着念** 的 45 秒～2 分钟结构  
3. **追问** — 常被打穿的点  
4. **完整口语稿** — 链到面渣长文  

练习标准：

| 程度 | 标准 |
|------|------|
| 不及格 | 只会甩 HashMap、GC、Agent 等词 |
| 及格 | 能按「面试怎么答」说满 1～2 分钟，偶看稿 |
| 良好 | 不看稿说清 + 接住 2～3 个追问 |
| 优秀 | 能接到自己项目 STAR 举例 |

---

## 4. 三条路径入口

| 路径 | 链接 |
|------|------|
| AI 工程 | [路径-AI工程.md](./路径-AI工程.md) |
| Java 后端 | [路径-Java后端.md](./路径-Java后端.md) |
| Python | [路径-Python.md](./路径-Python.md) |

路径表 **左边「主教材 / 面渣」优先点**，右边知识点第二。

---

## 5. 仍觉得某个专题「太薄」

可能原因：

1. 你打开的是知识点不是面渣  
2. 该专题面渣本身偏短（部分 AI 周边专题）— 以知识点「可照着说」+ 追问补练  
3. 需要结合项目 — 看 [STAR 范例](./项目STAR范例-AI.md) / [Java STAR](./项目STAR范例-Java.md)

可以点名专题继续加厚（例如：「只要 Redis 再加长」「Agent 再多两个场景」）。

---

## 6. 一句话记住

> **面渣练嘴，知识点理结构；不会说时先面渣，不要只背表。**

[← 回首页](./README.md)
'''

def main():
    files = {
        "操作系统高频面试题与知识点.md": OS,
        "计算机网络高频面试题与知识点.md": NET,
        "Agent高频面试题与知识点.md": AGENT,
        "Redis高频面试题与知识点.md": REDIS,
        "MySQL高频面试题与知识点.md": MYSQL,
        "LLM基础常识高频面试题与知识点.md": LLM,
        "Python高频面试题与知识点.md": PYTHON,
        "如何使用本仓库.md": HOW,
    }
    for name, text in files.items():
        w(name, text)


if __name__ == "__main__":
    main()
