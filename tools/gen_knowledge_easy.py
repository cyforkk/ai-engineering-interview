# -*- coding: utf-8 -*-
"""知识点：大白话易懂版。面渣不动。卡片可保留。"""
from pathlib import Path

DOCS = Path(__file__).resolve().parents[1] / "docs"


def w(name, text):
    p = DOCS / name
    p.write_text(text.strip() + "\n", encoding="utf-8")
    print("ok", name, p.stat().st_size)


def nav(oral, card, path):
    return f"""<!-- NAV:START -->
> 📖 **本文用大白话讲懂** · 🗣️ [练嘴·面渣](./{oral}) · 🃏 [卡片速记](./{card})
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md) · [路径](./{path})
>
<!-- NAV:END -->

> 读不懂术语？先看 **生活例子**。会说了再去面渣练开口。
"""


JAVA = f"""# Java 基础 · 大白话详解

{nav('Java面渣级口述.md','Java卡片速记.md','路径-Java后端.md')}

---

## 这个专题在问啥？

就三件事，搞懂就够用：

1. 两个对象怎么算「一样」？  
2. 字符串为啥改不了？  
3. **HashMap 怎么存、怎么找**（必问）

---

## 1. == 和 equals：到底比什么？

### 先记一句

- `==`：基本类型比**数一不一样**；对象比**是不是同一个东西**。  
- `equals`：可以改成比**内容一不一样**（比如两个用户 id 相同就算同一个人）。

### 生活例子

- 你和你的双胞胎：**两个人**（`==` 是 false），但身份证号可以做成「业务上相等」（`equals` true）。  
- 同一个水杯你左手递右手：还是**那一个杯子**（`==` true）。

### 慢慢讲

Java 里每个对象默认的 `equals`，其实也是比「是不是同一个」。  
String、Integer 这些类**重写了** equals，所以 `"hello".equals("hello")` 是 true，即使可能不是同一个对象。

### hashCode 是干嘛的？

可以想成：**快递柜编号**。  
HashMap 先看编号进哪个柜子，再在柜子里用 equals 细看是不是你的快递。

### 必须遵守的规矩（很重要）

> 如果两个对象 equals 说「一样」，那它们的 hashCode **必须一样**。

**为啥？**  
不然 HashMap 把东西放进 3 号柜，下次 equals 认为一样却去 8 号柜找——**永远找不到**。  
这不是风格问题，是会出 bug。

反过来：hashCode 一样，不一定 equals 一样（两个人可能碰巧同一柜号，再细比）。

### 易错

| 情况 | 结果 |
|------|------|
| 只改 equals 不改 hashCode | HashMap/HashSet 行为错乱 |
| 用会变的字段当 key，后来又改了 | 放进去取不出来 |

---

## 2. String 为啥不能改？

### 先记一句

字符串一旦创建，**里面的字就改不了**。你以为的修改，其实是**造了一个新的**。

### 生活例子

石碑刻了字（不可变）。你要改字，只能再刻一块新碑，旧的还在。  
黑板（可变）可以擦了重写——String 不是黑板。

### 为啥要设计成这样？

1. **能共用**：很多地方都写 `"OK"`，可以共用同一份，省内存。要是能改，一处改了所有人都遭殃。  
2. **当钥匙安全**：经常拿 String 当 HashMap 的 key，内容不变才能放心记住 hash。  
3. **更安全**：密码、类名、路径不会被别人半路改掉。

### 拼接怎么写？

循环里老用 `+` 会不停造新对象。  
要用 **StringBuilder**（像可擦写的草稿本，写完再变成 String）。

---

## 3. HashMap：最重要的一块

### 先记一句

HashMap = **一排抽屉（数组）** + 抽屉里可能挂**一条链**，链太长就改成**树**，好找东西。

### 生活例子

图书馆：

1. 先按书名算一个「架号」（hash）  
2. 走到那个书架  
3. 书架上可能有好几本，再一本本比对书名（equals）  
4. 某一格书多到爆炸，就整理成更好找的目录树（红黑树）

### 放进去（put）在干嘛？

1. 用 key 算出 hash  
2. 换算成数组下标（哪个抽屉）  
3. 抽屉空 → 直接放  
4. 不空 → 看有没有相同 key：有就**改 value**，没有就**挂到后面**  
5. 东西太多 → **扩容**（抽屉变多，重新摆）

### 为啥长度经常是 2、4、8、16…（2 的幂）？

这样可以用位运算快速算「进几号抽屉」，又快又均匀。你面试说到「2 的幂 + 位运算」就够用。

### 链多长会变树？

大概：**一条链上超过 8 个**，并且数组已经够大（≥64），才变红黑树。  
太短就急着变树不划算；太短时更可能先**扩容**把人分散到更多抽屉。

### 为啥说它线程不安全？

多个人同时往同一个 HashMap 里塞东西，可能互相覆盖、数据乱。  
多线程请用 **ConcurrentHashMap**。

### 默认 16 和 0.75 是啥？

- 一开始 16 个抽屉  
- 装到大约 75% 就扩容成两倍，免得抽屉太挤、链太长

---

## 4. ArrayList 一句话

底层是**可扩容数组**：按下标拿很快；中间插入要挪位置。  
日常「列表」十有八九用它。

遍历时如果一边遍历一边乱改结构，可能直接报错（fail-fast），这是故意早发现错误。

---

## 学完自测（用人话答）

1. equals 为 true 为啥 hashCode 必须一样？  
2. String 不可变三个好处？  
3. HashMap 用抽屉+链，put 大概几步？  
4. 多线程为啥不能共用普通 HashMap？

**练嘴：** [Java面渣级口述.md](./Java面渣级口述.md)  
**背要点：** [Java卡片速记.md](./Java卡片速记.md)
"""

REDIS = f"""# Redis · 大白话详解

{nav('Redis面渣级口述.md','Redis卡片速记.md','路径-Java后端.md')}

---

## 这个专题在问啥？

1. Redis 为啥快？常见五种类型干啥用？  
2. **穿透 / 击穿 / 雪崩**——三个词别混  
3. 分布式锁怎么加才靠谱？  
4. 缓存和数据库谁先谁后？

---

## 1. Redis 是啥？为啥快？

### 先记一句

Redis 是**内存里的超快小仓库**，常放「热点数据」，减轻数据库压力。

### 生活例子

- 数据库 = 大仓库（全、稳、慢一点）  
- Redis = 收银台旁边的**常卖货小货架**（近、快，但只放一部分）

### 为啥快？（四点就够）

1. **在内存里干活**——比读写磁盘快很多  
2. **执行命令基本单线程**——少抢锁、逻辑简单  
3. 数据结构自己做得很高效  
4. 一个线程可以盯很多网络连接（多路复用）

注意：别说「Redis 命令随便多线程并行跑」。网络收发可以多线程优化，**命令执行**你仍按单线程理解最稳。

### 五种类型记场景

| 类型 | 你可以把它想成 | 典型用途 |
|------|----------------|----------|
| String | 一个标签贴一句话 | 缓存、计数、锁 |
| Hash | 一张小表格 | 用户资料多个字段 |
| List | 一队人 | 最新消息列表 |
| Set | 不重复的一堆 | 点赞人、去重 |
| ZSet | 带分数的排名 | **排行榜**、按时间排序 |

---

## 2. 缓存三连：最容易混，用故事分开

### 穿透：查一个「根本没有的人」

有人拿假身份证号狂查。  
缓存没有（从来就没有）→ 每次都打到数据库 → 数据库被拖垮。

**怎么办？**

- 把「查无」也缓存一小会儿（空值缓存）  
- 或门口放个「大概有没有」的快速检查（布隆过滤器）  
- 参数先校验（id 明显非法直接拒）

### 击穿：超级网红商品，标签刚好过期

这个商品**其实有**，但缓存刚好过期那一秒，所有人一起冲进仓库进货 → 仓库炸。

**怎么办？**

- 同一时间只允许一个人去数据库重建缓存，别人等一会儿  
- 或「逻辑过期」：过期了先把旧的给你，后台偷偷更新

### 雪崩：很多标签同一时间掉光，或小货架塌了

一大片缓存一起失效，或 Redis 挂了，所有流量砸向数据库。

**怎么办？**

- 过期时间加一点随机，别整齐划一到期  
- Redis 高可用、限流、本地再挡一层  

### 三个差在哪？（考试就考这个）

| | 查的东西 | 像什么 |
|--|----------|--------|
| 穿透 | **从来不存在** | 假身份证狂刷 |
| 击穿 | **热点突然过期** | 网红商品标签掉了 |
| 雪崩 | **一大片一起挂** | 货架集体空了/倒了 |

---

## 3. 分布式锁：为啥要那么多参数？

### 先记一句

多台机器时，Java 的 `synchronized` 只锁**自己那台**。要用 Redis 当「公共厕所的门锁」。

### 正确姿势（记住命令形态）

```text
SET lock_key 随机唯一值 NX EX 30秒
```

| 参数 | 白话 | 没有会怎样 |
|------|------|------------|
| NX | 没有锁才加成功 | 大家一起进门 |
| EX | 最多锁多久 | 人晕倒了门永远锁死 |
| 随机唯一值 | 证明锁是我的 | 我超时后误删别人的锁 |
| 用 Lua 删锁 | 先确认是我的再删，一步完成 | 中间被插队，误删 |

业务特别长：要**续命**（锁快过期再续一段时间），否则锁没了别人进来。

---

## 4. 和数据库谁先谁后？

### 常见写法（Cache Aside）

1. **读：** 先看 Redis，没有再查库，再放回 Redis  
2. **写：** **先改数据库，再删 Redis**（下次读会重新加载）

### 为啥不先改缓存？

数据库才是「账本真相」。先改缓存、库没改成功，就会长期是假数据。  
删缓存更简单：当缓存暂时失效，下次读以库为准。

删失败？重试或消息补偿。业务大多接受「很快一致」，不是银行账户那种绝对同时。

---

## 学完自测

1. 穿透击穿雪崩各举一个生活例子  
2. 锁为啥要 NX、EX、唯一值？  
3. 写数据为啥常「先库后删缓存」？

**练嘴：** [Redis面渣级口述.md](./Redis面渣级口述.md) · **卡片：** [Redis卡片速记.md](./Redis卡片速记.md)
"""

MYSQL = f"""# MySQL · 大白话详解

{nav('MySQL面渣级口述.md','MySQL卡片速记.md','路径-Java后端.md')}

---

## 这个专题在问啥？

1. 索引为啥快？为啥有时「加了也没用」？  
2. 事务隔离、脏读这些词是啥意思？  
3. 慢 SQL 你怎么查、怎么改？

---

## 1. 索引：为啥能快？

### 先记一句

没索引 = 一本书**没有目录**，找人名只能从头翻到尾。  
有索引 = 有目录，很快翻到那一页。

### InnoDB 里常见是 B+ 树

你可以想成：**多层目录**。  
第一层指到大章节，再指到小节，最后指到那一行。树不深，磁盘读几次就能定位。

### 主键索引和普通索引差在哪？

- **主键（聚簇）**：目录的最底层**直接就是整行数据**（人带行李住一起）  
- **普通二级索引**：最底层往往只记**主键号**，还要再拿主键去取整行 → 叫**回表**（先查到房号，再回房间拿行李）

如果查询要的字段，二级索引里全有了，就不用回表 → **覆盖索引**，更快。

### 联合索引 (a,b,c)：为啥强调「最左」？

目录是按 a，再 b，再 c 排序的。  
你不告诉 a，只说 b，目录没法从左边开始翻——所以常常用不上。  
条件里有 a，或 a+b，才好用。

### 哪些情况索引容易「失效」？

用人话：

- 对索引列做了函数：`WHERE YEAR(date)=2024`（先把列加工了，目录对不上）  
- `LIKE '%张'`（前边模糊，目录排序帮不上）  
- 类型乱转（字符串列你传了数字等）  

最终听 **EXPLAIN**：看它到底用没用索引。

---

## 2. 事务：一伙操作要成对成功

### 生活例子

转账：A 减钱、B 加钱。只成功一半就乱了。  
事务 = **要么一起成功，要么一起当没发生**。

### 三个吓人词

| 词 | 人话 |
|----|------|
| 脏读 | 读到别人**还没提交**的修改（人家可能马上撤销） |
| 不可重复读 | 同一事务里读两次同一行，中间被人改了，两次不一样 |
| 幻读 | 范围查询，中间有人插入/删除，**行数变了**像见鬼 |

MySQL InnoDB 默认隔离级别多是 **可重复读（RR）**。

### MVCC 是啥？（能说人话就行）

一行数据可以有**多个版本**（改之前、改之后）。  
普通查询像拍了张**快照**：在你这个事务里，尽量看到一致的一版，还不用老加锁堵着别人。  
`SELECT ... FOR UPDATE` 这种是**要看最新的并锁住**，和快照读不一样。

---

## 3. 慢 SQL 怎么办？

四步，按顺序：

1. 先找到慢的那条（慢查询日志/监控）  
2. `EXPLAIN` 看有没有走索引、扫了多少行  
3. 加合适索引或改写法  
4. 再测一次，确认真的快了  

**深分页**为啥痛：`LIMIT 1000000,10` 像翻到第 100 万页再取 10 条，前面白干很多。  
更好：`WHERE id > 上次最后id LIMIT 10` 这种「从上次接着走」。

---

## 学完自测

1. 索引像目录，回表像啥？  
2. 脏读和幻读差在哪？  
3. 慢 SQL 四步是啥？

**练嘴：** [MySQL面渣级口述.md](./MySQL面渣级口述.md) · **卡片：** [MySQL卡片速记.md](./MySQL卡片速记.md)
"""

CONC = f"""# Java 并发 · 大白话详解

{nav('并发面渣级口述.md','并发卡片速记.md','路径-Java后端.md')}

---

## 这个专题在问啥？

多线程一起干活时：

1. 会不会看错数？（可见性）  
2. 会不会步骤做一半被打断？（原子性）  
3. 线程池怎么配才不会把自己打爆？

---

## 1. 先建立感觉：三个坑

### 生活例子：合写文档

两个人同时改同一份在线文档：

- A 改了标题，B 屏幕还显示旧的 → **看不见最新**（可见性）  
- 两人都点「点赞+1」，结果只加了一次 → **步骤交叉**（原子性）  
- 系统自动重排保存顺序，搞出怪结果 → **顺序被打乱**（有序性）

Java 用锁、`volatile`、并发工具来管这些坑。

---

## 2. synchronized：最常用的「进门上锁」

### 先记一句

`synchronized` = 同一时刻**只让一个线程进这段代码**（同一把锁的前提下）。

### 生活例子

公共卫生间：有人进去反锁，别人等；出来下一个人进。  
出来的时候，相当于把你在里面做的改动「公开」给后面的人（可见性也有了）。

---

## 3. volatile：只能做信号灯，不能当柜台

### 先记一句

`volatile` 适合：**一个开关**，大家都能尽快看到最新开/关。  
**不适合** `i++` 这种「先读再改再写」。

### 例子

- `running = false` 通知线程停下来 → 用 volatile 很常见  
- 两个线程同时 `count++` → 即使用 volatile，仍可能丢次数，要加锁或用原子类  

**为啥 i++ 不行？**  
因为是三步：取出 → 加一 → 写回。两人交错就会算错。volatile 不管这三步捆在一起。

---

## 4. 线程池：为啥不要 new Thread 飞？

### 生活例子

饭店不能每来一个客人就新招一个厨师（雇人贵、厨房挤爆）。  
正确做法：**固定几个厨师 + 出餐口排队 + 人太多就婉拒或让顾客自己端一点**。

| 参数 | 人话 |
|------|------|
| 核心线程数 | 常驻厨师 |
| 最大线程数 | 忙时最多雇到几个 |
| 队列 | 出餐口最多堆多少单 |
| 拒绝策略 | 堆满了怎么办：报错 / 让提交的人自己干 / 丢掉 |

### 任务怎么走？（记住顺序）

1. 厨师还不满 → 先上人  
2. 人满了 → 先排队  
3. 队也满了 → 再临时加人到最大  
4. 还满 → 拒绝策略  

**坑：** 队列如果无限长，任务会堆到内存爆，临时工（最大线程）可能永远用不上。

---

## 5. ThreadLocal 一句话

每人一本**自己的小本子**，互不干扰。  
但线程池会**重复用同一批人**，用完不擦小本子，下一个客人可能看到上一个人的字 → 要 `remove`。

---

## 6. CAS / 原子类（了解）

「我看还是 5，才改成 6」——比较并交换。失败就再试。  
适合简单计数；竞争特别惨烈时有更合适的工具（如 LongAdder）。

---

## 学完自测

1. volatile 能做开关，为啥不能保证 count++？  
2. 线程池四个核心概念？  
3. ThreadLocal 为啥在线程池里容易漏？

**练嘴：** [并发面渣级口述.md](./并发面渣级口述.md) · **卡片：** [并发卡片速记.md](./并发卡片速记.md)
"""

JVM = f"""# JVM · 大白话详解

{nav('JVM面渣级口述.md','JVM卡片速记.md','路径-Java后端.md')}

---

## 这个专题在问啥？

1. 内存大概分成哪几块？  
2. 垃圾怎么判断、怎么收？  
3. 线上 OOM、CPU 飙高，你**第一步干啥**？

概念会背不够，**排查思路**更值钱。

---

## 1. 内存分区：像公司租办公楼

| 地方 | 人话 | 谁用 |
|------|------|------|
| 栈 | 每个人自己的**工位抽屉**（方法调用、局部变量） | 每个线程一份 |
| 堆 | **公司大仓库**，货物（对象）放这 | 大家共享，GC 主要清这里 |
| 方法区/元空间 | **说明书柜**（类的信息） | 共享 |
| 程序计数器 | 「我执行到哪一行了」便利贴 | 每线程一份 |

### 堆为啥还分新生代、老年代？

经验：很多对象用一下就扔（外卖盒），少数活很久（工位电脑）。  

- **新生代**：快生快死，勤快打扫  
- **老年代**：活得久的，打扫方式不同  

这样比「整栋楼每次大扫除」划算。

---

## 2. 垃圾怎么认？

### 先记一句

从「还被用到的根」出发，沿引用走，**走不到的就是垃圾**。

不是简单数「有几个箭头指着我」（循环引用会数不清）。

### GC 为啥有时让程序顿一下？

打扫仓库时，有时要暂停业务员进出（停顿）。  
Full GC 像**大扫除**，通常更痛；要尽量少发生。

G1 可以想成：仓库划成很多格子，优先清**垃圾最多的格子**，好控制停顿时间。

---

## 3. 类加载双亲委派（一句话故事）

加载一个类时，先问「爸爸加载器会不会」，爸爸不会再自己加载。  
为啥？避免你写个假的 `java.lang.String` 把系统核心类顶掉。

---

## 4. 线上问题：先会这三招

### 堆内存溢出（OOM）

像仓库爆满。  
**思路：** 留现场（dump）→ 看谁占地方最多、为啥没被扔掉（有没有被错误引用吊着）。

### Full GC 很频繁

先看：打扫完老年代**有没有降下去**。  
降不下去，像垃圾清不走（泄漏）；能降又涨，像生产垃圾太快。

### CPU 100%

像员工疯转。  
**思路：** 找到最忙的线程 → 看它堆栈在干啥（死循环？狂算？锁？）。

### 容器里进程被杀，但 Java 没报 OOM

可能是整栋楼电费限额（cgroup）到了，**堆外内存**也算占用。只盯 `-Xmx` 不够。

---

## 学完自测

1. 栈和堆人话区别？  
2. 怎么判断对象是垃圾？  
3. OOM 和 CPU 飙高第一步分别是啥？

**练嘴（含场景长答）：** [JVM面渣级口述.md](./JVM面渣级口述.md) · **卡片：** [JVM卡片速记.md](./JVM卡片速记.md)
"""

SPRING = f"""# Spring · 大白话详解

{nav('Spring面渣级口述.md','Spring卡片速记.md','路径-Java后端.md')}

---

## 1. IOC / DI：别自己 new 到死

### 生活例子

开饭店：你不自己养猪、发电、请整个供应链。  
**供应商送货上门**（容器帮你创建、装配依赖）。

- **IOC 控制反转：** 创建对象的控制权交给 Spring  
- **DI 依赖注入：** 需要的伙伴由外面塞进来（构造器最清晰）

好处：换实现、写单测 mock 都方便。

---

## 2. AOP：公共套路不用每个方法抄一遍

日志、事务、权限检查，是**横切**的——每个业务都可能要。  
AOP 像给方法外面套一层**透明外壳**（代理）：先进外壳再进真方法。

### 为啥事务注解有时失效？

你在类里面 `this.xxx()` 自己调自己，**没经过外壳**，注解加了也没用。  
像没走大门安检，从后门溜进去了。

---

## 3. 循环依赖（了解）

A 需要 B，B 需要 A。  
Spring 对**单例**某些情况能拆开先放半成品；**构造器互相要**往往无解。  
面试说清「为啥难、啥情况能解」即可。

---

## 4. Spring Boot 为啥省事？

你引入 starter，它根据 classpath **自动猜你要啥配置**。  
爽的代价是「魔法」多：出问题要会看自动配置、条件是否生效。

**练嘴：** [Spring面渣级口述.md](./Spring面渣级口述.md) · **卡片：** [Spring卡片速记.md](./Spring卡片速记.md)
"""

LLM = f"""# LLM 基础 · 大白话详解

{nav('LLM基础常识面渣级口述.md','LLM基础常识卡片速记.md','路径-AI工程.md')}

---

## 先建立正确心态

大模型很会说话，但：

- 它不是实时搜索引擎  
- 它会**一本正经胡说**  
- 它按 **token** 计长度和钱，不是按「字数感觉」

工程师要管：**成本、长度、稳定性、胡话风险**。

---

## 1. Token 和上下文窗口

### 生活例子

Token ≈ 模型吃饭的「小块饼干」。  
一句话会被掰成很多小块；中文常常掰得更碎 → **更贵、更容易塞满**。

上下文窗口 ≈ **餐盘大小**。  
系统设定、历史聊天、检索来的资料、用户问题、还要留空给回答——都要装进盘子。  
盘子满了：不是模型突然变笨，是**装不下或被截断**。

### 工程上怎么办？

历史太长就**做摘要**；检索别一股脑塞 50 段；控制每段长度。

---

## 2. 温度：稳还是活

### 生活例子

- 温度低：像考试填空，尽量选最稳的答案  
- 温度高：像头脑风暴，更跳脱，也更容易跑偏  

客服、抽字段、调工具参数 → **偏低**。  
写小说可以略高。

---

## 3. 幻觉：为啥压不灭只能管

### 先记一句

模型在猜「下一个词更像什么」，不是在查公司数据库。  
所以会说得流畅但**是错的**。

### 怎么办？（面试常答）

1. 先检索公司知识再答（RAG），尽量带引用  
2. 资料不相关就说「不知道」  
3. 关键数字做校验  
4. 用固定考题集做评测，防止越改越差  

不要承诺「某某模型永不胡说」。

---

## 4. 预训练 / SFT / 对齐

| 阶段 | 人话 |
|------|------|
| 预训练 | 读海量文本，学会说话和常识 |
| SFT | 用问答例子教会「听人话」 |
| 对齐 | 更有用、更安全、更符合人类偏好 |

公司知识天天变 → 优先 **RAG**；只要固定语气/格式 → 才考虑微调。

---

## 学完自测

1. token 和字数为啥不是一回事？  
2. 幻觉为啥存在？你怎么压？  
3. 知识常变为啥优先 RAG？

**练嘴：** [LLM基础常识面渣级口述.md](./LLM基础常识面渣级口述.md) · **卡片：** [LLM基础常识卡片速记.md](./LLM基础常识卡片速记.md)
"""

AGENT = f"""# Agent · 大白话详解

{nav('Agent面渣级口述.md','Agent卡片速记.md','路径-AI工程.md')}

---

## 1. Agent 到底是啥？

### 先记一句

普通聊天：你问它答。  
**Agent：有目标，会多步想，会动手用工具，看结果再接着干。**

### 生活例子

你让助理「帮我订下周去上海的机票」：

1. 先查日历（工具）  
2. 再比价（工具）  
3. 发现要护照信息，回来问你  
4. 最后下单（危险操作要你确认）

这就是 Agent 味道：**不是一次说完，而是边做边看**。

公式：`大脑(LLM) + 计划 + 记忆 + 工具 + 刹车（护栏）`

---

## 2. 和 Chat / RAG / 工作流差在哪？

| | 像什么 | 特点 |
|--|--------|------|
| Chat | 对面聊天 | 只说话 |
| RAG | 先翻资料再答 | 知识可更新 |
| 工作流 | 工厂流水线 | 步骤人写死，稳 |
| Agent | 外包全能助理 | 灵活，但贵、难控 |

**啥时候别上 Agent？**  
步骤固定、合规要求严、特别在意成本和可控——用工作流更合适。

---

## 3. ReAct：想一下 → 做一下 → 看结果

```text
思考 → 调用工具 → 观察返回 → 再思考 … → 最终回答
```

### 为啥要刹车？

助理可能：

- 同一件事问十遍  
- 工具一直失败还死磕  
- 步数爆炸，钱烧没了  

所以必须有：

1. 最多几步  
2. 重复动作检测  
3. 超时  
4. 工具失败就停  
5. 转账/删数据等人批准（HITL）

---

## 4. 工具谁来执行？

模型只负责说：「我想调用查天气(城市=上海)」。  
**真正执行的是你的后端**：验权限、验参数、记日志。  
千万别信模型说「我有权限」。

---

## 学完自测

1. 用订机票故事解释 Agent  
2. 防死循环说得出 3 条以上  
3. 举一个不该用 Agent 的场景  

**练嘴：** [Agent面渣级口述.md](./Agent面渣级口述.md) · **卡片：** [Agent卡片速记.md](./Agent卡片速记.md)
"""

PYTHON = f"""# Python · 大白话详解

{nav('Python面渣级口述.md','Python卡片速记.md','路径-Python.md')}

---

## 1. 默认参数大坑（必考）

### 错法

```python
def add(item, bag=[]):  # 糟糕
    bag.append(item)
    return bag
```

多次调用会共用**同一个 list**，数据串了。

### 为啥？

默认参数在**函数定义时**就创建好了，不是每次调用新建。

### 对法

```python
def add(item, bag=None):
    if bag is None:
        bag = []
    bag.append(item)
    return bag
```

---

## 2. 装饰器 / 生成器 / with（用途向）

| 特性 | 人话 | 干啥用 |
|------|------|--------|
| 装饰器 | 给函数外面套马甲 | 日志、登录检查、重试 |
| 生成器 | 一次吐一点，不一次全造好 | 读大文件省内存 |
| with | 用完自动收拾 | 关文件、放锁 |

---

## 3. GIL：为啥多线程算得不快？

### 先记一句

CPython 里，**同一时刻通常只有一个线程在跑 Python 字节码**（简化理解）。

### 怎么选？

| 你在干啥 | 更合适 |
|----------|--------|
| 狂算 CPU | **多进程**或丢到别的服务 |
| 等网络/磁盘 | 多线程或 **asyncio** 都行 |
| 大量等 LLM 接口 | **asyncio** 很合适 |

### asyncio 大忌

在 async 函数里写 `time.sleep` 或同步 `requests`：  
像单行道上有人停车睡觉——**后面全堵死**。  
要用异步库、`await asyncio.sleep`。

FastAPI 适合做 AI 接口服务，但仍要自己做超时、限流。  
长文： [Python异步与FastAPI面渣级口述.md](./Python异步与FastAPI面渣级口述.md)

**练嘴：** [Python面渣级口述.md](./Python面渣级口述.md) · **卡片：** [Python卡片速记.md](./Python卡片速记.md)
"""

OS = f"""# 操作系统 · 大白话详解

{nav('操作系统面渣级口述.md','操作系统卡片速记.md','路径-Java后端.md')}

---

## 1. 进程和线程

- **进程：** 一个独立小程序世界，有自己的内存地盘（隔离好）  
- **线程：** 世界里的多个工人，**共用仓库**（堆），所以要排队、加锁  

Java 里多线程一起改同一个 HashMap 会出事，就是因为**共享堆**。

---

## 2. 用户态 / 内核态

普通程序不能随便碰硬件。  
要读文件、上网，得喊操作系统帮忙（系统调用），像办事要去柜台。  
跑柜台太勤也慢，所以要有缓冲、批量、epoll 这些优化。

---

## 3. epoll（后端常问）

几千上万连接时，不能一人一个服务员死等。  
epoll：一个调度员盯着，**谁就绪就招呼谁**。  
Nginx、Redis、Netty 都是这类思路。

---

## 4. 死锁

四人过独木桥互相卡住：你等我、我等你。  
四个条件都满足才容易死锁；破坏一个就能预防（比如大家按固定顺序拿锁）。

**练嘴：** [操作系统面渣级口述.md](./操作系统面渣级口述.md) · **卡片：** [操作系统卡片速记.md](./操作系统卡片速记.md)
"""

NET = f"""# 计算机网络 · 大白话详解

{nav('计算机网络面渣级口述.md','计算机网络卡片速记.md','路径-Java后端.md')}

---

## 1. TCP 和 UDP

- **TCP：** 打电话，接通才说，保证送到、顺序对——稳但手续多  
- **UDP：** 发短信广播，快，不保证对方一定收到——音视频常用  

不是 UDP 差，是场景不同。

---

## 2. 三次握手（为啥三次？）

两人通话前确认：我能说你能听、你能说我能听。  
三次比两次更能避免「迟到的旧呼叫」造成误会。  
面试能讲清「同步序号 + 双向确认」就很好。

---

## 3. 挥手和 TIME_WAIT

挂电话两边都要说再见（全双工）。  
主动挂的一方多等一会儿（TIME_WAIT），避免最后的再见丢了、旧包搅和下一次通话。  
CLOSE_WAIT 很多：多半是**程序忘了关连接**。

---

## 4. HTTPS

HTTP 裸奔；HTTPS 加了锁和证件（TLS + 证书）。  
大白话：证明你连的是真网站，路上内容加密。

## 5. 状态码速记

- 401：你是谁？（没登录）  
- 403：知道你是谁，但没权限  
- 502：网关后面的服务坏了  
- 504：网关后面的服务太慢超时  

**练嘴：** [计算机网络面渣级口述.md](./计算机网络面渣级口述.md) · **卡片：** [计算机网络卡片速记.md](./计算机网络卡片速记.md)
"""

# secondary easy
def easy(title, oral, card, path, body):
    return f"""# {title} · 大白话详解

{nav(oral, card, path)}

{body}

**练嘴：** [{oral}](./{oral}) · **卡片：** [{card}](./{card})
"""


AI_APP = easy("AI 应用工程", "AI应用工程面渣级口述.md", "AI应用工程卡片速记.md", "路径-AI工程.md", """
## 别把网页直接怼模型

要当「不稳定又贵的外部服务」来做：

```text
用户 → 网关(登录/限流) → 业务编排(RAG/工作流)
    → 调模型 → 检查输出 → 记日志/算钱
```

- **流式输出：** 先吐字，体感更快  
- **超时：** 每层都要有；超时给降级话术  
- **幂等：** 用户点两次别扣两次费  
""")

VEC = easy("向量库", "向量库面渣级口述.md", "向量库卡片速记.md", "路径-AI工程.md", """
## 为啥要向量？

关键词搜「退货」可能对不上写着「售后」的文档。  
向量：把句子变成坐标，**意思近的点靠近**，用来找相关段落。

## 切块 chunk

一次不能塞整本书给模型。切成小段再检索。  
切太大：噪声多；切太小：缺上下文。常按段落/标题切，可稍微重叠。

## 混合检索

编号、错误码用关键词准；同义句用向量强。两者一起往往更好。
""")

SAFE = easy("Prompt 注入与安全", "Prompt注入与AI安全面渣级口述.md", "Prompt注入与AI安全卡片速记.md", "路径-AI工程.md", """
## 攻击在干啥？

用户或网页里的字，企图让模型**改听它的**：泄露系统提示、乱调工具、骗数据。

## 为啥只写「你不要泄露」不够？

模型太听话，提示可被绕过。必须：

- 工具权限尽量小  
- 转账/删除要人点头  
- 服务端校验参数  
- 日志审计、异常告警  
""")

PROMPT = easy("Prompt 工程", "Prompt工程面渣级口述.md", "Prompt工程卡片速记.md", "路径-AI工程.md", """
## 系统提示

规定：你是谁、能干什么、输出啥格式、边界在哪。

## 少样本

给 1～3 个例子，格式更稳。

## 模板化

把可变部分当变量，方便测试和改版，别把提示写死在代码字符串堆里。
""")

EVAL_ = easy("AI 评测", "AI评测金标SOP面渣级口述.md", "AI评测金标SOP卡片速记.md", "路径-AI工程.md", """
## 为啥要金标集？

今天改 prompt「感觉更好」，可能把上周对的题改错。  
固定一套题 + 评分标准，发版前必跑。像考试 rank 榜。
""")

LG = easy("LangGraph / 工作流", "LangGraph与自研工作流面渣级口述.md", "LangGraph与自研工作流卡片速记.md", "路径-AI工程.md", """
## 为啥用图/工作流？

纯 Agent while 循环：难测、难恢复、难给人插入。  
节点画清楚：走到哪、失败去哪、哪里要人审——更稳。
""")

OPS = easy("LLMOps", "LLMOps与可观测面渣级口述.md", "LLMOps与可观测卡片速记.md", "路径-AI工程.md", """
## 要盯啥？

慢不慢、贵不贵、错不错、胡不胡说。  
prompt/模型要有版本，坏了能回滚。请求要能 trace：检索→模型→工具。
""")

INF = easy("推理部署", "推理部署面渣级口述.md", "推理部署卡片速记.md", "路径-AI工程.md", """
## 人话

GPU 很贵。一堆请求一起算（batch）更省，但可能让单人等久一点。  
量化：精度换速度/显存。限流、多副本防打挂。
""")

K8S = easy("Docker 与 K8s", "Docker与K8s面渣级口述.md", "Docker与K8s卡片速记.md", "路径-Java后端.md", """
## Docker

「在我电脑能跑」→ 打成镜像，别处同样跑。容器=跑起来的实例。

## K8s

帮你管很多容器：要几个副本、挂了拉起、怎么对外访问、滚动升级。  
探针：活着吗？准备好接流量吗？
""")

MQ = easy("消息队列", "MQ面渣级口述.md", "MQ卡片速记.md", "路径-Java后端.md", """
## 为啥用？

解耦、削峰、异步。像快递中转站，不必同步死等对方。

## 代价

消息可能重复 → 消费要**幂等**。可能积压 → 加消费者或优化处理。
""")

KAFKA = easy("Kafka", "Kafka面渣级口述.md", "Kafka卡片速记.md", "路径-Java后端.md", """
Topic 下分很多**分区**并行。  
同组消费者分摊分区。offset 记读到哪。要顺序：同 key 进同分区。
""")

RMQ = easy("RocketMQ", "RocketMQ面渣级口述.md", "RocketMQ卡片速记.md", "路径-Java后端.md", """
国内业务常用。事务消息、延时消息是高频考点。  
事务消息：先半消息，本地事务成功再真投递，失败回查。
""")

MS = easy("微服务", "微服务与分布式面渣级口述.md", "微服务与分布式卡片速记.md", "路径-Java后端.md", """
拆小服务：好独立发版，但网络会失败、事务变难。  
接口要幂等；超时、重试、熔断、限流是基本功。
""")

DP = easy("设计模式", "设计模式面渣级口述.md", "设计模式卡片速记.md", "路径-Java后端.md", """
不是背 UML。是：**哪里会变，就用模式把变化关进盒子**。  
工厂管创建、策略管算法切换、代理管增强（AOP 同源）、模板管固定流程。
""")

ALGO = easy("算法", "算法面渣级口述.md", "算法卡片速记.md", "路径-Java后端.md", """
先判断题型：双指针、哈希、栈、树、BFS、二分、DP…  
再写代码，边写边说复杂度。多练比多背模板重要。
""")

SD = easy("系统设计", "后端系统设计面渣级口述.md", "后端系统设计卡片速记.md", "路径-Java后端.md", """
步骤：问清楚需求和量 → 接口 → 存哪 → 主流程 → 哪里会成瓶颈（缓存/分库/异步）。  
用数字说话，别一上来堆中间件名词。
""")

BEHAV = easy("行为面试", "行为面试面渣级口述.md", "行为面试卡片速记.md", "路径-Java后端.md", """
STAR：背景 → 任务 → **你做了啥** → 结果（最好有数字）。  
失败题讲复盘，不甩锅。
""")

STAR = easy("项目 STAR", "项目面试面渣级口述.md", "项目面试STAR卡片速记.md", "路径-Java后端.md", """
准备 1～2 个主项目：你的角色、难点、方案对比、结果数据。  
避免全程「我们」，说清「我」。
""")


def main():
    mapping = {
        "Java高频面试题与知识点.md": JAVA,
        "Redis高频面试题与知识点.md": REDIS,
        "MySQL高频面试题与知识点.md": MYSQL,
        "并发高频面试题与知识点.md": CONC,
        "JVM高频面试题与知识点.md": JVM,
        "Spring高频面试题与知识点.md": SPRING,
        "LLM基础常识高频面试题与知识点.md": LLM,
        "Agent高频面试题与知识点.md": AGENT,
        "Python高频面试题与知识点.md": PYTHON,
        "操作系统高频面试题与知识点.md": OS,
        "计算机网络高频面试题与知识点.md": NET,
        "AI应用工程高频面试题与知识点.md": AI_APP,
        "向量库高频面试题与知识点.md": VEC,
        "Prompt注入与AI安全高频面试题与知识点.md": SAFE,
        "Prompt工程高频面试题与知识点.md": PROMPT,
        "AI评测金标SOP高频面试题与知识点.md": EVAL_,
        "LangGraph与自研工作流高频面试题与知识点.md": LG,
        "LLMOps与可观测高频面试题与知识点.md": OPS,
        "推理部署高频面试题与知识点.md": INF,
        "Docker与K8s高频面试题与知识点.md": K8S,
        "MQ高频面试题与知识点.md": MQ,
        "Kafka高频面试题与知识点.md": KAFKA,
        "RocketMQ高频面试题与知识点.md": RMQ,
        "微服务与分布式高频面试题与知识点.md": MS,
        "设计模式高频面试题与知识点.md": DP,
        "算法高频面试题与知识点.md": ALGO,
        "后端系统设计高频面试题与知识点.md": SD,
        "行为面试高频题与知识点.md": BEHAV,
        "项目面试STAR高频题与知识点.md": STAR,
    }
    for k, v in mapping.items():
        w(k, v)


if __name__ == "__main__":
    main()
