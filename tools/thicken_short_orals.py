# -*- coding: utf-8 -*-
"""加长最短面渣（约10篇）到可说2～3分钟。"""
from pathlib import Path

DOCS = Path(__file__).resolve().parents[1] / "docs"


def w(name, text):
    p = DOCS / name
    p.write_text(text.strip() + "\n", encoding="utf-8")
    print(name, p.stat().st_size)


# Each oral: 口语连贯，2-3 min


def main():
    w(
        "LLMOps与可观测面渣级口述.md",
        """# LLMOps 与可观测 · 面渣级口述

> 知识点：[LLMOps与可观测高频面试题与知识点.md](./LLMOps与可观测高频面试题与知识点.md) · [AI完整卷](./AI-Agent高频面试题与知识点.md)

---

## 开场（30 秒）

LLMOps 不是换个名字的 DevOps，而是把 **Prompt、模型版本、检索索引、评测、发布** 当成会变的配置来管。  
没有版本和观测，改 Prompt 可能 silent 变差，还不知道是哪次上线引入的。

---

## 我怎么管一轮上线（1.5 分钟）

第一，**版本**：Prompt 模板、模型名与参数、Embedding 模型、向量索引版本，全部打 tag，能回滚。  
第二，**门禁**：改这些东西必须跑金标评测集，看正确率、拒答率、引用率，不达标不发。  
第三，**灰度**：先小流量，看延迟、错误率、用户反馈，再全量。

可观测我分三层：

1. **指标**：TTFT、总延迟、token 消耗、错误码、工具失败率、成本/请求  
2. **日志**：请求 id、用户/租户、用的模型与 Prompt 版本  
3. **Trace**：检索 span、模型 span、每个工具调用的输入输出摘要  

出问题先看：是延迟涨了、还是质量差了、还是成本爆了——三条线分开查。

---

## 常见追问怎么接（1 分钟）

**问：和传统监控差在哪？**  
多了「质量」和「token 成本」两条业务线，不只是 5xx。

**问：如何发现幻觉变多？**  
离线金标 + 在线抽样人工/小模型打分；关键字段规则校验。

**问：你项目里怎么做的？**  
（结合实际：网关统一打点、某次 Prompt 回滚、某次 topK 调参降延迟。）

---

## 收尾

一句话：LLMOps = **可版本、可评测、可观测、可回滚**。  
细节回知识点文档；评测展开见 [AI评测面渣](./AI评测金标SOP面渣级口述.md)。
""",
    )

    w(
        "RocketMQ面渣级口述.md",
        """# RocketMQ · 面渣级口述

> 知识点：[MQ完整卷](./MQ高频面试题与知识点.md) · [中间件频率](./其他中间件八股频率排序.md)

---

## 开场

RocketMQ 是偏业务消息的中间件，国内电商类场景常见。我重点讲：架构、和 Kafka 对比、事务消息、延迟消息、如何保证不丢不重。

---

## 架构（1 分钟）

核心角色：

- **NameServer**：轻量路由，Broker 注册，Producer/Consumer 拉路由，无强中心状态机  
- **Broker**：存消息，主从  
- **Producer / Consumer**：生产消费；消费分集群模式（分摊）和广播模式  

和 Kafka 比：Kafka 吞吐与流处理生态强；RocketMQ **事务消息、延时消息**对业务更直接，运维模型对国内团队友好。

---

## 事务消息（1 分钟，必考）

流程：

1. 发 **半消息**（消费者不可见）  
2. 执行本地事务（如下单写库）  
3. 成功则 Commit，失败 Rollback  
4. 超时未知则 Broker **回查**生产者本地事务状态  

解决的是：本地事务和「消息一定发出/不发出」对齐，做最终一致。

---

## 不丢不重与积压（1 分钟）

- **不丢**：同步刷盘/主从、发送重试、消费成功再 ACK  
- **不重**：业务幂等（唯一键、状态机）  
- **顺序**：同一业务 key 进同一队列，单线程消费  
- **积压**：扩消费者、优化消费、临时专题扩容、非关键降级  

延迟消息：催单、超时关单常用，比自己扫表更干净。

---

## 收尾

选型看场景：日志流大数据偏 Kafka；订单事务延时偏 RocketMQ。  
项目里结合：用过事务消息还是普通主题、如何幂等、积压怎么处理。
""",
    )

    w(
        "AI评测金标SOP面渣级口述.md",
        """# AI 评测金标 SOP · 面渣级口述

> 知识点：[AI评测金标SOP高频面试题与知识点.md](./AI评测金标SOP高频面试题与知识点.md) · [AI完整卷](./AI-Agent高频面试题与知识点.md)

---

## 开场

LLM 输出不稳定，改 Prompt「感觉更好」可能把别的 case 改坏。所以必须有 **金标集 + 固定流程**，把质量变成可回归的工程问题。

---

## 金标集怎么建（1 分钟）

1. 覆盖主路径、边界、对抗/注入、应拒答的题  
2. 每题有标准答案或 **rubric**（部分正确怎么给分）  
3. **版本化**，改题集要记 changelog  
4. 样本量够支撑对比，不要只有 5 条手测  

离线评测：发版前跑全量，卡阈值。  
在线评测：抽样真实流量，看分布漂移。

---

## 指标（1 分钟）

- 质量：正确 / 部分正确 / 有害 / 拒答是否合理  
- RAG：引用是否可追溯、忠实度  
- 系统：延迟、TTFT、token、成本  
- Agent：任务成功率、步数、工具失败率、人工接管率  

人评要校准：双评、争议仲裁，避免个人口味。

---

## SOP 口述（流程）

```text
改 Prompt/模型/检索 → 跑金标 → 对比基线
 → 不达标阻断发布 → 达标灰度 → 在线抽样监控
 → 坏 case 回流金标
```

---

## 追问

**为何不能只看用户点赞？** 延迟、幸存者偏差、刷单。  
**如何发现回归？** 基线对比 + 固定随机种子/温度策略。  

收尾：评测是 LLM 工程的 CI，不是可选装饰。
""",
    )

    w(
        "推理部署面渣级口述.md",
        """# 推理部署 · 面渣级口述

> 知识点：[推理部署高频面试题与知识点.md](./推理部署高频面试题与知识点.md)

---

## 开场

推理部署关心三件事：**延迟、吞吐、成本（显存）**。在线业务和离线批跑目标不同。

---

## 关键概念（2 分钟）

**吞吐 vs 延迟**：Batch 越大 GPU 利用率越高，但单请求可能多等。在线要看 P99，不能只看平均值。

**量化**：降精度（如 INT8/FP8）省显存提速，有精度损失，关键业务要评测。

**KV Cache**：自回归解码缓存中间状态，加速后续 token；显存占用随上下文涨。

**连续批处理 / 动态 batch**：框架侧提升吞吐的常见手段（vLLM 等，点到即可）。

**弹性**：队列削峰、限流、多副本、小模型降级、超时熔断。

---

## 云 API vs 自建

| | 云 API | 私有化 |
|--|--------|--------|
| 优点 | 快上线 | 数据可控 |
| 代价 | 出域、单价 | GPU 运维 |

常见混合：网关统一，敏感走私有。

---

## 排查口语

延迟高：看排队、batch、上下文长度、首 token 还是解码慢。  
OOM：上下文、并发、KV、batch。  
质量差：别只怪部署，先分模型和数据问题。

---

## 收尾

一句话：在线推理是 **SLA + 成本** 的权衡；用限流、批处理、量化、副本组合，而不是单拧一个旋钮。
""",
    )

    w(
        "Kafka面渣级口述.md",
        """# Kafka · 面渣级口述

> 知识点：[MQ完整卷](./MQ高频面试题与知识点.md)

---

## 开场

Kafka 是高吞吐分布式日志，适合日志采集、流处理、事件总线。我讲：核心概念、为何快、可靠性、和业务 MQ 对比。

---

## 核心概念（1 分钟）

- **Topic / Partition**：分区是并行单位，同 key 进同分区可保序  
- **副本**：Leader / Follower；**ISR** 是与 leader 同步的集合  
- **Producer**：acks 控制可靠性（0/1/all）  
- **Consumer Group**：组内分摊分区，组间独立  

HW（高水位）限制消费者可见进度，和 ISR 有关，保证不读未充分复制的数据（简化说法）。

---

## 为什么吞吐高（45 秒）

1. 磁盘顺序追加写  
2. 零拷贝发送  
3. 分区并行  
4. 批量发送与拉取  
5. 页缓存  

---

## 可靠性与顺序、积压（1 分钟）

- **不丢**：acks=all、足够副本、消费处理成功再提交 offset  
- **不重**：业务幂等（Kafka 默认至少一次语义常见）  
- **顺序**：单分区有序，全局有序代价大  
- **积压**：加消费者（分区数要够）、优化消费、扩分区要谨慎  

---

## vs RocketMQ

Kafka：流/日志/超高吞吐；RocketMQ：事务消息、延时消息等业务特性。选型看场景不是谁绝对更好。

---

## 收尾

项目结合：用的 acks、分区键、消费提交方式、是否遇到 rebalance 或积压。
""",
    )

    w(
        "Docker与K8s面渣级口述.md",
        """# Docker 与 K8s · 面渣级口述

> 知识点：[Docker与K8s高频面试题与知识点.md](./Docker与K8s高频面试题与知识点.md)

---

## 开场

容器解决「环境一致」；K8s 解决「大量容器怎么调度、发布、自愈」。

---

## Docker（1 分钟）

- **镜像**：只读分层模板  
- **容器**：镜像的运行实例  
- 好处：依赖打包、启动快、密度高  
- 注意：容器不是虚拟机，隔离靠 Namespace/Cgroups  

---

## K8s 核心对象（1.5 分钟）

| 对象 | 一句话 |
|------|--------|
| Pod | 最小调度单元，可共享网络/卷 |
| Deployment | 声明副本与滚动更新 |
| Service | 稳定访问入口，负载到 Pod |
| ConfigMap/Secret | 配置与密钥 |
| Ingress | 七层入口（常见） |

**探针**：liveness 挂了重启；readiness 未就绪不接流量。  
**资源**：request 调度保证；limit 防打爆邻居。

---

## 发布与排障口语

滚动更新：新 Pod 起来且 ready，再下老 Pod。  
常见问题：镜像拉不下来、探针失败、OOMKilled（limit 太小）、配置没挂载。

---

## 收尾

后端面试够用：镜像/容器、Pod/Deployment/Service、探针、request/limit、滚动发布。结合项目讲一次上线与回滚即可。
""",
    )

    w(
        "Prompt工程面渣级口述.md",
        """# Prompt 工程 · 面渣级口述

> 知识点：[Prompt工程高频面试题与知识点.md](./Prompt工程高频面试题与知识点.md) · [AI完整卷](./AI-Agent高频面试题与知识点.md)

---

## 开场

Prompt 工程是把业务约束写进模型输入：**角色、边界、格式、示例**，并模板化、可测、可版本管理，而不是玄学咒语。

---

## 我怎么写生产 Prompt（2 分钟）

1. **系统提示**：你是谁、能做什么、不能做什么、输出合同（JSON 字段）  
2. **边界**：材料不足说不知道，禁止编造  
3. **少样本**：1～3 个正例稳格式  
4. **结构化输出**：字段名固定，便于解析校验  
5. **模板变量**：用户问题、检索片段分槽位，不把一切糊成一段  

变量化之后才能：单测、回归、A/B、回滚。

---

## 和 RAG / Agent 的关系

- RAG：Prompt 里明确「仅依据下列资料回答并引用」  
- Agent：系统提示约束工具使用规范与停止条件  
- 安全：用户输入当数据不当指令，防注入  

---

## 追问

**温度？** 事实与抽字段偏低。  
**越写越长？** 要评测驱动删减，不是越长越好。  
**项目？** 举一次改 Prompt 前后金标指标变化。

---

## 收尾

Prompt 是产品配置，纳入版本与评测，不靠上线碰运气。
""",
    )

    w(
        "LangGraph与自研工作流面渣级口述.md",
        """# LangGraph 与自研工作流 · 面渣级口述

> 知识点：[LangGraph与自研工作流高频面试题与知识点.md](./LangGraph与自研工作流高频面试题与知识点.md) · [AI完整卷](./AI-Agent高频面试题与知识点.md)

---

## 开场

纯 while-Agent 灵活但难测、难恢复、难插入人工。LangGraph 类方案用 **图 + 状态** 做可控编排。

---

## 为什么用图（1 分钟）

- **节点**：一步（调模型、调工具、人审）  
- **边**：成功/失败/条件分支  
- **状态**：全程可序列化，可断点续跑  

比线性 Chain 强在：循环、分支、HITL、可视化与回放。

---

## vs 纯 Agent / vs 死工作流

| | 死工作流 | 纯 Agent | Graph |
|--|----------|----------|-------|
| 灵活 | 低 | 高 | 中高 |
| 可控 | 高 | 低 | 高 |
| 可测 | 易 | 难 | 节点可测 |

生产常见：外层图控风险，内层节点可调 LLM 或小子 Agent。

---

## 自研要注意（1 分钟）

1. 状态序列化与版本  
2. 重试幂等、超时、死信  
3. 每步 trace  
4. 人机节点与权限  
5. 费用熔断（max_steps）  

---

## 收尾

LangGraph 解决的是 **可控的多步智能**，不是取代所有业务 if-else。结合项目讲：几个节点、哪里循环、哪里要人审。
""",
    )

    w(
        "后端系统设计面渣级口述.md",
        """# 后端系统设计 · 面渣级口述（总述）

> 知识点：[系统设计完整卷](./后端系统设计高频面试题与知识点.md) · [场景题](./场景题高频面试题与知识点.md) · [频率](./系统设计八股频率排序.md)

---

## 开场：我怎么答设计题

我用固定框架，避免东拉西扯：

```text
1. 澄清需求（功能 + QPS/数据量/一致性/可用性）
2. 容量粗算
3. 画架构：接入→网关→服务→缓存→DB→MQ
4. 关键路径深入
5. 瓶颈与优化
6. 兜底与监控
```

大厂要的是 **权衡**，不是唯一标准答案。

---

## 我会主动问的澄清（45 秒）

- 读写比、峰值 QPS、数据规模、是否强一致  
- 是否允许最终一致、延迟要求  
- 主要客户端（App/Web）  

然后粗算：日活 × 行为 × 峰值系数 → 读写 QPS；存储量级。

---

## 通用高并发手段（1 分钟）

- 无状态服务水平扩展  
- 缓存挡读、CDN  
- MQ 削峰异步  
- 限流熔断降级  
- 分库分表 / 分片  
- 幂等与对账  

---

## 高频场景一句话锚点

| 场景 | 锚点 | 口述长文 |
|------|------|----------|
| 秒杀 | 限流+Redis预扣+MQ | 完整卷/场景题 |
| 短链 | 发号 Base62+缓存 | 完整卷 |
| Feed | 推拉结合 | [Feed](./系统设计口述-Feed流.md) |
| 延时 | 延迟队列/ZSet | [延时](./系统设计口述-延时消息.md) |
| 支付 | 状态机+幂等 | [支付](./系统设计口述-支付与幂等.md) |

---

## 收尾

先框架后细节；每个方案说清 **为什么、缺点、兜底**；尽量落到自己项目里的限流、缓存、异步经验。
""",
    )

    w(
        "LLM基础常识面渣级口述.md",
        """# LLM 基础常识 · 面渣级口述

> 知识点：[LLM基础常识高频面试题与知识点.md](./LLM基础常识高频面试题与知识点.md) · [AI完整卷](./AI-Agent高频面试题与知识点.md)

---

## 开场

AI 工程岗问基础，不考推公式，考你能不能用工程语言解释：**token、窗口、温度、幻觉、训练阶段、云与私有**。

---

## Token 与上下文（1 分钟）

模型按 **token** 计长度和钱，不是按字数；中文往往更吃 token。  
上下文窗口是一次请求的硬上限：系统提示、历史、检索、问题、预留输出都占窗口。  
超了要：摘要历史、少塞检索、截断旧轮——是 **抢预算**，不是模型突然变笨。

---

## 温度与幻觉（1 分钟）

温度低更稳，适合抽字段、客服、工具参数；温度高更发散。  
**幻觉**是生成式特性：预测下一个 token，不是查权威库。  
工程上：RAG 引用、低相关拒答、校验、金标评测——不承诺永不胡说。

---

## 训练三阶段（45 秒）

| 阶段 | 干什么 |
|------|--------|
| 预训练 | 学语言与广博知识 |
| SFT | 学会听指令 |
| 偏好对齐 | 更有用、更安全 |

知识常变 → **优先 RAG**；风格格式稳定再考虑微调。

---

## 云 API vs 私有

看数据是否出域、调用量、团队运维。常混合 + 统一网关。  
指标：TTFT、总延迟、成本。

---

## 收尾

基础常识服务于选型与风控；细节回知识点，应用见 Agent/RAG 口述。
""",
    )

    print("done 10 orals")


if __name__ == "__main__":
    main()
