# -*- coding: utf-8 -*-
"""知识点：面试笔记体（题号 + 分层要点）。面渣不动。"""
from pathlib import Path

DOCS = Path(__file__).resolve().parents[1] / "docs"


def w(name, text):
    p = DOCS / name
    p.write_text(text.strip() + "\n", encoding="utf-8")
    print(name, p.stat().st_size)


def nav(oral, card, path):
    return f"""<!-- NAV:START -->
> 📖 **知识点（笔记体）** · 🗣️ [面渣练嘴](./{oral}) · 🃏 [卡片](./{card})
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md) · [路径](./{path})
>
<!-- NAV:END -->
"""


JAVA = f"""# Java 基础 · 知识点

{nav('Java面渣级口述.md', 'Java卡片速记.md', '路径-Java后端.md')}

> 笔记体：题号 + 分层要点。要完整口述 → [面渣](./Java面渣级口述.md)；速记 → [卡片](./Java卡片速记.md)。

---

## 1. == 和 equals() 的区别？

- **==**
  - 基本数据类型：比较**值**。
  - 引用数据类型：比较**内存地址**（是不是同一个对象）。
- **equals()**
  - 默认（`Object`）：等价于 `==`，比较地址。
  - 重写后（如 `String`、`Integer`）：比较**内容**。

---

## 2. 为什么重写 equals() 必须重写 hashCode()？（必考）

- **约定**：两个对象 `equals` 为 true，它们的 `hashCode` **必须**相等。
- **原因**：`HashMap` / `HashSet` 先按 `hashCode` 找桶，再在桶里用 `equals` 比 key。
- **后果**：只重写 `equals` 不重写 `hashCode` → 逻辑上相等的对象进不同桶 → **put 进去 get 不出来**。
- **反向**：`hashCode` 相同 **不代表** `equals` 一定为 true（允许碰撞）。

---

## 3. String、StringBuffer、StringBuilder 的区别？

- **String**
  - 不可变（`final` 类 + 内部数组不可对外改）。
  - 线程安全（状态不可变）。
  - 修改会生成新对象；常量池可共享。
- **StringBuilder**
  - 可变；**线程不安全**；单线程拼接**效率最高**。
- **StringBuffer**
  - 可变；方法带 `synchronized`，**线程安全**；效率较低。
- **选型**：单线程拼接用 `StringBuilder`；需要同步再用 `StringBuffer`（现代代码少用）。

---

## 4. 自动装箱与拆箱？Integer 的缓存机制？

- **装箱**：基本类型 → 包装类（如 `Integer.valueOf(i)`）。
- **拆箱**：包装类 → 基本类型（如 `intValue()`）。
- **Integer 缓存**（默认 **-128～127**）
  - 该区间 `valueOf` 复用缓存对象。
  - `Integer a = 100; Integer b = 100;` → `a == b` 为 **true**（同一缓存对象）。
  - `Integer a = 200; Integer b = 200;` → `a == b` 为 **false**（不同对象）；比内容用 `equals`。
- **注意**：`new Integer(100)` 不走缓存；业务比较包装类内容优先 `equals`。

---

## 5. HashMap 底层结构？（必考）

- **JDK 8**：数组 + 链表 + 红黑树。
- **默认**：容量 16，负载因子 0.75，扩容为 2 倍。
- **树化**：某桶链表长度 ≥ 8 **且** 数组长度 ≥ 64 → 红黑树；树节点 ≤ 6 可退回链表。
- **下标**：容量为 2 的幂时，用 `(n - 1) & hash` 代替取模。
- **线程安全**：否；并发用 `ConcurrentHashMap`。

---

## 6. HashMap 的 put 流程？

1. 计算 key 的 hash（扰动）。
2. 算数组下标。
3. 桶为空 → 直接放入。
4. 桶头 key 的 hash 与 equals 相同 → 覆盖 value。
5. 否则沿链表/树查找；没有则追加。
6. 链长达到阈值且表够长 → 树化。
7. 元素数超过 `容量 × 负载因子` → 扩容 rehash。

---

## 7. HashMap 为什么线程不安全？ConcurrentHashMap？

- **HashMap**：多线程 put 可能丢数据、死循环（历史版本）等，结构非并发设计。
- **ConcurrentHashMap（JDK 8）**：CAS + `synchronized` 锁桶头节点等，读多写少场景常用；不锁整张表。

---

## 8. ArrayList 和 LinkedList？

- **ArrayList**：动态数组；随机访问 O(1)；尾部追加均摊 O(1)；中间插入/删除要搬移。
- **LinkedList**：双向链表；头尾插入删除快；随机访问慢。
- **实践**：绝大多数场景 `ArrayList`；小数据量中间插入也不一定 LinkedList 更快。

---

## 9. fail-fast 是什么？

- 用迭代器遍历时，若集合结构被**其他方式**修改，可能抛 `ConcurrentModificationException`。
- 目的：尽早暴露错误修改；并发容器迭代语义通常是弱一致，不保证同样 CME。

---

## 10. final、finally、finalize？

- **final**：类不可继承 / 方法不可重写 / 变量不可重新赋值（引用不可变，不代表对象内容不可变）。
- **finally**：`try` 后几乎总执行的块（用于释放资源）；注意 `System.exit` 等极端情况。
- **finalize**：对象被 GC 前调用（已不推荐，别依赖）。

---

## 11. 深拷贝 vs 浅拷贝？

- **浅拷贝**：只复制外层对象，内部引用仍指向同一对象。
- **深拷贝**：递归复制内部对象，互不影响。

---

## 自测

- [ ] equals / hashCode 契约 + HashMap 后果  
- [ ] String 三兄弟选型  
- [ ] Integer 缓存区间与 `==`  
- [ ] HashMap 结构 + put + 树化条件  

**口述：** [Java面渣级口述.md](./Java面渣级口述.md) · **卡片：** [Java卡片速记.md](./Java卡片速记.md)
"""

REDIS = f"""# Redis · 知识点

{nav('Redis面渣级口述.md', 'Redis卡片速记.md', '路径-Java后端.md')}

---

## 1. Redis 为什么快？

- 数据在**内存**。
- **命令执行**单线程，无锁竞争（逻辑简单）。
- 高效数据结构。
- **IO 多路复用**处理大量连接。
- **注意**：Redis 6+ 多线程主要优化网络读写，不是「命令随意多线程并行」。

---

## 2. 五种基本类型及场景？

- **String**：缓存、计数、Session、分布式锁。
- **Hash**：对象多字段（局部更新）。
- **List**：最新列表、简单队列。
- **Set**：去重、共同关注。
- **ZSet**：排行榜、按 score 排序/延时。

---

## 3. 缓存穿透 / 击穿 / 雪崩？（必考，分开说）

- **穿透**
  - 定义：查**根本不存在**的数据，缓存不中，每次打 DB。
  - 方案：缓存空值（短 TTL）、布隆过滤器、参数校验。
- **击穿**
  - 定义：**热点 key** 过期瞬间，大量并发回源。
  - 方案：互斥锁重建、逻辑过期、永不过期 + 异步刷新。
- **雪崩**
  - 定义：大量 key **同时过期**，或 Redis 宕机。
  - 方案：TTL 加随机、集群高可用、限流降级、多级缓存。

---

## 4. 分布式锁怎么做？（必考）

- 命令：`SET key unique_value NX EX seconds`
  - **NX**：不存在才成功（互斥）。
  - **EX**：过期防死锁。
  - **unique_value**：标识持有者，防误删别人的锁。
- 删除：Lua 脚本「校验 value 再 del」，保证原子。
- 长任务：需要**续期**（如 Redisson 看门狗）。
- 风险：主从切换极端情况可能丢锁；业务需评估。

---

## 5. 缓存与 DB 一致性？

- 常见 **Cache Aside**
  - 读：先缓存，miss 再 DB，回填缓存。
  - 写：**先更新 DB，再删除缓存**。
- 删缓存失败：重试 / binlog 补偿。
- 多最终一致；强一致成本高。

---

## 6. 持久化 RDB / AOF？

- **RDB**：快照，恢复快，可能丢最近一段时间数据。
- **AOF**：记写命令，更耐丢数据，文件可能更大；可重写压缩。
- 可混合使用。

---

## 自测

- [ ] 三连定义不混  
- [ ] 锁四要素（NX/EX/唯一值/Lua）  
- [ ] 先 DB 再删缓存  

**口述：** [Redis面渣级口述.md](./Redis面渣级口述.md) · **卡片：** [Redis卡片速记.md](./Redis卡片速记.md)
"""

MYSQL = f"""# MySQL · 知识点

{nav('MySQL面渣级口述.md', 'MySQL卡片速记.md', '路径-Java后端.md')}

---

## 1. 索引为什么快？InnoDB 用什么结构？

- 索引减少扫描行数，避免全表扫描。
- InnoDB 常用 **B+ 树**：矮胖、适合磁盘、支持范围查询。
- **主键索引（聚簇）**：叶子节点是**整行数据**。
- **二级索引**：叶子存**主键值**，常需**回表**再查聚簇。
- **覆盖索引**：查询列都在二级索引中，无需回表。

---

## 2. 联合索引与最左前缀？

- 索引 `(a,b,c)` 按 a、b、c 排序。
- 能用：`a` / `a,b` / `a,b,c`。
- 通常不能很好用：只有 `b` 或只有 `c`（无 a）。
- 设计时把区分度高、常出现在条件里的列放左边（结合查询模式）。

---

## 3. 索引失效常见情况？

- 对索引列使用函数 / 运算。
- `LIKE '%xx'` 左模糊。
- 类型隐式转换。
- 违反最左前缀。
- 优化器认为全表扫描更便宜。
- **以 EXPLAIN 为准**（看 key、type、rows、Extra）。

---

## 4. 事务 ACID？隔离级别？三种读问题？

- **ACID**：原子性、一致性、隔离性、持久性。
- **读问题**
  - 脏读：读到未提交数据。
  - 不可重复读：同一事务两次读同一行结果不同（被改）。
  - 幻读：范围读行数变化（插入/删除）。
- InnoDB **默认 RR（可重复读）**。

---

## 5. MVCC 是什么？

- 多版本并发控制：行有版本链（undo）。
- **快照读**：普通 `SELECT`，按 ReadView 看版本，一般不加锁。
- **当前读**：`SELECT ... FOR UPDATE` / 更新删除等，读最新并加锁。

---

## 6. 慢 SQL 怎么排查？

1. 慢查询日志 / 监控定位 SQL。  
2. `EXPLAIN` 看执行计划。  
3. 加索引 / 改写 SQL / 避免 `SELECT *`。  
4. 验证耗时与计划。  
- 深分页：`LIMIT 很大偏移` 差 → 用 `id > last` 或延迟关联。

---

## 7. redo / undo / binlog？

- **redo**：崩溃恢复，保证已提交不丢（WAL）。
- **undo**：回滚 + MVCC 旧版本。
- **binlog**：主从复制、归档恢复。

---

## 自测

- [ ] 聚簇 / 回表 / 覆盖 / 最左  
- [ ] 三种读问题 + 默认隔离级别  
- [ ] 慢 SQL 四步  

**口述：** [MySQL面渣级口述.md](./MySQL面渣级口述.md) · **卡片：** [MySQL卡片速记.md](./MySQL卡片速记.md)
"""

CONC = f"""# Java 并发 · 知识点

{nav('并发面渣级口述.md', '并发卡片速记.md', '路径-Java后端.md')}

---

## 1. 并发要解决什么？

- **原子性**：多步操作不被打断成中间态。
- **可见性**：一个线程的写，其他线程能看到。
- **有序性**：禁止非法重排序，保证多线程语义。

---

## 2. synchronized 和 Lock 区别？

- **synchronized**：JVM 关键字；自动加锁释放；可重入；非公平。
- **Lock（如 ReentrantLock）**：API 锁；可尝试锁、可中断、可超时、可公平；需手动 unlock（常 `try/finally`）；可多 Condition。

---

## 3. volatile 作用？能保证 i++ 吗？

- 作用：**可见性** + **有序性**（禁止部分重排序）。
- **不能**保证复合操作原子性（`i++` 是读改写）。
- 适用：状态标志、一个写多个读的简单发布等。

---

## 4. 线程池核心参数与流程？

- 参数：corePoolSize、maximumPoolSize、keepAliveTime、workQueue、threadFactory、handler。
- 提交流程：
  1. 线程数 < core → 创建核心线程。  
  2. ≥ core → 入队。  
  3. 队列满且 < max → 创建非核心线程。  
  4. 仍满 → 拒绝策略。  
- 拒绝策略：Abort、CallerRuns、Discard、DiscardOldest。
- **坑**：无界队列可能导致 max 线程用不上、任务堆到 OOM。

---

## 5. ThreadLocal 注意点？

- 每个线程一份变量副本。
- 线程池复用线程 → 用完必须 `remove`，防脏数据与内存泄漏。

---

## 6. CAS？ABA？

- CAS：比较并交换，无锁更新；失败重试。
- ABA：值 A→B→A，CAS 误判未变 → 版本号 / `AtomicStampedReference`。
- `AtomicInteger` 等基于 CAS。

---

## 7. AQS 一句话？

- 同步器框架：`state` + 等待队列；`ReentrantLock`、`CountDownLatch` 等基于它。

---

## 自测

- [ ] volatile 与 i++  
- [ ] 线程池参数与提交顺序  
- [ ] ThreadLocal + remove  

**口述：** [并发面渣级口述.md](./并发面渣级口述.md) · **卡片：** [并发卡片速记.md](./并发卡片速记.md)
"""

JVM = f"""# JVM · 知识点

{nav('JVM面渣级口述.md', 'JVM卡片速记.md', '路径-Java后端.md')}

---

## 1. 运行时内存分区？

- **线程私有**：程序计数器、虚拟机栈、本地方法栈。
- **线程共享**：堆、方法区（JDK 8+ 元空间在本地内存）。
- **堆**：对象实例；分代：新生代（Eden + S0/S1）+ 老年代。
- **直接内存**：NIO 堆外，受本机/容器限制。

---

## 2. 如何判断对象可回收？

- **可达性分析**：从 GC Roots 出发不可达 → 可回收。
- 不是引用计数（无法很好处理循环引用）。
- GC Roots 例子：栈引用、静态变量、常量、JNI 等。

---

## 3. 常见 GC 算法 / 收集器？

- 算法：标记-清除、复制、标记-整理、分代。
- **Minor GC**：收新生代；**Full GC**：范围大、停顿通常更长。
- **G1**：Region 划分，优先回收收益高的区域，可设停顿目标（JDK 9+ 常见默认）。

---

## 4. 类加载过程？双亲委派？

- 加载 → 验证 → 准备 → 解析 → 初始化。
- **双亲委派**：先让父加载器加载，父没有再自己加载。
- **目的**：保证核心类唯一、安全。
- 可破坏：SPI、热部署、自定义 ClassLoader 等。

---

## 5. 线上排查（社招重点）

- **堆 OOM**：dump → MAT 看占用与引用链。
- **Full GC 频繁**：看回收后老年代是否下降；不降偏泄漏。
- **CPU 飙高**：找忙线程 → `jstack` 看栈。
- **容器被杀无 Java OOM**：RSS 超 cgroup，关注堆外。

---

## 自测

- [ ] 私有/共享区  
- [ ] 可达性  
- [ ] OOM / CPU 排查第一步  

**口述：** [JVM面渣级口述.md](./JVM面渣级口述.md) · **卡片：** [JVM卡片速记.md](./JVM卡片速记.md)
"""

SPRING = f"""# Spring · 知识点

{nav('Spring面渣级口述.md', 'Spring卡片速记.md', '路径-Java后端.md')}

---

## 1. IOC / DI？

- **IOC**：控制反转，对象创建与装配交给容器。
- **DI**：依赖注入（构造器 / setter / 字段），容器把依赖塞进来。
- 好处：解耦、易测、易替换实现。

---

## 2. AOP？代理？

- 面向切面：日志、事务、权限等横切逻辑。
- 实现：动态代理
  - 有接口：JDK 动态代理。
  - 无接口：CGLIB 子类代理。
- **自调用**（`this.xxx()`）不走代理 → 事务等注解可能失效。

---

## 3. @Transactional 失效常见原因？

- 同类内部调用。
- 方法非 public（视配置）。
- 异常被 catch 吞掉未抛出。
- 异常类型/传播行为配置不当。

---

## 4. Bean 作用域？循环依赖？

- 默认**单例**；还有 prototype、request、session 等。
- 单例循环依赖：三级缓存可解部分（字段/setter）；**构造器循环**通常无法解。

---

## 5. Spring Boot 自动配置？

- 条件注解 + starter + 自动配置类。
- classpath / 配置满足则自动装配 Bean。
- 出问题可查自动配置报告。

---

## 自测

- [ ] IOC/DI 一句话  
- [ ] 事务失效 3 条  
- [ ] JDK 代理 vs CGLIB  

**口述：** [Spring面渣级口述.md](./Spring面渣级口述.md) · **卡片：** [Spring卡片速记.md](./Spring卡片速记.md)
"""

LLM = f"""# LLM 基础 · 知识点

{nav('LLM基础常识面渣级口述.md', 'LLM基础常识卡片速记.md', '路径-AI工程.md')}

---

## 1. 什么是 Token？上下文窗口？

- **Token**：模型计费与长度的基本单位（子词片），≠ 字数。
- 中文往往更耗 token。
- **上下文窗口**：一次请求能塞下的上限（系统提示 + 历史 + 检索 + 问题 + 预留输出）。
- 超窗：摘要历史、减少检索条数/长度、截断旧轮。

---

## 2. 温度（temperature）？

- 控制生成随机性。
- **低**：更稳，适合抽字段、客服、工具参数。
- **高**：更发散，易跑偏。

---

## 3. 什么是幻觉？怎么压？

- 流畅但错误/捏造的内容。
- 原因：生成式预测下一 token，不是查权威库。
- 手段：RAG 引用、低相关拒答、校验、金标评测；不承诺永不幻觉。

---

## 4. 预训练 / SFT / RLHF？

- **预训练**：海量文本学语言与知识。
- **SFT**：指令数据学会听人话。
- **RLHF/偏好对齐**：更有用、更安全、更符合偏好。
- 知识常变 → 优先 **RAG**；风格/格式稳定 → 可考虑微调。

---

## 5. 云 API vs 私有化？

- 云：上线快、运维轻，数据出域风险。
- 私有：可控，要 GPU 与运维。
- 常混合 + 统一网关。

---

## 自测

- [ ] token / 窗口  
- [ ] 幻觉 3 条手段  
- [ ] 三阶段训练  

**口述：** [LLM基础常识面渣级口述.md](./LLM基础常识面渣级口述.md) · **卡片：** [LLM基础常识卡片速记.md](./LLM基础常识卡片速记.md)
"""

AGENT = f"""# Agent · 知识点

{nav('Agent面渣级口述.md', 'Agent卡片速记.md', '路径-AI工程.md')}

---

## 1. Agent 是什么？

- 目标驱动：多步推理 + 调工具 + 观察结果 + 再决策，直到完成或停止。
- 公式：`Agent ≈ LLM + 规划 + 记忆 + 工具 + 护栏`。

---

## 2. Chat / RAG / 工作流 / Agent 区别？

- **Chat**：生成回复。
- **RAG**：先检索再生成。
- **工作流**：步骤人写死，可控。
- **Agent**：步骤模型动态选，灵活但贵、难控。
- 步骤固定、强合规、成本敏感 → 优先工作流，不必上 Agent。

---

## 3. ReAct？

- Thought → Action → Observation 循环 → Final Answer。
- 风险：死循环、步数/费用爆炸。
- **护栏**：max_steps、重复检测、超时、工具错误熔断、危险操作 HITL。

---

## 4. 工具调用安全？

- 模型只输出调用意图；**服务端执行**。
- 参数校验、鉴权、最小权限、审计日志、写操作审批。

---

## 5. 记忆？

- 短时：对话窗口；长时：向量/DB；过长要摘要。

---

## 自测

- [ ] Agent 定义  
- [ ] 防死循环 5 条  
- [ ] 何时不用 Agent  

**口述：** [Agent面渣级口述.md](./Agent面渣级口述.md) · **卡片：** [Agent卡片速记.md](./Agent卡片速记.md)
"""

PYTHON = f"""# Python · 知识点

{nav('Python面渣级口述.md', 'Python卡片速记.md', '路径-Python.md')}

---

## 1. 可变 / 不可变？默认参数陷阱？

- 可变：list、dict、set；不可变：int、str、tuple 等。
- **坑**：`def f(a=[])` 的默认 list 在**定义时创建一次**，多次调用共享。
- **改法**：`def f(a=None):`，函数内 `if a is None: a = []`。

---

## 2. 装饰器 / 生成器 / with？

- **装饰器**：函数增强（日志、鉴权、重试）；`@wraps` 保留元数据。
- **生成器**：`yield` 惰性产出，省内存。
- **with**：上下文管理，保证资源释放。

---

## 3. GIL？线程 / 进程 / asyncio 怎么选？

- CPython：**同一时刻通常一个线程执行 Python 字节码**（简化理解）。
- **CPU 密集**：多进程 / 外部计算。
- **IO 密集**：多线程或 asyncio。
- **高并发等网络（调 LLM）**：asyncio + 异步客户端。
- **禁忌**：async 里 `time.sleep` / 同步 `requests` 会堵事件循环。

---

## 4. 浅拷贝 / 深拷贝？

- 浅：只复制外层；深：递归复制内部对象。

---

## 自测

- [ ] 默认参数陷阱  
- [ ] GIL 选型三句话  
- [ ] async 不能阻塞  

**口述：** [Python面渣级口述.md](./Python面渣级口述.md) · [FastAPI](./Python异步与FastAPI面渣级口述.md) · **卡片：** [Python卡片速记.md](./Python卡片速记.md)
"""

OS = f"""# 操作系统 · 知识点

{nav('操作系统面渣级口述.md', '操作系统卡片速记.md', '路径-Java后端.md')}

---

## 1. 进程 vs 线程？

- **进程**：资源分配单位，独立地址空间，隔离强。
- **线程**：调度单位，同进程共享堆/全局，需同步。
- Java 线程共享堆 → 要考虑线程安全。

---

## 2. 用户态 / 内核态？系统调用？

- 用户程序不能直接访问硬件 → 通过系统调用进内核。
- 切换有成本 → 缓冲、批量、多路复用、零拷贝。

---

## 3. 上下文切换？

- 保存/恢复寄存器等运行现场；过多切换浪费 CPU。

---

## 4. epoll？

- IO 多路复用：一线程监听多连接，处理就绪连接。
- 相对 select/poll：更适合海量连接（就绪队列）。
- Nginx / Redis / Netty 等使用同类思想。

---

## 5. 死锁？

- 四条件：互斥、占有且等待、不可抢占、循环等待。
- 预防：固定加锁顺序、超时、减少锁粒度等。

---

## 6. 虚拟内存？

- 页表映射虚拟地址到物理内存；缺页慢。
- 容器还受 cgroup 限制。

---

## 自测

- [ ] 进程线程  
- [ ] epoll 一句话  
- [ ] 死锁四条件  

**口述：** [操作系统面渣级口述.md](./操作系统面渣级口述.md) · **卡片：** [操作系统卡片速记.md](./操作系统卡片速记.md)
"""

NET = f"""# 计算机网络 · 知识点

{nav('计算机网络面渣级口述.md', '计算机网络卡片速记.md', '路径-Java后端.md')}

---

## 1. TCP vs UDP？

- **TCP**：面向连接、可靠、有序；适合 HTTP、DB、RPC。
- **UDP**：无连接、快、不保证可靠；适合 DNS、音视频等。

---

## 2. 三次握手？为什么是三次？

- SYN → SYN+ACK → ACK。
- 同步双方初始序列号；确认双向通信能力。
- 三次有助于处理历史延迟 SYN，降低错误连接风险。

---

## 3. 四次挥手？TIME_WAIT？CLOSE_WAIT？

- 全双工分别关闭 → 常四次挥手。
- **TIME_WAIT**（主动关闭方，约 2MSL）：保证最后 ACK；让旧包过期。
- **CLOSE_WAIT 多**：应用未 close。

---

## 4. HTTPS？

- HTTP + TLS。
- 证书验证身份；非对称换钥 + 对称加密传数据。

---

## 5. 常见状态码？

- **401**：未认证；**403**：无权限。
- **502**：上游异常；**504**：上游超时。

---

## 6. TCP 粘包 / 拆包？

- TCP 是字节流，无消息边界。
- 解决：长度前缀、分隔符、定长。

---

## 自测

- [ ] 握手为何三次  
- [ ] TIME_WAIT 两点  
- [ ] 401 vs 403  

**口述：** [计算机网络面渣级口述.md](./计算机网络面渣级口述.md) · **卡片：** [计算机网络卡片速记.md](./计算机网络卡片速记.md)
"""


def short(title, oral, card, path, items: str):
    return f"""# {title} · 知识点

{nav(oral, card, path)}

{items}

**口述：** [{oral}](./{oral}) · **卡片：** [{card}](./{card})
"""


def main():
    mapping = {
        "Java高频面试题与知识点.md": JAVA,
        "Redis高频面试题与知识点.md": REDIS,
        "MySQL高频面试题与知识点.md": MYSQL,
        "并发高频面试题与知识点.md": CONC,
        "JVM高频面试题与知识点.md": JVM,
        "Spring高频面试题与知识点.md": SPRING,
        "LLM基础常识高频面试题与知识点.md": LLM,
        "Agent高频面试题与知识点.md": AGENT,
        "Python高频面试题与知识点.md": PYTHON,
        "操作系统高频面试题与知识点.md": OS,
        "计算机网络高频面试题与知识点.md": NET,
        "AI应用工程高频面试题与知识点.md": short(
            "AI 应用工程",
            "AI应用工程面渣级口述.md",
            "AI应用工程卡片速记.md",
            "路径-AI工程.md",
            """
## 1. 典型链路？

- 接入（鉴权/限流）→ 编排（RAG/工作流/Agent）→ 模型网关 → 后处理 → 观测/计费。

## 2. 流式？超时？幂等？

- **流式**：改善首字等待体感。
- **超时**：分层超时 + 降级。
- **幂等**：防重试重复扣费/副作用。
""",
        ),
        "向量库高频面试题与知识点.md": short(
            "向量库",
            "向量库面渣级口述.md",
            "向量库卡片速记.md",
            "路径-AI工程.md",
            """
## 1. Embedding / 向量检索？

- 文本→向量；语义近则距离近。
- ANN（如 HNSW）：近似近邻，换精度换速度。

## 2. Chunk 切块？

- 控制窗口与噪声；过大嘈杂、过小缺上下文；可重叠。

## 3. 混合检索？

- 向量 + 关键词（专有名词/编号）。
""",
        ),
        "Prompt工程高频面试题与知识点.md": short(
            "Prompt 工程",
            "Prompt工程面渣级口述.md",
            "Prompt工程卡片速记.md",
            "路径-AI工程.md",
            """
## 1. 系统提示？

- 角色、边界、输出格式。

## 2. 少样本 / 结构化？

- 给例子稳格式；JSON/Schema 便于解析。
- 模板变量化，可测可版本管理。
""",
        ),
        "Prompt注入与AI安全高频面试题与知识点.md": short(
            "Prompt 注入与 AI 安全",
            "Prompt注入与AI安全面渣级口述.md",
            "Prompt注入与AI安全卡片速记.md",
            "路径-AI工程.md",
            """
## 1. 注入？

- 不可信输入篡改模型目标；间接注入藏在检索文档中。

## 2. 防御？

- 最小权限、参数校验、写操作审批、过滤与监控、审计；不单靠提示词约束。
""",
        ),
        "AI评测金标SOP高频面试题与知识点.md": short(
            "AI 评测",
            "AI评测金标SOP面渣级口述.md",
            "AI评测金标SOP卡片速记.md",
            "路径-AI工程.md",
            """
## 1. 金标集？

- 固定题目 + 标准/rubric；改 prompt/模型必回归。

## 2. 指标？

- 正确率、拒答是否合适、延迟、token/成本。
""",
        ),
        "LangGraph与自研工作流高频面试题与知识点.md": short(
            "LangGraph / 工作流",
            "LangGraph与自研工作流面渣级口述.md",
            "LangGraph与自研工作流卡片速记.md",
            "路径-AI工程.md",
            """
## 1. 图工作流？

- 节点 + 边 + 状态；比纯 while-Agent 更可控、可恢复、可人工节点。
""",
        ),
        "LLMOps与可观测高频面试题与知识点.md": short(
            "LLMOps",
            "LLMOps与可观测面渣级口述.md",
            "LLMOps与可观测卡片速记.md",
            "路径-AI工程.md",
            """
## 1. 要什么？

- prompt/模型版本、灰度回滚、评测门禁。
- 指标：延迟、token、错误率、质量、成本；请求级 trace。
""",
        ),
        "推理部署高频面试题与知识点.md": short(
            "推理部署",
            "推理部署面渣级口述.md",
            "推理部署卡片速记.md",
            "路径-AI工程.md",
            """
## 1. 要点？

- 吞吐 vs 延迟（batch 权衡）。
- 量化省显存；KV cache 加速解码；限流与多副本。
""",
        ),
        "Docker与K8s高频面试题与知识点.md": short(
            "Docker / K8s",
            "Docker与K8s面渣级口述.md",
            "Docker与K8s卡片速记.md",
            "路径-Java后端.md",
            """
## 1. 镜像 vs 容器？

- 镜像：只读模板；容器：运行实例。

## 2. K8s 核心？

- Pod、Deployment、Service；liveness/readiness；request/limit。
""",
        ),
        "MQ高频面试题与知识点.md": short(
            "MQ",
            "MQ面渣级口述.md",
            "MQ卡片速记.md",
            "路径-Java后端.md",
            """
## 1. 为什么用 MQ？

- 解耦、削峰、异步。

## 2. 至少一次？

- 可能重复 → 消费幂等。
- 积压：扩消费者、降耗时、限速生产。
""",
        ),
        "Kafka高频面试题与知识点.md": short(
            "Kafka",
            "Kafka面渣级口述.md",
            "Kafka卡片速记.md",
            "路径-Java后端.md",
            """
## 1. 分区 / 消费组 / offset？

- 分区：并行单位；同 key 可保序。
- Consumer Group：组内分摊分区。
- offset：消费进度；ISR/ack 影响可靠性。
""",
        ),
        "RocketMQ高频面试题与知识点.md": short(
            "RocketMQ",
            "RocketMQ面渣级口述.md",
            "RocketMQ卡片速记.md",
            "路径-Java后端.md",
            """
## 1. 事务消息 / 延时消息？

- 事务：半消息 + 本地事务回查。
- 延时：定时/延时投递场景。
""",
        ),
        "微服务与分布式高频面试题与知识点.md": short(
            "微服务与分布式",
            "微服务与分布式面渣级口述.md",
            "微服务与分布式卡片速记.md",
            "路径-Java后端.md",
            """
## 1. 拆分？代价？

- 独立部署扩展 vs 分布式复杂。
- 幂等、超时重试、熔断限流、可观测。
""",
        ),
        "设计模式高频面试题与知识点.md": short(
            "设计模式",
            "设计模式面渣级口述.md",
            "设计模式卡片速记.md",
            "路径-Java后端.md",
            """
## 1. 常考？

- 单例、工厂、策略、模板方法、观察者、代理（AOP 同源）。
- 讲场景与权衡，不背类图。
""",
        ),
        "算法高频面试题与知识点.md": short(
            "算法",
            "算法面渣级口述.md",
            "算法卡片速记.md",
            "路径-Java后端.md",
            """
## 1. 怎么答？

- 先分类（双指针/哈希/树/BFS/DP…）→ 思路 → 复杂度 → 代码。
""",
        ),
        "后端系统设计高频面试题与知识点.md": short(
            "系统设计",
            "后端系统设计面渣级口述.md",
            "后端系统设计卡片速记.md",
            "路径-Java后端.md",
            """
## 1. 步骤？

- 需求量级 → API → 存储 → 主路径 → 缓存/异步/分片 → 降级。
""",
        ),
        "行为面试高频题与知识点.md": short(
            "行为面试",
            "行为面试面渣级口述.md",
            "行为面试卡片速记.md",
            "路径-Java后端.md",
            """
## 1. STAR？

- Situation / Task / Action / Result（最好量化）。
""",
        ),
        "项目面试STAR高频题与知识点.md": short(
            "项目 STAR",
            "项目面试面渣级口述.md",
            "项目面试STAR卡片速记.md",
            "路径-Java后端.md",
            """
## 1. 项目深挖？

- 角色、难点、方案对比、你的动作、数据结果、复盘。
""",
        ),
    }
    for k, v in mapping.items():
        w(k, v)


if __name__ == "__main__":
    main()
