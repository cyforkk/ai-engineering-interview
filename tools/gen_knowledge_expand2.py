# -*- coding: utf-8 -*-
"""二次加厚：把过短的知识点详解补到可用深度。不碰面渣。"""
from pathlib import Path

DOCS = Path(__file__).resolve().parents[1] / "docs"


def w(name, text):
    p = DOCS / name
    p.write_text(text.strip() + "\n", encoding="utf-8")
    print(name, p.stat().st_size)


def nav(oral, card, path):
    return f"""<!-- NAV:START -->
> **三分法** · 📖详解 · 🗣️[面渣](./{oral}) · 🃏[卡片](./{card})
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md) · [路径](./{path})
>
<!-- NAV:END -->
"""


# ---------- 并发（重点加厚）----------
CONC = f"""# Java 并发 · 知识点详解（所以然）

{nav('并发面渣级口述.md','并发卡片速记.md','路径-Java后端.md')}

> 本文讲**为什么**这样设计、机制如何保证正确性。口语练嘴见面渣；考前回忆见卡片。

---

## 一、并发到底在解决什么问题

单核也能「并发」（时间片轮转），多核可以「并行」。难点不在「开线程」，而在：

1. **原子性**：多步操作中间被打断会不会看到半成品  
2. **可见性**：A 线程写的值 B 能否及时看到  
3. **有序性**：指令重排后语义是否仍符合程序员直觉  

CPU 缓存、编译器优化都是为了**快**；JMM（Java 内存模型）用规则告诉你：在什么同步手段下，跨线程语义可预期。

---

## 二、happens-before：可见性的「法律条文」

若没有规范，编译器/CPU 可以在单线程语义不变的前提下重排指令；但对多线程，重排可能让 B 永远看不到 A 的写。

**happens-before（hb）**：若操作 A hb 操作 B，则 A 的写对 B 可见，且 A 的顺序在 B 之前（从内存语义角度）。

常见建立 hb 的方式：

| 规则 | 含义 |
|------|------|
| 程序次序 | 单线程内前面的 hb 后面的 |
| 监视器锁 | unlock hb 后续对同一锁的 lock |
| volatile | 写 hb 后续对同一变量的读 |
| 线程 start/join | start 前写对线程内可见；join 后线程内写对 join 后可见 |
| 传递性 | A hb B 且 B hb C ⇒ A hb C |

**所以然：** 你用的 `synchronized`/`volatile`/`concurrent` 工具，底层都在建立 hb，而不是「魔法让变量同步」。

---

## 三、volatile：能做什么、不能做什么

### 机制直觉

写 volatile：插入内存屏障，把工作内存刷新到主内存相关可见路径。  
读 volatile：使后续读不会用过期缓存值（简化理解）。

### 为什么不保证 i++

`i++` 是读-改-写三步。两线程可交错成丢更新。volatile 只保证单次读写的可见与有序，**不加锁就不给复合操作原子性**。

### 适用

- 状态标志：`shutdown = true`  
- 安全发布（配合正确双检锁模式中的实例引用）  
- 一个线程写、多线程读的简单发布  

---

## 四、synchronized 与 Lock

### synchronized 为什么能保证可见+原子

进入/退出监视器时建立 hb：解锁前的写对后续加锁的线程可见；互斥保证临界区不交叉执行。

锁升级（偏向→轻量→重量）是 JVM 实现优化，面试能提「自适应降低无竞争开销」，细节不必背字节码级别。

### ReentrantLock / AQS

显式锁提供：可中断、tryLock 超时、公平可选、多个 Condition。  
**AQS：** 用 `state` 表示同步状态 + CLH 风格等待队列。子类实现 tryAcquire/tryRelease，队列阻塞唤醒复用。  
**所以然：** 锁、信号量、CountDownLatch 结构相似，抽公共骨架避免重复。

可重入：同一线程再次加锁 `state++`，防止自己堵自己。

---

## 五、线程池：每个参数的存在理由

| 参数 | 所以然 |
|------|--------|
| corePoolSize | 常驻线程，承接稳态负载，避免反复创建 |
| maximumPoolSize | 突发时上限，防止无限创建打爆内存 |
| workQueue | 缓冲任务；**有界**才能把压力传导到拒绝策略 |
| keepAliveTime | 非核心线程空闲回收，省资源 |
| handler | 系统过载时明确失败/降级，而不是静默丢光 |

### 提交顺序为什么是 core → 队列 → max

先希望用核心线程稳稳干活；忙不过来先排队（吸收毛刺）；队列也满再扩到 max；再满则拒绝。  
若队列**无界**，任务永远进队，max 线程可能永远不创建，最后 OOM——这是经典误配。

### 拒绝策略

- Abort：抛异常，调用方感知  
- CallerRuns：调用线程自己跑，反向施压  
- Discard / DiscardOldest：丢弃，要业务能接受  

---

## 六、ThreadLocal

每个线程一个副本，避免传参与共享。  
**泄漏原因：** 线程池线程不销毁，ThreadLocalMap 条目长期活着；value 占大对象。必须 `try/finally remove`。

---

## 七、CAS 与原子类

CAS：CPU 级比较交换，无锁更新。失败则重试（自旋）。  
ABA：值从 A→B→A，CAS 以为没变；用版本号或 `AtomicStampedReference`。  
`AtomicInteger` 等封装 CAS，适合热点计数；竞争极高时再考虑 LongAdder 分段。

---

## 八、自测

- [ ] 能解释 hb 与「为什么需要 volatile/锁」  
- [ ] volatile 对 i++ 无效的原因  
- [ ] 线程池参数与误配无界队列  
- [ ] ThreadLocal+线程池泄漏  

**面渣：** [并发面渣级口述.md](./并发面渣级口述.md) · **卡片：** [并发卡片速记.md](./并发卡片速记.md)
"""

JVM = f"""# JVM · 知识点详解（所以然）

{nav('JVM面渣级口述.md','JVM卡片速记.md','路径-Java后端.md')}

---

## 一、为什么要分运行时区域

JVM 规范把内存按**生命周期与共享性**切开，便于：线程隔离、GC 范围清晰、安全与调试。

| 区域 | 设计动机 |
|------|----------|
| PC + 栈 | 调用与字节码位置是线程私有状态，切开最自然 |
| 堆 | 对象跨方法、跨线程共享，必须集中管理与回收 |
| 元空间 | 类元数据与堆对象生命周期不同；移出永久代避免「类太多把堆配参搞崩」且用本地内存更灵活 |
| 直接内存 | NIO 减少一次用户态拷贝路径，但别忘了也占机器/容器额度 |

### 分代假说

经验：多数对象活不久。新生代用**复制算法**（活的少，拷走便宜）；老年代活对象多，用标记-整理/清除更合适。G1 进一步按 Region 回收「性价比高」的垃圾区，服务停顿目标。

---

## 二、GC 判断：可达性

引用计数遇环失败（A↔B）。可达性从 GC Roots 出发，标不到的就是垃圾。  
Roots 包括：栈上引用、静态变量、JNI、持有的锁等——即「程序还可能碰到的入口」。

Minor GC 勤而相对快；Full GC 贵。频繁 Full 要问：是分配速率、晋升失败、碎片，还是泄漏（回收后老年代几乎不降）。

---

## 三、类加载与双亲委派

加载→链接（验证/准备/解析）→初始化。  
双亲委派：**先让父加载器尝试**，保证 `java.lang.*` 等由启动类加载器加载，防核心类被篡改。  
破坏双亲用于 SPI、热部署、容器隔离——是有场景的例外，不是默认。

---

## 四、排查路径的所以然

```text
现象时间线 → 指标（GC、CPU、内存、流量）→ 工具（jstat/jstack/dump）→ 根因 → 止血+根治
```

| 场景 | 为何这么查 |
|------|------------|
| 堆 OOM | dump 看谁占、引用链是谁让它活着 |
| Full GC 频繁 | 看回收后 Old 是否回落；不回落像泄漏 |
| CPU 高 | 找到热线程栈，才知道是死循环还是锁争用 |
| 容器被杀 | cgroup 看的是 RSS 等，不只是 -Xmx |

**场景长答：** [JVM面渣级口述.md](./JVM面渣级口述.md) · **卡片：** [JVM卡片速记.md](./JVM卡片速记.md)
"""

# Expand secondary topics with more 所以然
VEC = f"""# 向量库与检索 · 知识点详解（所以然）

{nav('向量库面渣级口述.md','向量库卡片速记.md','路径-AI工程.md')}

---

## 一、为什么需要向量检索

关键词检索依赖字面匹配：用户说「怎么退货」，文档写「售后流程」可能搜不到。  
Embedding 把文本映射到高维向量，**语义相近 ⇒ 向量距离近**，从而用近邻搜索找到相关段落。

这不是魔法：同义、改写、多语言在训练目标下被拉近；专有名词、极短 query、数据噪声仍会失手，所以要工程兜底。

---

## 二、索引：为何常用 ANN 而不是精确暴力

暴力对每个向量算距离：数据量×维度上来后延迟不可接受。  
ANN（近似最近邻）如 **HNSW**（分层导航小世界图）：在可接受的召回损失下换数量级加速。  
业务要调：`topK`、ef 搜索宽度、是否重排（用更强模型精排）。

---

## 三、Chunk（切块）的所以然

1. **窗口预算**：检索片段要塞进 LLM 上下文  
2. **噪声控制**：整章塞入会稀释主题，模型抓不住重点  
3. **粒度权衡**：块过大 → 嘈杂；过小 → 缺上下文、实体被切断  

实践：按标题/段落/语义切；适当重叠（overlap）减少边界切断；元数据带文档名、章节便于引用。

---

## 四、混合检索为什么常常更好

| 通路 | 擅长 |
|------|------|
| 向量 | 同义、语义 |
| 关键词（BM25 等） | 订单号、错误码、专有名词精确命中 |

融合（加权/RRF）后精排，能同时覆盖「说人话」和「搜编号」。

---

## 五、召回差时的排查顺序

1. 语料是否切错、是否过期  
2. Embedding 模型是否匹配语言与领域  
3. topK 是否过小；是否该上 hybrid  
4. query 改写/HyDE 是否需要  
5. 精排与阈值：低相关宁可不答（降幻觉）

**面渣：** [向量库面渣级口述.md](./向量库面渣级口述.md) · **卡片：** [向量库卡片速记.md](./向量库卡片速记.md)
"""

SAFE = f"""# Prompt 注入与 AI 安全 · 知识点详解（所以然）

{nav('Prompt注入与AI安全面渣级口述.md','Prompt注入与AI安全卡片速记.md','路径-AI工程.md')}

---

## 一、攻击面在哪里

LLM 应用把**不可信文本**与**系统指令、工具权限**放在同一上下文。攻击者通过用户输入或「间接注入」（网页/邮件/上传文档被检索进来）试图：

- 覆盖系统目标（忽略以上指令…）  
- 套取系统提示、密钥、他人数据  
- 诱使模型调用危险工具（转账、删库、外发）

**所以然：** 模型会「听话」地跟从上下文中的指令式文本；不能把安全只押在「模型懂规矩」。

---

## 二、为什么「提示里写禁止」不够

提示是软约束，可被更强诱导、编码绕过、多轮渗透削弱。必须**系统层硬控制**：

| 层 | 手段 |
|----|------|
| 权限 | 工具最小权限；默认只读 |
| 执行 | 写操作 HITL；参数白名单 |
| 数据 | 租户隔离；脱敏；出域审批 |
| 检测 | 输入输出过滤、异常工具调用告警 |
| 运维 | 审计日志、一键熔断 |

---

## 三、间接注入为什么更难防

内容来自检索，像「正常知识」进入上下文。防御：来源标记、不信任文档中的指令语气、工具调用不因文档要求而提权、关键操作二次确认。

**面渣：** [Prompt注入与AI安全面渣级口述.md](./Prompt注入与AI安全面渣级口述.md) · **卡片：** [Prompt注入与AI安全卡片速记.md](./Prompt注入与AI安全卡片速记.md)
"""

AI_APP = f"""# AI 应用工程 · 知识点详解（所以然）

{nav('AI应用工程面渣级口述.md','AI应用工程卡片速记.md','路径-AI工程.md')}

---

## 一、为什么要「应用工程」而不只是调 API

直接把前端接到模型：无法限流、无法审计、无法换模型、无法评测回归、无法处理超时与降级。  
工程化是把 LLM 当成**不可靠的高延迟依赖服务**来做架构。

---

## 二、推荐链路及每段职责

```text
客户端 → 接入层(鉴权/限流/配额)
      → 编排(工作流/Agent/RAG)
      → 模型网关(路由/超时/重试/降级)
      → 后处理(格式/安全/引用)
      → 观测(trace/log/metrics) + 计费
```

| 段 | 所以然 |
|----|--------|
| 接入 | 防止被刷爆账单与下游 |
| 编排 | 业务步骤可测可改 |
| 网关 | 模型可替换；统一超时 |
| 后处理 | 模型输出不可直接信 |
| 观测 | 没有数据就无法优化与追责 |

---

## 三、流式、超时、幂等

- **流式：** 改善 TTFT 体感；注意取消与半截输出处理  
- **超时：** 分层（网关 < 上游预算）；超时要有降级文案或缓存答案  
- **幂等：** 客户端重试常见；防止重复扣费、重复写业务  

**面渣：** [AI应用工程面渣级口述.md](./AI应用工程面渣级口述.md) · **卡片：** [AI应用工程卡片速记.md](./AI应用工程卡片速记.md)
"""

PYTHON = f"""# Python · 知识点详解（所以然）

{nav('Python面渣级口述.md','Python卡片速记.md','路径-Python.md')}

---

## 一、对象模型：可变与默认参数

一切皆对象。可变对象可原地改；不可变「改」往往产生新对象。  

**默认参数在定义时绑定**：`def f(a=[])` 的 `[]` 只建一次，多次调用共享 → 串数据。  
所以然：这是语言语义，不是偶然 bug。用 `None` 作哨兵，函数体内新建。

浅拷贝只复制容器外壳；嵌套可变对象仍共享 → 需深拷贝才独立。

---

## 二、装饰器 / 生成器 / with

| 特性 | 解决什么问题 |
|------|----------------|
| 装饰器 | 无侵入增加日志、鉴权、重试；`wraps` 保留元数据便于调试 |
| 生成器 | 惰性产出，大文件/流式数据不一次进内存 |
| with | 异常路径也执行清理，消灭漏关文件/锁 |

---

## 三、GIL 与并发选型（核心）

CPython GIL：同进程同时多一线程执行 Python 字节码，简化内存管理。

| 负载 | 策略 | 所以然 |
|------|------|--------|
| CPU 密集 | 多进程 / 外部计算服务 | 多线程吃不满多核 |
| IO 密集 | 线程或 asyncio | 等待时释放 GIL 或让出循环 |
| 高并发 HTTP/LLM | asyncio + 异步客户端 | 单线程交错大量等待 |

**asyncio 禁忌：** `time.sleep`、同步 `requests`、重 CPU 循环会**堵死事件循环**，所有协程一起卡。应用 `asyncio.sleep`、httpx async、进程池卸载计算。

FastAPI：类型注解 + async + OpenAPI，适合 AI 网关；仍需自己做超时、限流、连接池与观测。详见 [Python异步与FastAPI面渣级口述.md](./Python异步与FastAPI面渣级口述.md)。

**卡片：** [Python卡片速记.md](./Python卡片速记.md) · **面渣：** [Python面渣级口述.md](./Python面渣级口述.md)
"""

OS = f"""# 操作系统 · 知识点详解（所以然）

{nav('操作系统面渣级口述.md','操作系统卡片速记.md','路径-Java后端.md')}

---

## 一、进程与线程

**进程**是资源分配与隔离单位（独立地址空间）。**线程**是调度单位，同进程共享堆/全局，通信便宜但要同步。  
Java 业务线程共享堆 → 集合线程安全问题来自 OS/运行时模型，不是集合库「故意坑你」。

---

## 二、用户态 / 内核态 / 系统调用

用户程序不能直接碰硬件与内核关键结构。syscall 陷入内核：有切换成本。  
高性能路径：缓冲、批量 IO、mmap、epoll、零拷贝——都在**减少切换与拷贝次数**。

---

## 三、epoll

C10K 问题：一连接一线程扛不住。IO 多路复用让一线程监听多 fd。  
select/poll：每次携带并扫描大量 fd。epoll：就绪队列，适合连接多、活跃子集相对有限的服务。Nginx/Redis/Netty 同源思想。

---

## 四、死锁与虚拟内存

死锁四条件缺一不可同时成立；破其一即可预防（顺序加锁、超时等）。  
虚拟内存用页表隔离与超卖；缺页走磁盘极慢。容器 limit 看的是物理占用，不只是进程「虚拟很大」。

**面渣（已加长）：** [操作系统面渣级口述.md](./操作系统面渣级口述.md) · **卡片：** [操作系统卡片速记.md](./操作系统卡片速记.md)
"""

NET = f"""# 计算机网络 · 知识点详解（所以然）

{nav('计算机网络面渣级口述.md','计算机网络卡片速记.md','路径-Java后端.md')}

---

## 一、TCP 与 UDP 的取舍

TCP 用连接状态、确认、重传、排序、拥塞控制换**可靠有序**。  
UDP 极简，把可靠性交给应用——实时音视频宁可丢包也不愿卡死。选型看业务能否容忍丢与乱序。

---

## 二、三次握手 / 四次挥手

握手同步初始序号并确认双向通信；三次有助于处理延迟的旧 SYN，降低错误连接。  
挥手因全双工需分别关闭两端；TIME_WAIT（2MSL）保护最后 ACK 并清空旧报文；CLOSE_WAIT 堆积多查应用是否漏 close。

---

## 三、HTTPS

TLS 提供加密与身份（证书链）。非对称交换密钥，对称加密传数据——性能与安全折中。

## 四、状态码与粘包

401 未认证 / 403 无权限；502 上游坏 / 504 上游超时——反代场景高频。  
TCP 字节流无消息边界 → 应用层定长/长度头/分隔符。

**面渣（已加长）：** [计算机网络面渣级口述.md](./计算机网络面渣级口述.md) · **卡片：** [计算机网络卡片速记.md](./计算机网络卡片速记.md)
"""

AGENT = f"""# Agent · 知识点详解（所以然）

{nav('Agent面渣级口述.md','Agent卡片速记.md','路径-AI工程.md')}

---

## 一、定义与边界

Agent = 目标驱动 + 多步决策 + 工具调用 + 观察反馈 + 停止/护栏。  
不是「更长的 Chat」，而是可能**改变外部世界**（写库、发消息、花钱）的系统——因此工程中心是**可控性**。

---

## 二、与工作流 / RAG 的分工

| 模式 | 决策者 | 适合 |
|------|--------|------|
| 工作流 | 人事先写死 | 合规、稳定 SOP |
| RAG | 检索决定知识 | 知识问答 |
| Agent | 模型动态选工具与步骤 | 探索性、工具组合事先难枚举 |

生产常 **工作流包 Agent 局部能力**：外层可控，内层灵活。

---

## 三、ReAct 与护栏

Thought→Action→Observation 让模型基于真实反馈调整。  
无护栏会：死循环、账单爆炸、危险副作用。  
**max_steps / 去重 / 超时 / 熔断 / HITL** 是上线必要条件。

## 四、工具调用安全

模型输出意图，**服务端执行**。鉴权、参数校验、最小权限、审计日志不可省。

**面渣：** [Agent面渣级口述.md](./Agent面渣级口述.md) · **卡片：** [Agent卡片速记.md](./Agent卡片速记.md)
"""

LLM = f"""# LLM 基础常识 · 知识点详解（所以然）

{nav('LLM基础常识面渣级口述.md','LLM基础常识卡片速记.md','路径-AI工程.md')}

---

## 一、Token 与上下文

模型按 token 读写与计费。中文往往更耗 token。窗口是硬预算：系统提示+历史+检索+问题+输出预留。  
超窗工程手段：摘要、限 topK、截断旧轮——都是**资源分配**问题。

---

## 二、温度与幻觉

温度调控采样随机性：事实任务宜低。  
幻觉源于生成式目标（预测下一 token）而非查库；压风险靠 RAG、拒答、校验、评测，不靠幻想「完美模型」。

---

## 三、训练阶段

预训练学语言与知识；SFT 学听指令；偏好对齐学「更有用更安全」。  
知识日更优先 RAG；稳定风格与格式才考虑微调。

**面渣：** [LLM基础常识面渣级口述.md](./LLM基础常识面渣级口述.md) · **卡片：** [LLM基础常识卡片速记.md](./LLM基础常识卡片速记.md)
"""

SPRING = f"""# Spring · 知识点详解（所以然）

{nav('Spring面渣级口述.md','Spring卡片速记.md','路径-Java后端.md')}

---

## 一、IOC/DI

对象自己 `new` 依赖 → 高层绑死实现，难测难换。容器负责创建与装配（IOC），通过构造器等注入（DI）→ 面向接口、可 mock。

---

## 二、AOP 与事务

横切逻辑（事务/日志/鉴权）用代理织入。JDK 代理接口、CGLIB 子类。  
`this.xxx()` 自调用不经代理 → `@Transactional` 失效——机制必然。异常被吞、非 public、传播配置错误同理。

---

## 三、循环依赖

单例字段/setter 注入可通过三级缓存暴露早期引用；**构造器循环**创建都完成不了，仍失败。理解边界比背字段名重要。

## 四、Boot 自动配置

条件注解 + starter：classpath 与配置满足则装配。约定大于配置，出问题看自动配置报告。

**面渣：** [Spring面渣级口述.md](./Spring面渣级口述.md) · **卡片：** [Spring卡片速记.md](./Spring卡片速记.md)
"""

MQ = f"""# 消息队列 · 知识点详解（所以然）

{nav('MQ面渣级口述.md','MQ卡片速记.md','路径-Java后端.md')}

---

## 一、为什么引入 MQ

同步调用把峰值与故障直接串到整条链路。MQ 提供：

- **解耦：** 生产者不关心消费者是谁  
- **削峰：** 峰值进队列，消费者按能力消化  
- **异步：** 非关键路径后置  

代价：最终一致、重复消息、顺序与积压、运维复杂度。

---

## 二、投递语义

网络与重试导致**至少一次**最常见 ⇒ 消费端必须**幂等**（唯一键、状态机）。  
恰好一次昂贵，多靠幂等逼近。顺序：单分区/单队列键保序，并行与顺序矛盾要权衡。

---

## 三、积压治理

扩容消费者、优化消费耗时、暂时限流生产、冷热分流。先观测积压曲线再动手。

**Kafka/RMQ 卡片与面渣见路径表。**
"""

MS = f"""# 微服务与分布式 · 知识点详解（所以然）

{nav('微服务与分布式面渣级口述.md','微服务与分布式卡片速记.md','路径-Java后端.md')}

---

## 一、拆分动机

独立部署、独立扩缩、团队并行。代价：分布式事务、网络不可靠、可观测变难。按**业务边界**拆，避免分布式大单体。

---

## 二、CAP 与一致性

分区发生时，在可用性与强一致间权衡。互联网多最终一致 + 补偿。  
幂等、重试、超时、熔断、限流是标配，不是加分项。

---

## 三、分布式事务常见路

本地消息表、事务消息、Saga、TCC——都是在「不要两阶段卡死全局」的前提下换方案复杂度。选最简单能满足业务的。
"""

# shorter but better than 800 bytes
def mid(title, oral, card, path, body):
    return f"""# {title} · 知识点详解（所以然）

{nav(oral, card, path)}

{body}

**面渣：** [{oral}](./{oral}) · **卡片：** [{card}](./{card})
"""


def main():
    files = {
        "并发高频面试题与知识点.md": CONC,
        "JVM高频面试题与知识点.md": JVM,
        "向量库高频面试题与知识点.md": VEC,
        "Prompt注入与AI安全高频面试题与知识点.md": SAFE,
        "AI应用工程高频面试题与知识点.md": AI_APP,
        "Python高频面试题与知识点.md": PYTHON,
        "操作系统高频面试题与知识点.md": OS,
        "计算机网络高频面试题与知识点.md": NET,
        "Agent高频面试题与知识点.md": AGENT,
        "LLM基础常识高频面试题与知识点.md": LLM,
        "Spring高频面试题与知识点.md": SPRING,
        "MQ高频面试题与知识点.md": MQ,
        "微服务与分布式高频面试题与知识点.md": MS,
        "AI评测金标SOP高频面试题与知识点.md": mid(
            "AI 评测金标 SOP",
            "AI评测金标SOP面渣级口述.md",
            "AI评测金标SOP卡片速记.md",
            "路径-AI工程.md",
            """## 为什么必须评测

生成式输出不稳定；改 prompt/模型/温度可能「手感变好、指标变差」。没有固定金标集就无法回归。

## 金标集怎么建

覆盖主路径、边界、对抗与拒答；每题有标准答案或评分 rubric；版本化，改集要记录。

## 离线 vs 在线

离线：可控对比、发版门槛。在线：真实分布、漂移监控。人评要校准一致性。

## 指标

质量（正确、部分正确、有害）、业务（转化）、系统（延迟、token、成本）一起看，避免唯延迟论。
""",
        ),
        "Prompt工程高频面试题与知识点.md": mid(
            "Prompt 工程",
            "Prompt工程面渣级口述.md",
            "Prompt工程卡片速记.md",
            "路径-AI工程.md",
            """## 系统提示

放角色、边界、输出合同。安全红线不能只靠用户消息。

## 少样本与结构化

例子稳定格式；JSON/Schema 降低解析成本。变量模板化才能单测与版本管理。

## 思维链

复杂推理可分步；对用户暴露时注意信息泄漏与延迟成本。
""",
        ),
        "LangGraph与自研工作流高频面试题与知识点.md": mid(
            "LangGraph 与工作流",
            "LangGraph与自研工作流面渣级口述.md",
            "LangGraph与自研工作流卡片速记.md",
            "路径-AI工程.md",
            """## 图编排的动机

纯 while-Agent 难测难恢复。节点+边+显式状态：可分支、可人工节点、可回放。

## 自研底座

状态序列化、重试幂等、超时、死信、trace——比「调通一次 LLM」更重要。
""",
        ),
        "LLMOps与可观测高频面试题与知识点.md": mid(
            "LLMOps 与可观测",
            "LLMOps与可观测面渣级口述.md",
            "LLMOps与可观测卡片速记.md",
            "路径-AI工程.md",
            """## 为何 Ops

prompt/模型/索引都是可变配置；要版本、灰度、回滚、评测门禁。

## 观测

TTFT、总延迟、token、错误率、质量抽样、成本；trace 串检索与工具。
""",
        ),
        "推理部署高频面试题与知识点.md": mid(
            "推理部署",
            "推理部署面渣级口述.md",
            "推理部署卡片速记.md",
            "路径-AI工程.md",
            """## 延迟与吞吐

Batch 提利用率可能增等待；在线看 SLA。量化省显存；KV cache 加速解码；限流与多副本保稳定。
""",
        ),
        "Docker与K8s高频面试题与知识点.md": mid(
            "Docker 与 K8s",
            "Docker与K8s面渣级口述.md",
            "Docker与K8s卡片速记.md",
            "路径-Java后端.md",
            """## 容器

镜像固化依赖；容器是运行实例。

## K8s

Pod 最小调度单元；Deployment 管副本与发布；Service 稳入口；探针区分存活与就绪；request/limit 管资源噪音。
""",
        ),
        "Kafka高频面试题与知识点.md": mid(
            "Kafka",
            "Kafka面渣级口述.md",
            "Kafka卡片速记.md",
            "路径-Java后端.md",
            """## 分区

并行与顺序的基本单位；键决定分区则同键有序。Consumer Group 分摊分区。offset 记进度；ISR 与 ack 影响可靠性。
""",
        ),
        "RocketMQ高频面试题与知识点.md": mid(
            "RocketMQ",
            "RocketMQ面渣级口述.md",
            "RocketMQ卡片速记.md",
            "路径-Java后端.md",
            """## 特性动机

事务消息（半消息+回查）对齐本地事务；延时/定时消息服务催单等。选型结合生态与运维习惯。
""",
        ),
        "设计模式高频面试题与知识点.md": mid(
            "设计模式",
            "设计模式面渣级口述.md",
            "设计模式卡片速记.md",
            "路径-Java后端.md",
            """## 本质

管理变化点的可复用结构：创建解耦（工厂）、算法可替换（策略）、访问增强（代理/AOP）、流程骨架（模板方法）。讲场景与代价，不背类图。
""",
        ),
        "算法高频面试题与知识点.md": mid(
            "算法",
            "算法面渣级口述.md",
            "算法卡片速记.md",
            "路径-Java后端.md",
            """## 考什么

在约束下选结构与策略，分析复杂度，写对边界。工具箱：双指针、哈希、栈队列、树遍历、BFS/DFS、二分、堆、DP。先分类再写码。
""",
        ),
        "后端系统设计高频面试题与知识点.md": mid(
            "后端系统设计",
            "后端系统设计面渣级口述.md",
            "后端系统设计卡片速记.md",
            "路径-Java后端.md",
            """## 步骤

需求与量级 → API → 存储 → 主路径 → 缓存/异步/分片 → 一致性与降级。用数字驱动，避免空谈中间件。
""",
        ),
        "行为面试高频题与知识点.md": mid(
            "行为面试",
            "行为面试面渣级口述.md",
            "行为面试卡片速记.md",
            "路径-Java后端.md",
            """## STAR

Situation / Task / Action / Result。缺 Action 显空，缺 Result 不可信。冲突与失败谈学习与改进，不攻击他人。
""",
        ),
        "项目面试STAR高频题与知识点.md": mid(
            "项目面试 STAR",
            "项目面试面渣级口述.md",
            "项目面试STAR卡片速记.md",
            "路径-Java后端.md",
            """## 深挖点

角色、规模、难点权衡、你的具体动作、量化结果、复盘。准备 1～2 个主项目对齐简历。
""",
        ),
    }
    for k, v in files.items():
        w(k, v)


if __name__ == "__main__":
    main()
