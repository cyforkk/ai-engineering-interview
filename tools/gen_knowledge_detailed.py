# -*- coding: utf-8 -*-
"""重写知识点为「详解·所以然」。不碰面渣文件。"""
from pathlib import Path

DOCS = Path(__file__).resolve().parents[1] / "docs"


def w(name: str, text: str) -> None:
    p = DOCS / name
    p.write_text(text.strip() + "\n", encoding="utf-8")
    print("detail", name, p.stat().st_size)


def head(title: str, oral: str, card: str, path: str) -> str:
    return f"""# {title} · 知识点详解（所以然）

<!-- NAV:START -->
> **三分法**
>
> 📖 本文：**讲清楚、讲为什么**  
> 🗣️ 怎么说：[{oral}](./{oral})（面渣，不动）  
> 🃏 速记：[{card}](./{card})
>
> [首页](./README.md) · [如何使用](./如何使用本仓库.md) · [路径](./{path})
>
<!-- NAV:END -->

> **读法：** 先搞懂下面「为什么」，再去面渣练嘴，用卡片考前回忆。本文**不是**口语稿。

---
"""


# ========== JAVA ==========
JAVA = head("Java 基础与集合", "Java面渣级口述.md", "Java卡片速记.md", "路径-Java后端.md") + r'''
## 一、本专题在考什么

面试官表面上在问 `equals`、`String`、`HashMap`，实际在考三层能力：

1. **语言契约**：对象相等、哈希、不可变——这是集合正确性的前提  
2. **数据结构取舍**：数组、链表、树各自解决什么问题、付出什么代价  
3. **工程直觉**：线程安全、扩容、迭代失败——能不能接到线上事故  

---

## 二、==、equals、hashCode

### 是什么

| 概念 | 含义 |
|------|------|
| `==` | 基本类型比**值**；引用类型比**是不是同一个对象**（引用相等） |
| `equals` | `Object` 默认实现也是引用相等；子类可重写为**业务相等**（内容） |
| `hashCode` | 把对象映射成 `int`，供哈希表**先分桶** |

### 为什么需要 equals 契约

哈希表查找分两步：**先用 hash 找桶，再在桶内用 equals 确认**。  
若两个对象业务上相等（equals true），但 hashCode 不同，它们会进**不同桶**——`put` 进去的对象 `get` 时可能永远找不到。这不是风格问题，是**正确性**问题。

因此契约是：

- equals 为 true ⇒ hashCode **必须**相同  
- hashCode 相同 **不** ⇒ equals 一定 true（允许碰撞）

### 为什么默认 equals 用引用

通用对象没有「业务主键」概念；语言只能保守地按身份比。业务类（用户、订单）自己定义相等维度，并**成对**重写 equals/hashCode（或用 `record`/`Objects` 工具）。

### 边界

- 只用 IDE 生成 equals 却漏字段 → 逻辑错  
- 可变对象当 HashMap key 且改了参与 hash 的字段 → 丢数据  
- `hashCode` 分布差 → 碰撞多 → 链表/树变长 → 变慢  

**练嘴：** [Java面渣](./Java面渣级口述.md) · **速记：** [Java卡片](./Java卡片速记.md)

---

## 三、String 为什么设计成不可变

### 是什么

`String` 内容创建后不可改：类 `final`，内部字符数据不提供对外修改 API。`s = s + "x"` 是**新对象赋值**，不是原地改。

### 为什么这样设计（所以然）

1. **常量池可共享**  
   字面量可放入池，多处引用同一实例，省内存。若可变，一处修改会污染所有共享方——池化立刻不安全。

2. **hashCode 可缓存**  
   String 常作 HashMap key。不可变 ⇒ 内容不变 ⇒ hash 算一次可缓存。若可变，每次改内容都要让哈希表重新定位，几乎不可用。

3. **安全与线程安全**  
   类名、文件路径、网络参数、鉴权 token 若可被中途改写，安全模型崩溃。不可变天然无数据竞争。

### 拼接为什么用 StringBuilder

`+` 在循环里可能反复创建中间对象（编译器有时优化，不可依赖）。`StringBuilder` 在可变缓冲上追加，最后再 `toString`。`StringBuffer` 方法带同步，现代单线程拼接触用 Builder 即可。

### 边界

- `new String("a")`：字面量可能已在池；`new` 再在堆上造一个  
- `intern`：尝试返回池中引用（了解即可，别滥用）

---

## 四、HashMap（核心）

### 是什么

键值哈希表：平均 O(1) 级别查找/插入（在 hash 良好时）。JDK 8：`Node` 数组 + 链表，冲突严重时树化为红黑树。

### 为什么用哈希而不是每次遍历 List

数组按下标 O(1)，但键是任意对象，不能直接当地址。  
hash 把键**映射**到下标；冲突不可避免，所以桶内还要链表/树处理「多个键映到同一下标」。

### 为什么容量是 2 的幂

下标计算用 `(n - 1) & hash` 代替 `% n`：位运算更快，且在 2 的幂长度下等价于取模，分布相对均匀。扩容时也有「元素要么在原下标、要么在原下标+旧容量」的位运算性质，搬迁更清晰。

### put 路径（机制）

1. 计算 hash（扰动，减少高位无用）  
2. 算下标，桶空 → 直接放  
3. 桶头 key 相等 → 覆盖 value  
4. 否则沿链/树查找；没有则追加  
5. 链长达到阈值且表够长 → 树化  
6. 元素数超过 `threshold = 容量 × 负载因子(默认 0.75)` → 扩容为 2 倍并 rehash  

### 为什么要树化阈值 8、表长 64

- 短链时链表更简单、内存更省，常数更小  
- 泊松分布下，理想 hash 出现很长链的概率很低；8 是工程折中  
- 表还很短时优先**扩容摊平**，而不是急着为每个桶建树  

### 为什么默认线程不安全

单线程数据结构更简单更快。多线程并发 put 可能丢更新、出现数据竞争。需要并发时用 `ConcurrentHashMap`（分段/CAS+同步的演进实现），而不是多线程共用 HashMap。

### 负载因子 0.75 的权衡

- 更小：空间浪费，冲突少  
- 更大：更省空间，冲突与树化更多  
  0.75 是时间和空间的历史经验默认值。

**练嘴：** 面渣 HashMap 节 · **速记：** 卡片 4～8

---

## 五、ArrayList / 迭代

### ArrayList

底层扩容数组：随机访问 O(1)；尾部追加均摊 O(1)；中间插入要搬移。  
为何常用：缓存友好、实现简单、绝大多数业务「读多、尾部写」。

### fail-fast

用迭代器遍历时，若结构被**另一路径**修改（非迭代器自己的 remove），可能抛 `ConcurrentModificationException`。目的是尽早暴露并发/错误修改，而不是默默给错数据。并发容器的迭代语义通常是**弱一致**，不走同一套 CME 策略。

---

## 六、自测（懂所以然才算过）

- [ ] 能解释「equals true 必须同 hash」**为什么**绑 HashMap  
- [ ] 能解释 String 不可变的 **3 个设计动机**  
- [ ] 能讲 put、树化、2 的幂、线程不安全的 **原因** 而不只是结论  
- [ ] 卡片遮挡能回忆 · 面渣能说 2 分钟  

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 改为详解·所以然；口语拆到面渣；速记拆到卡片 |
'''

# ========== REDIS ==========
REDIS = head("Redis", "Redis面渣级口述.md", "Redis卡片速记.md", "路径-Java后端.md") + r'''
## 一、本专题在考什么

1. 性能从哪来（才能判断「该不该上 Redis」）  
2. 缓存三连能否**定义分开**并讲方案动机  
3. 分布式锁每一步参数的**必要性**  
4. 与 DB 一致性为何选主流做法  

---

## 二、为什么 Redis 快

| 因素 | 所以然 |
|------|--------|
| 内存 | 随机访问延迟远低于磁盘；热数据放内存是数量级差异 |
| 命令单线程执行 | 避免共享可变状态上的锁竞争与切换；逻辑简单正确 |
| 高效结构 | SDS、跳表、压缩列表等针对场景优化 CPU 与内存 |
| IO 多路复用 | 一线程盯多连接，不为一连接阻塞整站 |

**注意：** Redis 6+ 多线程主要加速**网络读写**，不要说成「多线程并行执行任意命令」。命令执行路径仍以单线程模型理解更安全。

### 五类型：为何不是只有 String

不同访问模式需要不同复杂度保证：排行需要有序+按分查 → ZSet（跳表等）；去重集合运算 → Set；字段局部更新 → Hash，避免每次整 JSON 读写。

---

## 三、缓存三连（必须讲清差异）

### 穿透：为什么会发生

缓存只对「曾经写过的 key」有意义。攻击或乱查**永远不存在**的 id，缓存永远 miss，流量打穿到 DB → DB 被拖垮。

**为何空值缓存有用：** 让「不存在」也成为可命中的结果，把打穿变成命中；必须短 TTL，防止恶意 key 占满内存。布隆过滤器在更前拦截「一定不存在」。

### 击穿：为什么是「一个热点」

热点 key 过期瞬间，成千上万请求同时回源重建，像针尖扎穿。  
与穿透差别：**数据本来存在**，只是失效窗口太大。

**互斥重建：** 只让一个请求加载 DB，其它等待或稍后重试——用正确性换 DB 保护。  
**逻辑过期：** 不真正删除，value 带逻辑过期时间，过期仍返回旧值并异步刷新——用短暂脏读换稳定。

### 雪崩：为什么是「一片」

大量 key **同一时刻**过期，或 Redis 集群故障，回源风暴或服务降级。  
TTL 加随机抖动：把过期曲线打散。高可用与多级缓存：降低「Redis 没了就全死」的概率。限流：保护 DB 活着。

---

## 四、分布式锁：每个参数为什么存在

```text
SET key unique_value NX EX seconds
```

| 要素 | 没有会怎样 |
|------|------------|
| NX | 无法互斥，多实例同时进临界区 |
| EX | 持锁进程崩溃 → 死锁 |
| unique value | 超时后 A 的锁过期，B 加锁，A 回来 del 会删掉 **B 的锁** |
| Lua 删 | get 与 del 非原子时仍有误删窗口 |

**续期：** 业务可能超过 EX，不续期会误释放。Redisson 看门狗是工程化续期。  
**主从风险：** 异步复制下主挂可能丢锁；要能讲清业务能否接受，而不是背「RedLock 绝对正确」。

---

## 五、Cache Aside：为何先 DB 再删缓存

目标：读多写少时，缓存是 DB 的加速视图。

- **写先 DB：** 以 DB 为权威真相  
- **再删缓存：** 下次读 miss 回源拿到新数据  

若先改缓存再写 DB：DB 失败时缓存已是新值 → 长期脏。  
若更新缓存而不是删：并发读写下更难推理「最终以谁为准」。删缓存是更简单的失效策略；删失败用重试/ binlog 补偿。强一致另论，成本高。

---

## 六、自测

- [ ] 三连能讲「差异的本质」而不背方案名词  
- [ ] 锁四要素每个能说「去掉会怎样」  
- [ ] 能解释为何不先更新缓存  

**面渣：** [Redis面渣级口述.md](./Redis面渣级口述.md) · **卡片：** [Redis卡片速记.md](./Redis卡片速记.md)
'''

# ========== MYSQL ==========
MYSQL = head("MySQL", "MySQL面渣级口述.md", "MySQL卡片速记.md", "路径-Java后端.md") + r'''
## 一、本专题在考什么

索引、事务、MVCC、慢 SQL——都是在问：你是否理解 **InnoDB 如何在正确性与性能之间做工程取舍**。

---

## 二、索引：为什么是 B+ 树

### 问题本质

磁盘 IO 一次很贵。要在千万行里找一行，必须**尽量少次 IO** 定位。树越矮、每层分叉越多，IO 越少。

### 为何偏 B+ 而不是二叉树/哈希

- 二叉树太高，IO 次数多  
- 哈希点查快但**范围查询、排序**弱  
- B+：内部节点主要存键与指针，叶子存数据（或主键）并顺序串联 → **点查与范围**都合适，贴合 SQL 的 `WHERE`/`ORDER BY`/`BETWEEN`

### 聚簇与回表的所以然

InnoDB 主键索引叶子就是**整行**（聚簇）——主键点查最快。  
二级索引叶子只存**主键值**，否则每个二级索引都复制整行，空间与维护成本爆炸。代价是：二级索引命中后往往还要再查聚簇 → **回表**。  
**覆盖索引**让查询列都在二级索引里，省掉回表，所以「联合索引列顺序」和「SELECT 列表」都值得设计。

### 最左前缀为什么

联合索引 (a,b,c) 在 B+ 里按 a、b、c 排序。没有 a 的条件时，索引无法从左边有序二分切入（一般情况），故「最左」。这是排序结构的必然，不是玄学规定。

### 失效的本质

优化器选「成本更低」的计划。对列套函数、类型转换、左模糊会让索引**无法按序定位**，成本变高，于是可能全表扫描。以 `EXPLAIN` 为准。

---

## 三、事务与隔离：为什么需要

并发事务交叉读写，会出现脏读、不可重复读、幻读。隔离级别是**正确性与并发度的旋钮**：级别越高锁/多版本成本越大，并发可能越差。

InnoDB 默认 **RR**，并配合 MVCC 与间隙锁等机制，在常见业务下平衡体验。

### MVCC 为什么能提高读并发

若每次读都加锁，读写互相堵。  
多版本：写生成新版本，读按 ReadView 看自己该看的旧/新版本 → **快照读**多数情况无需与写死锁式互斥。  
当前读（`FOR UPDATE`）必须看最新并阻止冲突写，所以要加锁。

---

## 四、日志三分：redo / undo / binlog

| 日志 | 解决什么问题 |
|------|----------------|
| redo | 崩溃后如何恢复已提交数据（WAL，先日志后落页） |
| undo | 回滚；MVCC 旧版本来源 |
| binlog | 复制、审计、时间点恢复（Server 层） |

两阶段提交思想保证 redo 与 binlog **同事务维度一致**，否则主从和崩溃恢复会对不齐。

---

## 五、慢 SQL 方法论为什么是这四步

1. **先量化**（慢日志）——避免凭感觉优化  
2. **EXPLAIN**——看是否用错计划  
3. **改索引/SQL**——对准根因  
4. **验证**——防「越优越慢」  

深分页慢：因为数据库仍可能扫描并丢弃大量行；`id > last` 把工作变成范围扫描起点推进。

---

## 六、自测

- [ ] B+ 相对哈希的优势能讲清  
- [ ] 回表与覆盖的成本逻辑  
- [ ] MVCC 如何换来读并发  
- [ ] 三日志各解决什么  

**面渣：** [MySQL面渣级口述.md](./MySQL面渣级口述.md) · **卡片：** [MySQL卡片速记.md](./MySQL卡片速记.md)
'''

# ========== JVM ==========
JVM = head("JVM", "JVM面渣级口述.md", "JVM卡片速记.md", "路径-Java后端.md") + r'''
## 一、本专题在考什么

概念（内存/GC/类加载）是基础；**社招分水岭**是：线上 OOM、Full GC、CPU 飙高时，你是否有**可复述的排查路径**，以及每一步为什么那样做。

---

## 二、运行时内存：为何这样切

| 区域 | 为何独立 |
|------|----------|
| 程序计数器 | 线程切换后要知道执行到哪；线程私有最简单 |
| 虚拟机栈 | 方法调用天然栈式；私有避免干扰 |
| 堆 | 对象共享、跨方法存活；GC 主战场 |
| 元空间 | 类元数据；移出堆避免永久代难调与易 OOM |
| 直接内存 | NIO 堆外，绕过部分堆限制，但受本机与容器 limit 约束 |

### 分代假说

「大多数对象朝生夕死」→ 新生代用复制算法廉价回收；活得久的进老年代，用不同策略。这是经验假设，不是数学定理，但与大量 Java 服务匹配。

---

## 三、GC：为何可达性而不是引用计数

引用计数对**循环引用**无能为力（A↔B 计数永不为 0 但已不可达）。可达性从 GC Roots 出发标记活对象，更符合语言语义。

### 算法与分代匹配

- 新生代活对象少 → **复制**到另一半，简单  
- 老年代活对象多 → **标记-整理/清除**，避免大量复制  
- G1：堆切 Region，可设定停顿目标，优先清垃圾多的 Region——为了**可控停顿**而不是极致吞吐 alone  

### Full GC 为何可怕

停顿更长、范围更大；频繁 Full GC 常是分配过快、晋升失败、老年代碎片或泄漏导致「越收越多」。

---

## 四、双亲委派：为什么

若任意应用 ClassLoader 都能先加载 `java.lang.String`，核心类可被替换 → 安全与一致性崩溃。  
先委派父加载：核心类由启动类加载器加载，保证唯一。  
破坏双亲（SPI、热部署、容器隔离）是为了**插件/隔离**需求，属于有意识的例外。

---

## 五、排查：为何这套顺序

```text
现象对齐时间 → 指标（GC/CPU/内存）→ 工具缩小范围 → 根因 → 止血与根治
```

- **先对齐时间**：避免把无关日志当原因  
- **GC 后 Old 降不降**：不降更像泄漏；降了又涨像分配过快  
- **dump 要在问题复现时**：事后空跑 dump 可能看不到元凶  
- **容器 limit**：只看 `-Xmx` 不够，RSS 含堆外与其他  

**完整场景口述：** [JVM面渣](./JVM面渣级口述.md)（含 TOP 场景）· **卡片：** [JVM卡片](./JVM卡片速记.md)

---

## 六、自测

- [ ] 能解释分代假说与算法匹配  
- [ ] 能解释双亲委派的安全动机  
- [ ] 能独立讲一条 OOM 排查链的**每步目的**  
'''

# ========== 并发 ==========
CONC = head("Java 并发", "并发面渣级口述.md", "并发卡片速记.md", "路径-Java后端.md") + r'''
## 一、本专题在考什么

并发的本质是：**多线程共享内存时，可见性、原子性、有序性**如何保证；以及线程池如何把「无限建线程」变成**受控资源**。

---

## 二、JMM 与 happens-before（所以然）

CPU 与编译器会重排序、会缓存，以提升性能。若无模型约束，线程 A 写的值线程 B 可能长期看不见。  

**happens-before** 是 Java 提供的**可见性偏序**保证：若 A hb B，则 A 的写对 B 可见。  
`synchronized` 解锁 hb 加锁；`volatile` 写 hb 读——这是工具能用的理论根。

### volatile

- **可见性：** 写尽快对其他线程可见  
- **有序性：** 插入内存屏障，防止特定重排  
- **不保证** `i++` 原子性——因为那是读改写多步  

适用：状态标志、双检锁中的发布（配合正确模式）等。

### synchronized / Lock

互斥保证临界区原子性，同时带来可见性（解锁发布）。  
`Lock` API 更丰富（可中断、尝试锁、多条件队列），实现多基于 AQS。

### AQS 为何存在

各种同步器（锁、信号量、CountDownLatch）都需要「等待队列 + 状态 state」。AQS 把队列与阻塞唤醒骨架抽出来，子类只定义 tryAcquire/tryRelease 语义，避免重复造轮子。

---

## 三、线程池：为什么不能 new Thread 飞

无界创建线程 → 内存打爆、调度 thrash、上下文切换爆炸。  
线程池：**复用**线程 + **有界**队列/最大线程 + **拒绝策略**，把负载变成可观测可降级。

### 提交流程的为什么

先 core 再队列再 max：  
- 希望平稳负载用核心线程  
- 突发先缓冲进队列，避免立刻膨胀线程  
- 队列满再增到 max，最后拒绝——每一层都是容量阀  

队列选大无界（如无界 LinkedBlockingQueue）可能导致 max 线程永远用不上、任务堆内存——这是常见误配。

---

## 四、ThreadLocal 为何漏内存

ThreadLocal 把变量挂在**线程**的 map 上。线程池线程**长期复用**，若不 `remove`，请求 A 的数据可能残留给请求 B，或对象无法回收（尤其在线程不退出时）。

---

## 五、自测

- [ ] volatile 能做什么不能做什么  
- [ ] 线程池参数每个阀的意义  
- [ ] 能画 AQS 排队直觉  

**面渣：** [并发面渣级口述.md](./并发面渣级口述.md) · **卡片：** [并发卡片速记.md](./并发卡片速记.md)
'''

# ========== SPRING ==========
SPRING = head("Spring", "Spring面渣级口述.md", "Spring卡片速记.md", "路径-Java后端.md") + r'''
## 一、IOC/DI 为什么存在

没有容器时：对象自己 `new` 依赖 → 高层绑死低层实现，难测、难换。  
**控制反转：** 创建与装配交给容器；**依赖注入：** 通过构造器等方式把依赖传入。好处是面向接口、可替换、可单测 mock。

---

## 二、AOP 为什么用代理

日志、事务、鉴权与业务正交（横切）。若每个方法手写，重复且易漏。  
AOP 在调用链外层套代理：调用前后织入逻辑。  
JDK 动态代理基于接口；无接口用 CGLIB 子类。自调用（`this.x()`）不经代理 → 事务注解失效，这是机制必然不是偶然。

---

## 三、事务失效的所以然

`@Transactional` 本质是代理拦截。以下情况拦截不到或语义不对：

- 同类自调用  
- 非 public（视配置）  
- 异常被 catch 吞掉未抛出  
- 传播行为/只读理解错  

---

## 四、循环依赖与三级缓存（直觉）

单例 A、B 互相依赖：需要「早期暴露半成品引用」才能打破构造环。  
构造器循环依赖仍难解，因为实例都创建不出来。理解「为何能解/不能解」比背三级缓存字段名更重要。

---

## 五、Spring Boot 自动配置为什么爽

约定大于配置：classpath 存在某类 + 条件注解 → 自动装配 Bean。  
Starter 聚合依赖；你专注业务属性。代价是「魔法」变多，出问题要会看条件报告与自动配置类。

**面渣：** [Spring面渣级口述.md](./Spring面渣级口述.md) · **卡片：** [Spring卡片速记.md](./Spring卡片速记.md)
'''

# ========== LLM ==========
LLM = head("LLM 基础常识", "LLM基础常识面渣级口述.md", "LLM基础常识卡片速记.md", "路径-AI工程.md") + r'''
## 一、为什么 AI 工程师必须懂「基础常识」

你不需要手推全套公式，但必须理解：**计费单位、长度限制、随机性、不可靠生成、训练阶段分工**——否则无法做超时、成本、评测与安全设计。

---

## 二、Token 与窗口：为什么不是「按字数」

模型词表按子词切分。中文常比英文更「碎」，同样业务文案 token 更多 → 更贵、更容易触顶。  

上下文窗口是**硬约束**：系统提示、历史、RAG 片段、工具结果、用户问题、预留输出都挤在一起。超窗不是「模型变笨」的玄学，是输入被截断或请求失败。  
所以工程上要：摘要、限 topK、控单段长度、分层记忆——都是在**抢窗口预算**。

---

## 三、温度：为什么影响「稳不稳」

解码时从概率分布采样。温度抬高分布更平 → 更随机；压低 → 更贪心确定。  
结构化抽取、工具参数、客服话术要**可复现** → 低温。  
创意可以升温，但生产事实任务升温 = 更多跑偏与幻觉空间。

---

## 四、幻觉：为什么「不可避免只能压」

目标是预测下一 token，不是检索真理数据库。训练数据有缺口、提示有诱导、解码有随机，都会产生**流畅的错**。  

所以工程答案不是「换个永不幻觉的模型」，而是：

- 检索 grounding + 引用  
- 低相关拒答  
- 规则/模式校验  
- 金标评测回归  

---

## 五、预训练 / SFT / 对齐：为什么分阶段

| 阶段 | 解决的问题 |
|------|------------|
| 预训练 | 语言能力与广博统计知识 |
| SFT | 听人话、按格式完成任务 |
| 偏好对齐 | 更有用、更安全、更符合人类偏好 |

业务知识日更 → **RAG** 比天天全量微调更划算；风格/格式稳定且数据够 → 才考虑 SFT。

**面渣：** [LLM基础常识面渣级口述.md](./LLM基础常识面渣级口述.md) · **卡片：** [LLM基础常识卡片速记.md](./LLM基础常识卡片速记.md)
'''

# ========== AGENT ==========
AGENT = head("Agent", "Agent面渣级口述.md", "Agent卡片速记.md", "路径-AI工程.md") + r'''
## 一、Agent 相对 Chat 多了什么

Chat：上下文 → 文本。  
Agent：目标 → **多步决策** → **调用工具**改变世界或获取信息 → 观察 → 再决策。  

公式：`Agent ≈ LLM + 规划 + 记忆 + 工具 + 停止条件/护栏`。

---

## 二、为什么不能处处 Agent

| 方式 | 优点 | 代价 |
|------|------|------|
| 工作流 | 可控、可测、便宜 | 僵硬 |
| Agent | 灵活、少硬编码分支 | 贵、慢、难测、易循环 |

合规、固定 SOP、成本敏感 → 工作流（或工作流套一点工具）。  
探索性任务、工具组合事先写不全 → 才值得 Agent。

---

## 三、ReAct 为什么有效，又为什么危险

交错「推理与行动」让模型基于真实观察调整，而不是一次幻觉到底。  

危险在于：模型可能重复无效动作、步数膨胀、工具失败后胡乱尝试。  
故 **max_steps、去重、超时、熔断、HITL** 不是附加题，是产品能否上线的必要条件。

---

## 四、工具调用：为什么执行权必须在服务端

模型只生成「我想调 X(参数)」的结构化意图。若让模型「自己说有权限就执行」，等于把授权交给不可信生成。  
服务端：鉴权、参数白名单、超时、审计、写操作审批——这是安全边界。

---

## 五、记忆为什么要分层

窗口有限且贵。短时靠对话；长时靠外部存储检索；中间状态要压缩。  
全量历史硬塞 = 贵、慢、噪声大、还可能串隐私。

**面渣：** [Agent面渣级口述.md](./Agent面渣级口述.md) · **卡片：** [Agent卡片速记.md](./Agent卡片速记.md)
'''

# ========== PYTHON ==========
PYTHON = head("Python", "Python面渣级口述.md", "Python卡片速记.md", "路径-Python.md") + r'''
## 一、默认参数陷阱：为什么会发生

函数定义时求值默认参数并绑在函数对象上。可变对象被多次调用共享 → 「脏默认」。  
这是语言定义时求值语义，不是 bug；用 `None` 哨兵在函数体内创建新对象。

---

## 二、GIL：为什么存在，影响什么

CPython 用 GIL 简化内存管理与 C 扩展安全：同一时刻多一线程执行 Python 字节码。  

- CPU 密集多线程：**加速有限**，应用多进程或把计算推到 C/外部  
- IO 等待：释放 GIL 或让出循环 → 线程/asyncio 仍有价值  

asyncio 用单线程协作式多任务处理大量网络等待；一旦协程里同步阻塞，**整个事件循环停摆**——这是机制问题。

---

## 三、装饰器 / 生成器 / with 的所以然

- 装饰器：横切逻辑复用，避免侵入业务函数  
- 生成器：惰性，避免大列表一次性进内存  
- with：用协议保证异常路径也释放资源，消灭漏 close  

**面渣：** [Python面渣级口述.md](./Python面渣级口述.md) · [FastAPI 长口述](./Python异步与FastAPI面渣级口述.md) · **卡片：** [Python卡片速记.md](./Python卡片速记.md)
'''

# ========== OS ==========
OS = head("操作系统", "操作系统面渣级口述.md", "操作系统卡片速记.md", "路径-Java后端.md") + r'''
## 一、进程/线程为何分开

进程提供**隔离的资源容器**（地址空间），崩溃与权限边界清晰。  
线程提供**廉价的并发执行流**，共享堆，适合同进程协作。  
代价：共享 ⇒ 必须同步。Java 线程共享堆，是并发 bug 的根源语境。

---

## 二、用户态/内核态为何切换贵

硬件与内核数据结构不能让任意用户代码乱写。系统调用陷入内核：存现场、鉴权、执行、返回。  
频繁小 IO 不缓冲 = 切换次数爆炸。高性能网络栈用缓冲、多路复用、零拷贝降低次数。

---

## 三、epoll 为何能支撑海量连接

一线程一连接阻塞模型线程数爆炸。  
多路复用：线程只处理**就绪**连接。select/poll 每次线性扫；epoll 用就绪队列，活跃连接场景更合适。Redis/Nginx/Netty 思想同源。

---

## 四、死锁四条件为何要记

预防时「破任一条件」即可：顺序加锁破环路、超时破不可抢占等待等。便于结构化回答而非只说「别嵌套锁」。

**面渣：** [操作系统面渣级口述.md](./操作系统面渣级口述.md) · **卡片：** [操作系统卡片速记.md](./操作系统卡片速记.md)
'''

# ========== NET ==========
NET = head("计算机网络", "计算机网络面渣级口述.md", "计算机网络卡片速记.md", "路径-Java后端.md") + r'''
## 一、TCP 可靠的代价

连接状态、确认、重传、排序、流量与拥塞控制 → 延迟与复杂度。  
UDP 把可靠性交给应用：音视频可容忍丢包时更合适。选型是权衡不是鄙视。

---

## 二、三次握手为什么是三次

要同步双方初始序号，并确认「我发你能收、你发我能收」。  
两次难以妥善处理**旧 SYN 延迟到达**造成的错误连接与资源占用；第三次让主动方确认。

---

## 三、TIME_WAIT 为什么存在

主动关闭方等待 2MSL：  
1. 最后 ACK 丢失时可让被动方重传 FIN 的场景收敛  
2. 让旧连接报文从网络消失，避免序列号混淆  

CLOSE_WAIT 堆积：多半是应用没 close，不是内核玄学。

---

## 四、HTTPS 为何「证书 + 对称加密」

证书链解决「你是不是真网站」（防中间人冒充）。  
非对称适合密钥交换但不适合大量数据；对称加密吞吐高，用于会话数据。

**面渣：** [计算机网络面渣级口述.md](./计算机网络面渣级口述.md) · **卡片：** [计算机网络卡片速记.md](./计算机网络卡片速记.md)
'''

# ========== shorter expansions for remaining ==========
def expand_short(title, oral, card, path, sections: str) -> str:
    return head(title, oral, card, path) + sections + f'''

---

**练嘴：** [{oral}](./{oral}) · **速记：** [{card}](./{card})
'''

AI_APP = expand_short("AI 应用工程", "AI应用工程面渣级口述.md", "AI应用工程卡片速记.md", "路径-AI工程.md", r'''
## 典型链路为何这样拆

接入（鉴权/限流）→ 编排（工作流/Agent）→ 模型与检索 → 后处理（格式/安全）→ 观测（trace/指标）。  
拆分是为了：**可替换模型、可独立限流、可定位慢点、可审计**。

## 流式与超时

用户对「首字时间」敏感，流式改善体感。超时要分层（网关/模型/工具），避免一次慢调用拖死线程或协程。

## 幂等与计费

重试会导致重复扣费或重复副作用；要用请求 ID、业务幂等键。
''')

EVAL_ = expand_short("AI 评测金标 SOP", "AI评测金标SOP面渣级口述.md", "AI评测金标SOP卡片速记.md", "路径-AI工程.md", r'''
## 为什么必须有金标集

LLM 输出非确定性；改 prompt「手感变好」可能在别的 case 静默变差。固定集 + rubric 才能回归。

## 离线与在线

离线可控可对比；在线反映真实分布与漂移。两者互补。

## 指标

正确/部分正确、拒答是否合适、引用是否可追溯、延迟与 token 成本——质量与商业一起看。
''')

VEC = expand_short("向量库与检索", "向量库面渣级口述.md", "向量库卡片速记.md", "路径-AI工程.md", r'''
## 为什么要 Embedding

符号匹配（关键词）不懂同义与语义。向量空间里语义相近的文本距离更近，支持语义检索。

## 为什么 ANN

精确最近邻在高维大规模太慢；近似算法（HNSW 等）用精度换延迟，工程可接受。

## Chunk 为何切

窗口与噪声：整本塞入既超窗又稀释主题。切块粒度影响召回——过大嘈杂，过小缺上下文。

## 混合检索

关键词抓专有名词精确命中；向量抓同义。融合排序常优于单一通路。
''')

PROMPT = expand_short("Prompt 工程", "Prompt工程面渣级口述.md", "Prompt工程卡片速记.md", "路径-AI工程.md", r'''
## 系统提示为何重要

设定角色、边界、输出合同（格式）。用户消息不可覆盖的安全边界应放在系统侧并配合服务端校验。

## 少样本与结构化

例子稳定格式；JSON/Schema 降低后处理成本。模板变量化才能测试与版本管理。
''')

SAFE = expand_short("Prompt 注入与 AI 安全", "Prompt注入与AI安全面渣级口述.md", "Prompt注入与AI安全卡片速记.md", "路径-AI工程.md", r'''
## 注入本质

不可信输入（用户或检索文档）试图改写模型目标，骗过工具调用或套出系统提示/数据。

## 为何要分层防御

单靠「提示里写不要泄露」不够。需：权限最小化、输出过滤、工具审批、监控告警、敏感数据不出域。
''')

LG = expand_short("LangGraph 与工作流", "LangGraph与自研工作流面渣级口述.md", "LangGraph与自研工作流卡片速记.md", "路径-AI工程.md", r'''
## 图为何比纯 while-Agent 更稳

节点与边显式化状态迁移，便于：断点恢复、人工节点、分支测试、追踪可视化。  
自研时状态序列化、重试幂等、超时与 trace 是底座，不是业务边角。
''')

OPS = expand_short("LLMOps 与可观测", "LLMOps与可观测面渣级口述.md", "LLMOps与可观测卡片速记.md", "路径-AI工程.md", r'''
## 为何 LLM 也要 Ops

提示、模型、索引、温度都是「会变的配置」。没有版本、评测、监控，就无法回滚与追责。

## 关键信号

TTFT/总延迟、token、错误码、质量抽样分、成本/请求、工具失败率。Tracing 串起检索与模型跨服务跨度。
''')

INF = expand_short("推理部署", "推理部署面渣级口述.md", "推理部署卡片速记.md", "路径-AI工程.md", r'''
## 吞吐与延迟张力

Batch 提高 GPU 利用率但可能增加等待；在线业务要权衡 SLA。量化、KV cache、连续批处理都是在显存与速度间取舍。
''')

K8S = expand_short("Docker 与 K8s", "Docker与K8s面渣级口述.md", "Docker与K8s卡片速记.md", "路径-Java后端.md", r'''
## 容器解决什么

「在我机器能跑」→ 镜像固化依赖。  
K8s 解决编排：副本、发布、服务发现、自愈、资源配额。  
探针区分「进程活着」与「能接流量」；request/limit 防止吵闹邻居。
''')

MQ = expand_short("消息队列", "MQ面渣级口述.md", "MQ卡片速记.md", "路径-Java后端.md", r'''
## 为何引入 MQ

同步链路把峰值与故障直接传导。MQ 提供解耦、削峰、异步重试。代价是：最终一致、重复消费、顺序与积压治理。

## 投递语义

至少一次 ⇒ 业务幂等必须有。恰好一次成本高，多靠幂等+去重逼近。
''')

KAFKA = expand_short("Kafka", "Kafka面渣级口述.md", "Kafka卡片速记.md", "路径-Java后端.md", r'''
## 分区为什么是核心

并行度、顺序（单分区有序）、扩展单位。键决定分区则同键有序。Consumer Group 分摊分区实现水平扩展。
''')

RMQ = expand_short("RocketMQ", "RocketMQ面渣级口述.md", "RocketMQ卡片速记.md", "路径-Java后端.md", r'''
## 常见考点动机

事务消息（半消息+回查）解决本地事务与发消息一致；延时消息服务催单等场景。与 Kafka 选型看生态、运维与特性契合度。
''')

MS = expand_short("微服务与分布式", "微服务与分布式面渣级口述.md", "微服务与分布式卡片速记.md", "路径-Java后端.md", r'''
## 拆分动机与代价

独立发布与扩展 vs 分布式复杂性（事务、观测、网络）。按业务边界拆，避免「分布式大单体」。

## 幂等与容错

网络会重试；接口必须幂等。熔断限流保护整体可用性，牺牲部分流量换系统存活。
''')

DP = expand_short("设计模式", "设计模式面渣级口述.md", "设计模式卡片速记.md", "路径-Java后端.md", r'''
## 模式是什么

可复用的结构与交互，用来管理变化点：创建解耦（工厂）、算法替换（策略）、增强（代理）、骨架（模板方法）。面试要讲**场景与权衡**，不是背 UML。
''')

ALGO = expand_short("算法", "算法面渣级口述.md", "算法卡片速记.md", "路径-Java后端.md", r'''
## 面试算法在考什么

在约束下选择结构与策略：时空复杂度、正确边界、代码清晰。双指针/哈希/树/BFS/DP 是工具箱，先分类再写。
''')

SD = expand_short("后端系统设计", "后端系统设计面渣级口述.md", "后端系统设计卡片速记.md", "路径-Java后端.md", r'''
## 推荐步骤为何有效

先澄清目标与量级，再 API 与存储，再热点路径，再瓶颈（缓存、分片、异步）。避免一上来「上微服务+Kafka」却无数字。
''')

BEHAV = expand_short("行为面试", "行为面试面渣级口述.md", "行为面试卡片速记.md", "路径-Java后端.md", r'''
## STAR 为何好用

面试官要的是可验证故事：情境、任务、你的行动、可量化结果。缺 Action 或 Result 会显空。
''')

STAR = expand_short("项目面试 STAR", "项目面试面渣级口述.md", "项目面试STAR卡片速记.md", "路径-Java后端.md", r'''
## 项目深挖在问什么

你是否真参与：能否讲权衡、数字、失败与复盘。准备 1～2 个主项目的难点 STAR，对齐简历用词。
''')


def main():
    mapping = {
        "Java高频面试题与知识点.md": JAVA,
        "Redis高频面试题与知识点.md": REDIS,
        "MySQL高频面试题与知识点.md": MYSQL,
        "JVM高频面试题与知识点.md": JVM,
        "并发高频面试题与知识点.md": CONC,
        "Spring高频面试题与知识点.md": SPRING,
        "LLM基础常识高频面试题与知识点.md": LLM,
        "Agent高频面试题与知识点.md": AGENT,
        "Python高频面试题与知识点.md": PYTHON,
        "操作系统高频面试题与知识点.md": OS,
        "计算机网络高频面试题与知识点.md": NET,
        "AI应用工程高频面试题与知识点.md": AI_APP,
        "AI评测金标SOP高频面试题与知识点.md": EVAL_,
        "向量库高频面试题与知识点.md": VEC,
        "Prompt工程高频面试题与知识点.md": PROMPT,
        "Prompt注入与AI安全高频面试题与知识点.md": SAFE,
        "LangGraph与自研工作流高频面试题与知识点.md": LG,
        "LLMOps与可观测高频面试题与知识点.md": OPS,
        "推理部署高频面试题与知识点.md": INF,
        "Docker与K8s高频面试题与知识点.md": K8S,
        "MQ高频面试题与知识点.md": MQ,
        "Kafka高频面试题与知识点.md": KAFKA,
        "RocketMQ高频面试题与知识点.md": RMQ,
        "微服务与分布式高频面试题与知识点.md": MS,
        "设计模式高频面试题与知识点.md": DP,
        "算法高频面试题与知识点.md": ALGO,
        "后端系统设计高频面试题与知识点.md": SD,
        "行为面试高频题与知识点.md": BEHAV,
        "项目面试STAR高频题与知识点.md": STAR,
    }
    for name, text in mapping.items():
        w(name, text)


if __name__ == "__main__":
    main()
