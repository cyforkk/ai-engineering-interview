# 算法高频 30 题 · 代码册（Java）

<!-- NAV:START -->
> **导航**
>
> 📍 **Java后端** · 第 21/29 步 · [完整路径](./路径-Java后端.md)
> ← [11.OS·述](./操作系统面渣级口述.md)
> → [13.系统设计](./后端系统设计面渣级口述.md)
>
> [首页](./README.md) · [AI路径](./路径-AI工程.md) · [Java路径](./路径-Java后端.md) · [Python路径](./路径-Python.md)
>
<!-- NAV:END -->

> **统一入口：** [算法入口.md](./算法入口.md) · 冲刺：[一周冲刺-算法.md](./一周冲刺-算法.md) · [T0 错因](./算法T0错因与口述.md)  
> **完整题单：** [频率总榜](./算法面试频率总榜.md) · [Hot100 分类](./算法Hot100频率分类.md)  
> 语言：**Java**（Python 岗对照逻辑自写）  
> 用法：先说思路 → 默写 → 对答案；**LC 题号**与总榜档见下表  

---

## 目录（题号对齐总榜）

| 册# | LC | 总榜 | 题目 | 类型 | T0错因 |
|:---:|:--:|:----:|------|------|:------:|
| 1 | **1** | T0·#1 | 两数之和 | 哈希 | [错因](./算法T0错因与口述.md) |
| 2 | **15** | T0·#12 | 三数之和 | 双指针 | [错因](./算法T0错因与口述.md) |
| 3 | **11** | T0·#17 | 盛最多水的容器 | 双指针 | [错因](./算法T0错因与口述.md) |
| 4 | **283** | T1 | 移动零 | 双指针 | — |
| 5 | **88** | T1 | 合并两个有序数组 | 双指针 | — |
| 6 | **206** | T0·#2 | 反转链表 | 链表 | [错因](./算法T0错因与口述.md) |
| 7 | **141** | T0·#14 | 环形链表 | 链表 | [错因](./算法T0错因与口述.md) |
| 8 | **160** | T1 | 相交链表 | 链表 | — |
| 9 | **19** | T1 | 删除倒数第 N 个节点 | 链表 | — |
| 10 | **21** | T0·#5 | 合并两个有序链表 | 链表 | [错因](./算法T0错因与口述.md) |
| 11 | **20** | T0·#3 | 有效括号 | 栈 | [错因](./算法T0错因与口述.md) |
| 12 | **232** | T3 | 用栈实现队列 | 栈 | — |
| 13 | **704** | — | 二分查找 | 二分 | — |
| 14 | **35** | T4 | 搜索插入位置 | 二分 | — |
| 15 | **104** | T0·#9 | 二叉树最大深度 | 树 | [错因](./算法T0错因与口述.md) |
| 16 | **102** | T0·#10 | 层序遍历 | 树 | [错因](./算法T0错因与口述.md) |
| 17 | **101** | T1 | 对称二叉树 | 树 | — |
| 18 | **226** | T0·#15 | 翻转二叉树 | 树 | [错因](./算法T0错因与口述.md) |
| 19 | **112** | — | 路径总和 | 树 | — |
| 20 | **98** | T1 | 验证二叉搜索树 | 树 | — |
| 21 | **215** | T1 | 第 K 个最大元素 | 堆 | — |
| 22 | **347** | T2 | 前 K 个高频元素 | 堆/哈希 | — |
| 23 | **3** | T0·#4 | 无重复字符的最长子串 | 窗口 | [错因](./算法T0错因与口述.md) |
| 24 | **53** | T0·#7 | 最大子数组和 | DP | [错因](./算法T0错因与口述.md) |
| 25 | **46** | T1 | 全排列 | 回溯 | — |
| 26 | **78** | T1 | 子集 | 回溯 | — |
| 27 | **560** | T1 | 和为 K 的子数组 | 前缀和 | — |
| 28 | **70** | T0·#8 | 爬楼梯 | DP | [错因](./算法T0错因与口述.md) |
| 29 | **121** | T0·#6 | 买卖股票的最佳时机 | 贪心/DP | [错因](./算法T0错因与口述.md) |
| 30 | **146** | T0·#11 | LRU 缓存 | 设计 | [错因](./算法T0错因与口述.md) |

**覆盖说明：** 本册含 T0 中 **14** 题（缺 200 岛屿、236 LCA、56 合并区间、322 零钱、42 接雨水等，冲刺 D 表补，见总榜）。  
**对照口诀：** 总榜排名看「T0·#n」；默写看「册#」；力扣打开用「LC」。

---

## 1. 两数之和 · LC 1 · T0#1

```java
// O(n) 时间 O(n) 空间
public int[] twoSum(int[] nums, int target) {
    Map<Integer, Integer> map = new HashMap<>();
    for (int i = 0; i < nums.length; i++) {
        int need = target - nums[i];
        if (map.containsKey(need)) return new int[]{map.get(need), i};
        map.put(nums[i], i);
    }
    return new int[0];
}
```

---

## 2. 三数之和 · LC 15 · T0#12

```java
// 排序 + 双指针 O(n^2)
public List<List<Integer>> threeSum(int[] nums) {
    Arrays.sort(nums);
    List<List<Integer>> res = new ArrayList<>();
    for (int i = 0; i < nums.length - 2; i++) {
        if (i > 0 && nums[i] == nums[i - 1]) continue;
        int l = i + 1, r = nums.length - 1;
        while (l < r) {
            int s = nums[i] + nums[l] + nums[r];
            if (s == 0) {
                res.add(Arrays.asList(nums[i], nums[l], nums[r]));
                while (l < r && nums[l] == nums[l + 1]) l++;
                while (l < r && nums[r] == nums[r - 1]) r--;
                l++; r--;
            } else if (s < 0) l++;
            else r--;
        }
    }
    return res;
}
```

---

## 3. 盛最多水的容器 · LC 11 · T0#17

```java
// 双指针 O(n)
public int maxArea(int[] h) {
    int l = 0, r = h.length - 1, ans = 0;
    while (l < r) {
        ans = Math.max(ans, Math.min(h[l], h[r]) * (r - l));
        if (h[l] < h[r]) l++;
        else r--;
    }
    return ans;
}
```

---

## 4. 移动零 · LC 283 · T1

```java
public void moveZeroes(int[] nums) {
    int j = 0;
    for (int i = 0; i < nums.length; i++) {
        if (nums[i] != 0) {
            int t = nums[i]; nums[i] = nums[j]; nums[j] = t;
            j++;
        }
    }
}
```

---

## 5. 合并两个有序数组 · LC 88 · T1

```java
public void merge(int[] nums1, int m, int[] nums2, int n) {
    int i = m - 1, j = n - 1, k = m + n - 1;
    while (j >= 0) {
        if (i >= 0 && nums1[i] > nums2[j]) nums1[k--] = nums1[i--];
        else nums1[k--] = nums2[j--];
    }
}
```

---

## 6. 反转链表 · LC 206 · T0#2

```java
public ListNode reverseList(ListNode head) {
    ListNode pre = null, cur = head;
    while (cur != null) {
        ListNode nxt = cur.next;
        cur.next = pre;
        pre = cur;
        cur = nxt;
    }
    return pre;
}
```

---

## 7. 环形链表 · LC 141 · T0#14

```java
public boolean hasCycle(ListNode head) {
    ListNode s = head, f = head;
    while (f != null && f.next != null) {
        s = s.next;
        f = f.next.next;
        if (s == f) return true;
    }
    return false;
}
```

---

## 8. 相交链表 · LC 160 · T1

```java
public ListNode getIntersectionNode(ListNode a, ListNode b) {
    if (a == null || b == null) return null;
    ListNode p = a, q = b;
    while (p != q) {
        p = (p == null) ? b : p.next;
        q = (q == null) ? a : q.next;
    }
    return p;
}
```

---

## 9. 删除倒数第 N 个节点 · LC 19 · T1

```java
public ListNode removeNthFromEnd(ListNode head, int n) {
    ListNode dummy = new ListNode(0, head);
    ListNode f = dummy, s = dummy;
    for (int i = 0; i <= n; i++) f = f.next;
    while (f != null) { f = f.next; s = s.next; }
    s.next = s.next.next;
    return dummy.next;
}
```

---

## 10. 合并两个有序链表 · LC 21 · T0#5

```java
public ListNode mergeTwoLists(ListNode a, ListNode b) {
    ListNode dummy = new ListNode(0), t = dummy;
    while (a != null && b != null) {
        if (a.val <= b.val) { t.next = a; a = a.next; }
        else { t.next = b; b = b.next; }
        t = t.next;
    }
    t.next = (a != null) ? a : b;
    return dummy.next;
}
```

---

## 11. 有效括号 · LC 20 · T0#3

```java
public boolean isValid(String s) {
    Deque<Character> st = new ArrayDeque<>();
    for (char c : s.toCharArray()) {
        if (c == '(') st.push(')');
        else if (c == '[') st.push(']');
        else if (c == '{') st.push('}');
        else if (st.isEmpty() || st.pop() != c) return false;
    }
    return st.isEmpty();
}
```

---

## 12. 用栈实现队列 · LC 232 · T3

```java
class MyQueue {
    Deque<Integer> in = new ArrayDeque<>(), out = new ArrayDeque<>();
    public void push(int x) { in.push(x); }
    public int pop() { peek(); return out.pop(); }
    public int peek() {
        if (out.isEmpty()) while (!in.isEmpty()) out.push(in.pop());
        return out.peek();
    }
    public boolean empty() { return in.isEmpty() && out.isEmpty(); }
}
```

---

## 13. 二分查找 · LC 704

```java
public int search(int[] nums, int target) {
    int l = 0, r = nums.length - 1;
    while (l <= r) {
        int m = l + (r - l) / 2;
        if (nums[m] == target) return m;
        if (nums[m] < target) l = m + 1;
        else r = m - 1;
    }
    return -1;
}
```

---

## 14. 搜索插入位置 · LC 35 · T4

```java
public int searchInsert(int[] nums, int target) {
    int l = 0, r = nums.length;
    while (l < r) {
        int m = l + (r - l) / 2;
        if (nums[m] < target) l = m + 1;
        else r = m;
    }
    return l;
}
```

---

## 15. 二叉树最大深度 · LC 104 · T0#9

```java
public int maxDepth(TreeNode root) {
    if (root == null) return 0;
    return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));
}
```

---

## 16. 层序遍历 · LC 102 · T0#10

```java
public List<List<Integer>> levelOrder(TreeNode root) {
    List<List<Integer>> res = new ArrayList<>();
    if (root == null) return res;
    Queue<TreeNode> q = new LinkedList<>();
    q.offer(root);
    while (!q.isEmpty()) {
        int sz = q.size();
        List<Integer> level = new ArrayList<>();
        for (int i = 0; i < sz; i++) {
            TreeNode n = q.poll();
            level.add(n.val);
            if (n.left != null) q.offer(n.left);
            if (n.right != null) q.offer(n.right);
        }
        res.add(level);
    }
    return res;
}
```

---

## 17. 对称二叉树 · LC 101 · T1

```java
public boolean isSymmetric(TreeNode root) {
    return root == null || mirror(root.left, root.right);
}
boolean mirror(TreeNode a, TreeNode b) {
    if (a == null || b == null) return a == b;
    return a.val == b.val && mirror(a.left, b.right) && mirror(a.right, b.left);
}
```

---

## 18. 翻转二叉树 · LC 226 · T0#15

```java
public TreeNode invertTree(TreeNode root) {
    if (root == null) return null;
    TreeNode t = root.left;
    root.left = invertTree(root.right);
    root.right = invertTree(t);
    return root;
}
```

---

## 19. 路径总和 · LC 112

```java
public boolean hasPathSum(TreeNode root, int targetSum) {
    if (root == null) return false;
    if (root.left == null && root.right == null) return root.val == targetSum;
    return hasPathSum(root.left, targetSum - root.val)
        || hasPathSum(root.right, targetSum - root.val);
}
```

---

## 20. 验证二叉搜索树 · LC 98 · T1

```java
public boolean isValidBST(TreeNode root) {
    return check(root, Long.MIN_VALUE, Long.MAX_VALUE);
}
boolean check(TreeNode n, long lo, long hi) {
    if (n == null) return true;
    if (n.val <= lo || n.val >= hi) return false;
    return check(n.left, lo, n.val) && check(n.right, n.val, hi);
}
```

---

## 21. 第 K 个最大元素 · LC 215 · T1

```java
public int findKthLargest(int[] nums, int k) {
    PriorityQueue<Integer> pq = new PriorityQueue<>(); // 小顶堆
    for (int x : nums) {
        pq.offer(x);
        if (pq.size() > k) pq.poll();
    }
    return pq.peek();
}
```

---

## 22. 前 K 个高频元素 · LC 347 · T2

```java
public int[] topKFrequent(int[] nums, int k) {
    Map<Integer, Integer> cnt = new HashMap<>();
    for (int x : nums) cnt.merge(x, 1, Integer::sum);
    PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[1] - b[1]);
    for (var e : cnt.entrySet()) {
        pq.offer(new int[]{e.getKey(), e.getValue()});
        if (pq.size() > k) pq.poll();
    }
    int[] ans = new int[k];
    for (int i = k - 1; i >= 0; i--) ans[i] = pq.poll()[0];
    return ans;
}
```

---

## 23. 无重复字符的最长子串 · LC 3 · T0#4

```java
public int lengthOfLongestSubstring(String s) {
    Map<Character, Integer> last = new HashMap<>();
    int ans = 0, left = 0;
    for (int i = 0; i < s.length(); i++) {
        char c = s.charAt(i);
        if (last.containsKey(c)) left = Math.max(left, last.get(c) + 1);
        last.put(c, i);
        ans = Math.max(ans, i - left + 1);
    }
    return ans;
}
```

---

## 24. 最大子数组和 · LC 53 · T0#7

```java
public int maxSubArray(int[] nums) {
    int cur = nums[0], ans = nums[0];
    for (int i = 1; i < nums.length; i++) {
        cur = Math.max(nums[i], cur + nums[i]);
        ans = Math.max(ans, cur);
    }
    return ans;
}
```

---

## 25. 全排列 · LC 46 · T1

```java
public List<List<Integer>> permute(int[] nums) {
    List<List<Integer>> res = new ArrayList<>();
    boolean[] used = new boolean[nums.length];
    dfs(nums, used, new ArrayList<>(), res);
    return res;
}
void dfs(int[] nums, boolean[] used, List<Integer> path, List<List<Integer>> res) {
    if (path.size() == nums.length) { res.add(new ArrayList<>(path)); return; }
    for (int i = 0; i < nums.length; i++) {
        if (used[i]) continue;
        used[i] = true; path.add(nums[i]);
        dfs(nums, used, path, res);
        path.remove(path.size() - 1); used[i] = false;
    }
}
```

---

## 26. 子集 · LC 78 · T1

```java
public List<List<Integer>> subsets(int[] nums) {
    List<List<Integer>> res = new ArrayList<>();
    dfs(0, nums, new ArrayList<>(), res);
    return res;
}
void dfs(int i, int[] nums, List<Integer> path, List<List<Integer>> res) {
    res.add(new ArrayList<>(path));
    for (int j = i; j < nums.length; j++) {
        path.add(nums[j]);
        dfs(j + 1, nums, path, res);
        path.remove(path.size() - 1);
    }
}
```

---

## 27. 和为 K 的子数组 · LC 560 · T1

```java
public int subarraySum(int[] nums, int k) {
    Map<Integer, Integer> map = new HashMap<>();
    map.put(0, 1);
    int sum = 0, ans = 0;
    for (int x : nums) {
        sum += x;
        ans += map.getOrDefault(sum - k, 0);
        map.merge(sum, 1, Integer::sum);
    }
    return ans;
}
```

---

## 28. 爬楼梯 · LC 70 · T0#8

```java
public int climbStairs(int n) {
    if (n <= 2) return n;
    int a = 1, b = 2;
    for (int i = 3; i <= n; i++) {
        int c = a + b; a = b; b = c;
    }
    return b;
}
```

---

## 29. 买卖股票的最佳时机 · LC 121 · T0#6

```java
public int maxProfit(int[] prices) {
    int min = Integer.MAX_VALUE, ans = 0;
    for (int p : prices) {
        min = Math.min(min, p);
        ans = Math.max(ans, p - min);
    }
    return ans;
}
```

---

## 30. LRU 缓存 · LC 146 · T0#11

```java
class LRUCache extends LinkedHashMap<Integer, Integer> {
    int cap;
    public LRUCache(int capacity) {
        super(capacity, 0.75f, true);
        this.cap = capacity;
    }
    public int get(int key) { return super.getOrDefault(key, -1); }
    public void put(int key, int value) { super.put(key, value); }
    @Override
    protected boolean removeEldestEntry(Map.Entry<Integer, Integer> e) {
        return size() > cap;
    }
}
```

手写双向链表版见 [Java面试追问与手写题.md](./Java面试追问与手写题.md)。

---

## 辅助类型

```java
class ListNode {
    int val; ListNode next;
    ListNode() {}
    ListNode(int v) { val = v; }
    ListNode(int v, ListNode n) { val = v; next = n; }
}
class TreeNode {
    int val; TreeNode left, right;
    TreeNode() {}
    TreeNode(int v) { val = v; }
}
```

---

## 练习建议

```text
每天 2～3 题默写；不会的只背模板 3 天后再默
优先保证：6 反转链表、13 二分、16 层序、23 无重复子串、21 TopK
```

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-21 | 高频 30 题 Java 代码册初版 |
