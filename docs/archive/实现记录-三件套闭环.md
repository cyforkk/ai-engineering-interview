# 实现记录：三件套闭环（算法入口 + 验收模拟 + 侧栏/CI）

## 背景

针对仓库评审中的三项优先优化：

1. 算法入口页 + 一周算法冲刺  
2. Python 模拟 + 行为/STAR 15 问  
3. 侧栏收敛 + 死链检查  

## 新增文档

| 文件 | 作用 |
|------|------|
| `docs/算法入口.md` | 算法唯一入口；语言策略 Python 模板 / Java 默写 |
| `docs/一周冲刺-算法.md` | 7 天 T0 向冲刺 + D7 自测 10 题 |
| `docs/模拟面试-Python20问.md` | Python 验收 20 问 |
| `docs/模拟面试-行为STAR15问.md` | 行为+STAR 15 问 + 0/1/2 打分 |
| `tools/check_md_links.py` | 相对 md 死链检查 + 薄知识点告警 |
| `.github/workflows/docs-check.yml` | push/PR 跑死链检查 |

## 导航调整

- `_sidebar`：① 区增加算法冲刺与 Python/行为模拟；② 工具区算法三件套收成「算法入口」  
- README / 如何使用 / 进度清单 / 三路径 / 各冲刺包交叉链接  
- 算法分册顶栏统一指向算法入口  

## 验证

```text
python tools/check_md_links.py
```

- 无 MISSING 则 exit 0；THIN 仅为告警不失败  

## 未做（刻意）

- 未批量加厚 AI 分册薄页  
- 未把 Kafka/Rocket 合并进 MQ  
- 未做 Anki/间隔重复  

## 使用建议

岗位冲刺白天走 Java/AI/Python；有笔试晚上/周末走算法冲刺；终面前做行为 STAR 15。
