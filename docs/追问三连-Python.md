# 追问三连 · Python / 异步 / FastAPI

> 限时 30～45 秒/问。  
> 配套：[Python高频面试题与知识点.md](./Python高频面试题与知识点.md) · [Python异步与FastAPI面渣级口述.md](./Python异步与FastAPI面渣级口述.md)

---

## P0

### 基础

| 主问方向 | # | 追问 | 要点 |
|----------|---|------|------|
| 可变性 | 1 | 元组里有 list 算可变吗？ | 元组不可变指元素 identity；list 内容可变 |
| | 2 | 默认参数为何别用 list？ | 定义时创建一次，调用共享 |
| | 3 | 浅拷贝改内层？ | 内外层共享，两边都变 |
| 装饰器 | 1 | wraps 干什么？ | 保留 __name__ 文档 |
| | 2 | 带参装饰器几层？ | 通常三层 |
| | 3 | 类装饰器？ | `__call__` 包装 |
| 生成器 | 1 | 和 list 推导区别？ | 惰性 vs 立刻占内存 |
| | 2 | 生成器能二次遍历吗？ | 一般一次性耗尽 |
| | 3 | yield from？ | 委托子生成器 |
| GIL | 1 | 多线程完全没用？ | IO 仍有用 |
| | 2 | 如何吃满多核？ | 多进程/C 扩展/其它解释器 |
| | 3 | asyncio 能绕过 GIL 算 CPU 吗？ | 不能，算仍占循环 |
| is vs == | 1 | 何时用 is？ | None、单例 |
| | 2 | 小整数 is 为真？ | 驻留，不可依赖 |

### 异步 / FastAPI

| 主问方向 | # | 追问 | 要点 |
|----------|---|------|------|
| 事件循环 | 1 | time.sleep 会怎样？ | 堵死循环 |
| | 2 | 同步 requests 放哪？ | to_thread / 换 httpx |
| | 3 | gather 一个失败？ | 默认可取消其它；return_exceptions |
| 并发控制 | 1 | 如何限流调模型？ | Semaphore |
| | 2 | 超时？ | wait_for |
| | 3 | 取消任务？ | cancel + CancelledError |
| FastAPI | 1 | Depends 用途？ | 鉴权、资源注入 |
| | 2 | 流式怎么做？ | StreamingResponse / SSE |
| | 3 | 多 worker 注意？ | 内存不共享；模型×进程 |
| 后台 | 1 | BackgroundTasks 可靠吗？ | 不可靠，重要事用队列 |
| | 2 | 长文档解析？ | 异步任务 job_id |
| | 3 | 客户端断开流式？ | 取消上游，省钱 |

---

## P1

| 方向 | 追问 | 要点 |
|------|------|------|
| MRO | 菱形继承？ | C3 / __mro__ |
| classmethod | 和 static 区别？ | cls 工厂 vs 无绑定 |
| 类型注解 | 运行时强制吗？ | 默认不强制；工具/校验用 |
| 虚拟环境 | 为何必须？ | 依赖隔离可复现 |
| pytest | fixture？ | 预置依赖 |
| 异步锁 | 要不要 Lock？ | 共享复杂状态时需要 |

---

## 场景三连

**主问：设计 AI 查询接口**

| # | 追问 | 要点 |
|---|------|------|
| 1 | 如何防模型把 worker 占满？ | 超时、Semaphore、队列 |
| 2 | 如何观测？ | request_id、阶段耗时、token |
| 3 | 密钥放哪？ | 环境变量/KMS，不进日志 |

---

## 修订

| 日期 | 说明 |
|------|------|
| 2026-07-20 | Python 轨追问三连初版 |
