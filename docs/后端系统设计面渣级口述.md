# 后端系统设计 · 面渣级口述（加长版）

> [完整卷](./后端系统设计高频面试题与知识点.md) · [场景题](./场景题高频面试题与知识点.md) · [频率](./系统设计八股频率排序.md) · [结构图](./核心结构图.md)

---

## 一、我怎么答设计题（先亮框架）

我固定用六步，避免东拉西扯：

1. **澄清需求**：功能边界 + 非功能（QPS、数据量、一致性、可用性、延迟）  
2. **容量粗算**：读写 QPS、存储量级  
3. **画架构**：接入 → 网关 → 服务 → 缓存 → DB → MQ → 存储  
4. **关键路径深入**：最难的一致性、热点、超卖等  
5. **瓶颈与优化**  
6. **兜底与监控**：降级、对账、告警  

大厂要的是 trade-off，不是唯一标准答案。

---

## 二、澄清我会主动问什么

- 读写比、峰值与均值、数据保留多久  
- 是否允许最终一致、能否接受主从延迟  
- 主要客户端、是否有强合规  

粗算例子：日活 × 人均请求 × 峰均比，得到峰值 QPS；再算存储条数 × 单条大小 × 副本。

---

## 三、通用高并发积木（1 分钟）

无状态服务水平扩展；CDN 与缓存挡读；MQ 削峰；限流熔断降级；分库分表；幂等与对账；全链路追踪。  
每一块都要说清代价：缓存有一致性问题，MQ 有重复与积压，分库分表有跨库查询成本。

---

## 四、秒杀 2 分钟版

**目标：** 十万人抢一百件，核心是防超卖、扛尖峰、体验可接受。

分层：  
- 前端：按钮防抖、验证码、静态化  
- 网关：令牌桶限流、黑名单  
- 服务：活动校验、用户级限购  
- Redis：预热库存，Lua 原子扣减  
- MQ：异步创建订单，削峰  
- DB：条件更新 stock>0 兜底，订单唯一  

防重复下单：幂等 token + 唯一索引。  
热点：库存分段、本地缓存。  
失败：回补库存、对账任务。  
监控：限流触发量、扣减失败率、下单延迟、超卖对账。

---

## 五、其它场景锚点（方便追问切换）

| 场景 | 一句话 |
|------|--------|
| 短链 | 发号 Base62 + 映射缓存 + 302 |
| 分布式 ID | 雪花/号段，处理时钟回拨 |
| 限流 | 令牌桶；分布式用 Redis+Lua |
| Feed | 推拉结合 |
| 支付 | 状态机 + 幂等单号 |

长文见各「系统设计口述-*.md」。

---

## 六、收尾

先框架后细节；每个方案说为什么、缺点、兜底；尽量落到自己项目里的限流、缓存、异步经验。


---

## 面试临场补充（再加 1 分钟）

### 如果面试官说「再展开一点」

我会补三点：第一，关键参数或配置叫什么、大概怎么设；第二，失败时系统如何表现、用户侧如何降级；第三，我用什么指标判断方案有效，比如延迟、错误率、超卖次数、评测分数。

### 如果面试官问「有没有坑」

任何缓存、MQ、容器、模型方案都有坑。我会主动说一个真实或典型坑，以及怎么发现、怎么修、怎么防再发生。这样比只背正确步骤更像干过活。

### 和项目怎么挂钩

即使项目不是标准秒杀或标准 Agent，也可以迁移：限流、幂等、异步、检索、配置版本、发布回滚，都是同一套肌肉。面试前把简历里的两点业务，映射到本专题的两个问题上。
